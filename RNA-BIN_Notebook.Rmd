---
title: "RADICL-seq Notebook"
author: "Federico Agostini <federico.agostini@crick.ac.uk>"
date: "19 July 2017"
output:
   html_notebook:
      fig_align: center
      code_folding: hide
editor_options: 
  chunk_output_type: console
---

```{r setup, include=FALSE}
require("knitr")
knitr::opts_chunk$set(dev='CairoPNG', dpi=300, fig_height=480, fig_width=800)
```

## Session information: {-}

```{r setwd, echo=FALSE, warning=FALSE}
working.folder = "/Users/agostif/Desktop/Bonetti/"

while( !dir.exists(working.folder) )
   working.folder = readline(prompt="Working folder not found! Please enter a valid path: ")
if( substr(working.folder, nchar(working.folder), nchar(working.folder)) != "/")
   working.folder = paste0(working.folder, "/")

# setwd(working.folder)

# knitr::opts_chunk$set(root.dir=working.folder)

cat(sessionInfo()$R.version$version.string, fill=TRUE)
cat(paste("Platform", sessionInfo()$platform), fill=TRUE)
cat(paste("Running under", sessionInfo()$running), fill=TRUE)
cat(paste("Last knitted on", date()), fill=TRUE)
cat(paste("Working directory set to", working.folder), fill=TRUE)

```

```{r setParams}
reads.quality.min = 37 # Reads with RNA and DNA mapQ >= to this number will be retained
reads.length.min = 30  # Reads with RNA and DNA length >= to this number will be retained
```

```{r packages, message=FALSE, results="hide"}
require("BSgenome.Mmusculus.UCSC.mm10")
require("GenomicFeatures")
require("data.table")
require("ggpubr")
require("RColorBrewer")
require("gridExtra")
require("DT")
require("UpSetR")
require("VennDiagram")
require("eulerr")
require("ComplexHeatmap")
require("circlize")
require("Cairo")
require("png")
require("ggthemes")
require("cowplot")
require("scales")
require("ggsci")
require("viridis")
require("ggforce")
require("GGally")
require("Rmpfr")
require("CAGEr")

require("fedefun")
```

## Load the packages: {-}

  * BSgenome.Mmusculus.UCSC.mm10
  * GenomicFeatures
  * data.table
  * RColorBrewer
  * ggpubr
  * gridExtra
  * DT
  * VennDiagram
  * eulerr
  * UpSetR
  * ComplexHeatmap
  * circlize
  * Cairo
  * viridis

Custom function: _read.paired.bed()_

The function reads a non-standard BED file and convert it into a GRangesList object containing the RNA (first element in the list) and DNA (second element in the list) information. It also stores additional columns, though these will be removed as they are not useful for the analysis.

```{r readPairedBed, echo=TRUE}
read.paired.bed <- function(file, min.mapq=reads.quality.min, min.length=reads.length.min){
   column.names = paste(c("chr", "start", "stop", "score", "strand"), rep(c("R", "D"), each=5), sep=".")
   column.types = rep(c("character", "numeric", "numeric", "character", "numeric", "character"), 2)
   if( grepl("*.gz", file) ){
      bed = fread(cmd=paste("gunzip -c", file), select=c(1:3, 5:9, 11:12),
                  col.names=column.names,
                  colClasses=c(column.types, rep("character", 3)), showProgress=FALSE)
   }else{
      bed = fread(file, select=c(1:3, 5:9, 11:12),
                  col.names=column.names,
                  colClasses=c(column.types, rep("character", 3)), showProgress=FALSE)
   }
   
   bed = bed[score.R>=min.mapq & score.D>=min.mapq]
   bed = bed[(stop.R-start.R+1)<min.length & (stop.D-start.D+1)<min.length]
   
   bed = GRangesList(resize(with(bed,
                          GRanges(chr.R, IRanges(start.R, stop.R), strand.R, score=score.R,
                                  index=1:nrow(bed))), width=1, fix="center"),
                     resize(with(bed,
                          GRanges(chr.D, IRanges(start.D, stop.D), strand.D, score=score.D,
                                  index=1:nrow(bed))), width=1, fix="center"))
   
   return(bed)
}
```

# Reference annotation

Gencode genes are not selected for any level of annotation, which means that 1, 2 and 3 reliability levels are included.

```{r loadNewAnnotation}
if(!exists("gencode_version"))
   gencode_version = "m14"

load(paste0("GencodeReference/mm10_Gencode", toupper(gencode_version), "_annotations.RData"))

genes = sort.GenomicRanges(with(gene.metadata,
                                GRanges(seqnames, IRanges(start, end), strand, gene_id, gene_type, gene_name)),
                           ignore.strand=TRUE)
genes.dt = data.table(as.data.frame(genes), index=seq_along(genes), key="index")

tx.regions = readRDS(paste0("GencodeReference/mm10_Gencode", toupper(gencode_version), "_annotations.all.genes.regions.rds"))
tx.regions.dt = data.table(as.data.frame(tx.regions), index=1:length(tx.regions), key="index")

blacklist = fread(cmd=paste("gunzip -c", file.path(working.folder, "mm10.blacklist.bed.gz")), col.names=c("chr", "start", "end"))
blacklist.gr = with(blacklist, GRanges(chr, IRanges(start, end)))
```

## Non-annotated stem cell specific transcripts (NASTs) 

__REF:__ [Fort A. _et al._, Deep transcriptome profiling of mammalian stem cells supports a regulatory role for retrotransposons in pluripotency maintenance, _Nature Genetics_, 2014.](http://www.nature.com/ng/journal/v46/n6/abs/ng.2965.html)

```{r nastRNAs}
nast.dt = fread("Carninci_Mm3_CAGE_NAST_lifted.bed", header=FALSE,
                sel=c(1:3,6,4,7), col.names=c("chr", "start", "end", "strand", "name", "compartment"))
nast.gr = with(nast.dt, GRanges(chr, IRanges(start, end), strand, name, compartment))

# NASTs are resized to a length of 1000 nt
nast.gr = resize(nast.gr, width=width(nast.gr)+1e3, fix="start")

# NASTs overlapping Gencode annotated features are discarded
nast.gr = sort.GenomicRanges(nast.gr[!overlapsAny(nast.gr, tx.regions)], ignore.strand=TRUE)
mcols(nast.gr) = data.frame(gene_id=paste("NAST", seq_along(nast.gr), sep="_"),
                            gene_type="NAST", gene_name=nast.gr$name, annotation="NAST", type="exon")

# NASTs are added to the gene list
genes = sort.GenomicRanges(c(genes, nast.gr[, 1:3]), ignore.strand=TRUE)
genes.dt = data.table(as.data.frame(genes), index=seq_along(genes), key="index")

# NASTs are added to the genic regions
tx.regions = sort.GenomicRanges(c(tx.regions, nast.gr), ignore.strand=TRUE)
tx.regions.dt = data.table(as.data.frame(tx.regions), index=1:length(tx.regions), key="index")
```

```{bash, eval=FALSE, echo=FALSE}
# 1 Mb bins
bedtools makewindows -g GRCm38.primary_assembly.genome.fa.fai -w 100000 -i winnum > GRCm38.genome.100KB.bed
# 25 Kb bins
bedtools makewindows -g GRCm38.primary_assembly.genome.fa.fai -w 25000 -i winnum > GRCm38.genome.25KB.bed
# 10 Kb bins
bedtools makewindows -g GRCm38.primary_assembly.genome.fa.fai -w 10000 -i winnum > GRCm38.genome.10KB.bed
```

```{r readChromosomes}
bin_width = "25KB"

mm10 = fread(paste0("GRCm38.genome.", bin_width, ".bed"), col.names=c("seqnames", "start", "end", "genomic_bin"))

mm10 = with(mm10, GRanges(seqnames, IRanges(start, end), strand="*", genomic_bin))
mm10 = resize(mm10, width=width(mm10)-1, fix="start")

mm10.dt = data.table(as.data.frame(mm10))
mm10.dt[, `:=`(bin_id = paste(seqnames, genomic_bin, sep="_"), index=seq_along(genomic_bin))]

mm10$bin_id = mm10.dt[, bin_id]

setkey(mm10.dt, index)
```

```{r very_old_processEnhancer, eval=FALSE}
enhancer = fread(paste0(working.folder, "mouse_enhancers.midpoint.mm10.bed"), col.names=c("chr", "start", "end", "name"))

enhancer = with(enhancer, GRanges(chr, IRanges(start, end), "*", name))

enhancer.name = enhancer$name

enhancer = list(resize(enhancer, width=1e3, fix="start"),
                flank(enhancer, width=1e3, start=TRUE, both=FALSE))
strand(enhancer[[1]]) = "+"
strand(enhancer[[2]]) = "-"

enhancer[[1]]$name = paste("EnhancerF", seq_along(enhancer[[1]]), sep="_")
enhancer[[2]]$name = paste("EnhancerR", seq_along(enhancer[[2]]), sep="_")

enhancer[[1]]$old_name = enhancer.name
enhancer[[2]]$old_name = enhancer.name

enhancer = do.call(c, enhancer)

# Remove enhancers overlapping with annotated genes (or NASTs)
enhancer = enhancer[!overlapsAny(enhancer, tx.regions),]

# Remove overlapping enhancers
enhancer = enhancer[-as.data.table(findOverlaps(enhancer))[, .N, queryHits][N>1, queryHits],]
```

```{r old_processEnhancer, eval=FALSE}
enhancer = fread(paste0(working.folder, "F5.mm10.enhancers.bed"), select=c(1,4,7), col.names=c("chr", "name", "start"))

enhancer = with(enhancer, GRanges(chr, IRanges(start, width=1), "*", name))

enhancer.name = enhancer$name

# enhancer = list(IRanges::shift(resize(enhancer, width=1e3, fix="start"), 200),
#                 IRanges::shift(flank(enhancer, width=1e3, start=TRUE, both=FALSE), -200)
enhancer = list(resize(enhancer, width=1e3, fix="start"),
                flank(enhancer, width=1e3, start=TRUE, both=FALSE))
strand(enhancer[[1]]) = "+"
strand(enhancer[[2]]) = "-"

enhancer[[1]]$name = paste("EnhancerF", seq_along(enhancer[[1]]), sep="_")
enhancer[[2]]$name = paste("EnhancerR", seq_along(enhancer[[2]]), sep="_")

enhancer[[1]]$old_name = enhancer.name
enhancer[[2]]$old_name = enhancer.name

enhancer = do.call(c, enhancer)

# Remove enhancers overlapping with annotated genes (or NASTs)
enhancer = enhancer[!overlapsAny(enhancer, tx.regions),]

# Remove overlapping enhancers
enhancer = enhancer[-as.data.table(findOverlaps(enhancer))[, .N, queryHits][N>1, queryHits],]

# fwrite(as.data.table(enhancer), file=paste0(working.folder, "Enhancers_divergent_reference.tsv"), sep="\t", row.names=FALSE)
```

```{r processEnhancer}
# enhancer = fread(paste0(working.folder, "novel.mESC.enhancers.bed"), select=c(1,4,7), col.names=c("chr", "name", "start"))
# enhancer = fread(paste0(working.folder, "novel.mOPC.enhancers.bed"), select=c(1,4,7), col.names=c("chr", "name", "start"))
enhancer = fread(paste0(working.folder, "merge.enhancers.F5.CAGEfightR.bed"), select=c(1,4,7), col.names=c("chr", "name", "start"))

enhancer = with(enhancer, GRanges(chr, IRanges(start, width=1), "*", name))

enhancer.name = enhancer$name

# enhancer = list(IRanges::shift(resize(enhancer, width=1e3, fix="start"), 200),
#                 IRanges::shift(flank(enhancer, width=1e3, start=TRUE, both=FALSE), -200)
enhancer = list(resize(enhancer, width=1e3, fix="start"),
                flank(enhancer, width=1e3, start=TRUE, both=FALSE))
strand(enhancer[[1]]) = "+"
strand(enhancer[[2]]) = "-"

enhancer[[1]]$name = paste("EnhancerF", seq_along(enhancer[[1]]), sep="_")
enhancer[[2]]$name = paste("EnhancerR", seq_along(enhancer[[2]]), sep="_")

enhancer[[1]]$old_name = enhancer.name
enhancer[[2]]$old_name = enhancer.name

enhancer = do.call(c, enhancer)

# Remove enhancers overlapping with annotated genes (or NASTs)
enhancer = enhancer[!overlapsAny(enhancer, tx.regions),]

# Remove overlapping enhancers
enhancer = enhancer[-as.data.table(findOverlaps(enhancer))[, .N, queryHits][N>1, queryHits],]
```

### Enzymatic treatment

```{r processEnzymatic, eval=FALSE}
files = list.files(file.path(working.folder, "Enzymatic"), full.names=TRUE, pattern="*.bed.gz$")
files = setNames(files, tstrsplit(files, "\\.|_|/")[[8]])

interactions = list()
for( i in seq_along(files) ){

      bed = read.paired.bed(files[i], min.mapq=reads.quality.min, min.length=reads.length.min)
      
      # RNA
      overlaps.rna = as.data.table(findOverlaps(bed[[1]], tx.regions, ignore.strand=FALSE))
      setkey(overlaps.rna, subjectHits)
      overlaps.rna = overlaps.rna[tx.regions.dt[, list(index, gene_id.R=gene_id, annotation.R=annotation, gene_type.R=gene_type, region.R=type)], nomatch=0][, subjectHits := NULL]
      setkey(overlaps.rna, queryHits)
      # Multigenic
      overlaps.rna[queryHits%in%overlaps.rna[, .N, by="queryHits"][N>1, queryHits], `:=`(gene_id.R="Multigenic", annotation.R="Multigenic", gene_type.R="Multigenic", region.R="Multigenic")]
      overlaps.rna = unique(overlaps.rna)
      # Intergenic
      missing.rna = data.table(queryHits=setdiff(1:length(bed[[1]]), overlaps.rna[, queryHits]))
      sel = overlapsAny(bed[[1]][missing.rna[, queryHits],], tx.regions)
      missing.rna[sel,  `:=`(gene_id.R="Genic_unassigned", annotation.R="Genic_unassigned", gene_type.R="Genic_unassigned", region.R="Genic_unassigned")]
      missing.rna[!sel, `:=`(gene_id.R="Intergenic", annotation.R="Intergenic", gene_type.R="Intergenic", region.R="Intergenic")]
      
      overlaps.rna = rbindlist(list(overlaps.rna, missing.rna))
      setkey(overlaps.rna, queryHits)
      
      # DNA
      overlaps.dna = data.table(as.data.frame(findOverlaps(bed[[2]], mm10, ignore.strand=TRUE)), key="subjectHits")
      overlaps.dna = overlaps.dna[mm10.dt[, list(index, gene_id.D=bin_id)], nomatch=0][, subjectHits := NULL]
      setkey(overlaps.dna, queryHits)
      # Multigenic
      overlaps.dna[queryHits%in%overlaps.dna[, .N, by="queryHits"][N>1, queryHits], gene_id.D := "Multigenic"]
      overlaps.dna = unique(overlaps.dna)
      # Intergenic
      missing.dna = data.table(queryHits=setdiff(1:length(bed[[2]]), overlaps.dna[, queryHits]))
      sel = overlapsAny(bed[[2]][missing.dna[, queryHits],], mm10)
      missing.dna[, gene_id.D := ifelse(sel, "Bin_unassigned", "Intergenic")]
      
      bed = lapply(bed, function(x) data.table(as.data.frame(x), key="index"))
      
      overlaps.rna = overlaps.rna[bed[[1]][, list(index, chr.R=seqnames, start.R=start, end.R=end, strand.R=strand)], nomatch=0]
      setcolorder(overlaps.rna, c(1, 6:9,  2:5))
      overlaps.dna = overlaps.dna[bed[[2]][, list(index, chr.D=seqnames, start.D=start, end.D=end)], nomatch=0]
      setcolorder(overlaps.dna, c(1, 3:5,  2))
      
      rm(bed); gc()
      
      overlaps = overlaps.rna[overlaps.dna, nomatch=0][, queryHits := NULL]
      overlaps[, region.R := droplevels(region.R)]
      
      rm(overlaps.rna, overlaps.dna); gc()
      
      interactions = append(interactions, list(overlaps))
      
      rm(overlaps)
      gc()
   
}

names(interactions) = names(files)

# Concatenate the list
interactions = rbindlist(interactions, idcol="name")

saveRDS(interactions,
        file.path(working.folder, paste0("RData/Enzymatic_overlaps_", gencode_version, "_level_1_2_3_AllOverlapsUnique.rds")))
```

```{r statsEnzymatic, eval=FALSE}
interactions = readRDS(file.path(working.folder, paste0("RData/Enzymatic_overlaps_", gencode_version, "_level_1_2_3_AllOverlapsUnique.rds")))

sel.R = !overlapsAny(with(interactions, GRanges(chr.R, IRanges(start.R, end.R))), blacklist.gr)
sel.D = !overlapsAny(with(interactions, GRanges(chr.D, IRanges(start.D, end.D))), blacklist.gr)

interactions = interactions[sel.R & sel.D,]

interactions[, distance := abs(start.R-end.D)]

subset = interactions[distance<=1e3, .N, by=c("name", "distance")]
subset = rbindlist(list(subset, with(subset, expand.grid(name=unique(name), distance=0:1e3, N=0))))
subset = subset[, list(N=sum(N)), by=c("name", "distance")]
subset = subset[order(distance)]
subset[, cs := cumsum(N)/sum(N), by="name"]
subset[name%in%"NoEnzyme", name := "None"][name%in%"RNaseH", name := "H"][, name := factor(name, levels=c("None", "S1", "V1", "H"))]

gg = ggplot(subset[distance>0], aes(x=distance, y=cs, colour=name)) +
   geom_line() +
   scale_x_continuous("RNA and DNA tags distance (bp)") +
   scale_y_continuous("Cumulative distribution function (CDF)", limits=c(0,1)) +
   scale_colour_brewer("RNAse treatment", palette="Set1") +
   theme_light() + theme(legend.position=c(0.75, 0.25), legend.box.background = element_rect())
save_plot(gg, filename=paste0(working.folder, "plots/RNA-BIN_", bin_width, "_enzymatic_distance_between_tags_1kb.pdf"), base_height=4, base_aspect_ratio=1.1)

binDistance = as.character(cut(interactions[, distance], breaks=c(-1, 1e3, 1e4, 1e6, Inf), labels=c("<1Kb", "1-10Kb", "10Kb-1Mb", ">1Mb")))
binDistance[interactions[, chr.R!=chr.D]] = "Interchromosomal" 
interactions[, distance := factor(binDistance, levels=c(c("<1Kb", "1-10Kb", "10Kb-1Mb", ">1Mb", "Interchromosomal")))]

dt = interactions[, .N, by=c("name", "distance")][, list(distance, N, P=N/sum(N)), by="name"][order(distance)]
dt[name%in%"NoEnzyme", name := "None"][name%in%"RNaseH", name := "H"][, name := factor(name, levels=c("None", "S1", "V1", "H"))]

gg = ggplot(dt, aes(x=distance, y=P, fill=name)) +
   geom_bar(stat="identity", position="dodge", col="black") +
   scale_fill_brewer("RNAse treatment", palette="Set1") +
   scale_x_discrete("Distance between RNA and DNA tags") +
   scale_y_continuous("Dataset fraction", limits=c(0,1), labels=scales::percent) +
   theme_bw() + theme(legend.position=c(0.1, 0.85), legend.box.background = element_rect())
save_plot(gg, filename=paste0(working.folder, "plots/RNA-BIN_", bin_width, "_enzymatic_distance_between_tags.pdf"), base_height=6, base_aspect_ratio=1.5)

prom = promoters(genes, upstream=1000, downstream=1000)
prom = prom[prom$gene_type%in%"protein_coding",]

subset = interactions[gene_id.R%in%prom$gene_id,]
subset = subset[overlapsAny(with(subset, GRanges(chr.D, IRanges(start.D, width=1), strand="*")), prom),]

overlaps = as.data.table(findOverlaps(with(subset, GRanges(chr.D, IRanges(start.D, width=1), strand="*")), prom))
subset[, gene_id.D := prom[overlaps[!duplicated(queryHits), subjectHits],]$gene_id]

subset = subset[as.character(gene_id.R)==gene_id.D]
subset[, distance := abs(start.R-start.D)]
subset[, bin := findInterval(distance, c(seq(0, 5, 1)*1e3, Inf), all.inside=TRUE)]

subset = subset[, list(Count = .N), by=c("name", "bin")]
subset[, Percentage := Count/sum(Count), by=c("name")]

subset[name%in%"NoEnzyme", name := "None"][name%in%"RNaseH", name := "H"][, name := factor(name, levels=c("None", "S1", "V1", "H"))]

gg = ggplot(subset, aes(x=as.numeric(bin), y=Percentage, fill=name)) +
   ggtitle("Protein-coding RNA interactions with their promoter region (+/- 1Kb from TSS)") +
   geom_bar(stat="identity", position="dodge", colour="black") +
   scale_x_continuous("RNA and DNA tags distance (binned)",
                      breaks=1:6, labels=c("< 1Kb", "1-2Kb", "2-3Kb", "3-4Kb", "4-5Kb", "5Kb-gene end")) +
   scale_y_continuous("Averaga fraction of RNA tags") +
   scale_fill_brewer("RNAse treatment", palette="Set1") +
   theme_light()
save_plot(gg, filename=paste0(working.folder, "plots/RNA-BIN_", bin_width, "_enzymatic_distance_RNAs_and_promoters.pdf"), base_height=6, base_aspect_ratio=1.5)
```

## RADICL-seq analyses (RNA and genomic bins)

```{r readTable}
sampleTable = fread("sampleTable.tsv",
                    header=FALSE, col.names=c("Experiment", "Name", "Group", "Barcode", "Technical", "Path"))
sampleTable[, Path := paste0(working.folder, Path)] 

if( !all(file.exists(sampleTable[, Path])) )
   stop("One or more files cannot be found! Please check the paths in the sampleTable.tsv file.")

sampleTable[, c("Condition", "Replicate") := tstrsplit(Group, "\\.")][, Replicate := as.numeric(Replicate)]

sampleTable[, Label := paste(Experiment, Condition, Replicate, sep="_")]
setkey(sampleTable, Label)
```

### Read files

The _read.paired.bed()_ function is employed to parse the files containing the paired RNA-DNA reads. For each BED file, the corresponding gene names are assigned to the RNA and DNA fragments (these are already present in the input BED files but they correspond to a very old Gencode genes annotation). Pairs for which it is not possible to assign either of the framents to a unique position on the genome (i.e., BWA mapQ score < 37). Cases where the RNA fragment could not be assigned to an annotated features were marked _intergenic_, while fragments mapping to more than one gene, which could lead to double counting or misassignment, were marked _multigenic_. Note, the latter case should not occur considering that the annotation was designed to avoid such issue.

_WARNING: This chunk is labour-intensive. If the outputs have already been generated, make sure they are in the correct folder and have the correct name to avoid regenerating them all._

```{r qualityDistribution, eval=FALSE}
check.paired.bed <- function(file, min.mapq=reads.quality.min, min.length=reads.length.min){
   column.names = paste(c("chr", "start", "stop", "score", "strand"), rep(c("R", "D"), each=5), sep=".")
   column.types = rep(c("character", "numeric", "numeric", "character", "numeric", "character"), 2)
   if( grepl("*.gz", file) ){
      bed = fread(cmd=paste("gunzip -c", file), select=c(1:3, 5:9, 11:12),
                  col.names=column.names,
                  colClasses=c(column.types, rep("character", 3)), showProgress=FALSE)
   }else{
      bed = fread(file, select=c(1:3, 5:9, 11:12),
                  col.names=column.names,
                  colClasses=c(column.types, rep("character", 3)), showProgress=FALSE)
   }
   
   R37 = bed[, score.R>=min.mapq]
   D37 = bed[, score.D>=min.mapq]
   count = nrow(bed)
   rm(bed)
   
   tab = data.table(both37 = sum(R37&D37), rna37 = sum(R37&!D37), dna37 = sum(D37&!R37), none37 = sum(!D37&!R37), total=count)
   
   return(tab)
}

tab0 = lapply(setNames(unique(sampleTable[Label%like%"GRID_mESC_GRIDseq", Label]),
                       unique(sampleTable[Label%like%"GRID_mESC_GRIDseq", Label])),
              function(lbl){
                 
                 # Iterate through the individual files for the group
                 tab0.1 = lapply(setNames(sampleTable[Label%in%lbl, Path],
                                        basename(sampleTable[Label%in%lbl, Path])),
                               function(pth)
                                  check.paired.bed(pth, min.mapq=reads.quality.min, min.length=reads.length.min))
                 rbindlist(tab0.1, idcol="technical")
              })
tab0 = rbindlist(tab0, idcol="replicate")

fwrite(tab0, file=file.path(working.folder, "Quality_counts_GRID_raw.tsv"), append=FALSE, quote=FALSE, sep="\t")

t0 = data.table(sample = tab0[, replicate], tab0[, 3:6, with=FALSE]/tab0[, total])
t0 = t0[, list(both37 = mean(both37), rna37 = mean(rna37), dna37 = mean(dna37), none37 = mean(none37)), by="sample"]
fwrite(t0, file=file.path(working.folder, "Quality_percentage_GRID_raw.tsv"), append=FALSE, quote=FALSE, sep="\t")


# Iterate through the groups
tab1 = lapply(setNames(unique(sampleTable[Label%like%"RADICL_mESC_1FA", Label]),
                       unique(sampleTable[Label%like%"RADICL_mESC_1FA", Label])),
              function(lbl){
                 
                 # Iterate through the individual files for the group
                 tab2 = lapply(setNames(sampleTable[Label%in%lbl, Path],
                                        basename(sampleTable[Label%in%lbl, Path])),
                               function(pth)
                                  check.paired.bed(pth, min.mapq=reads.quality.min, min.length=reads.length.min))
                 rbindlist(tab2, idcol="technical")
              })
tab1 = rbindlist(tab1, idcol="replicate")

fwrite(tab1, file=file.path(working.folder, "Quality_counts_raw.tsv"), append=FALSE, quote=FALSE, sep="\t")

# Iterate through the groups
trimmed = lapply(setNames(unique(sampleTable[Label%like%"RADICL_mESC_1FA", Label]),
                       unique(sampleTable[Label%like%"RADICL_mESC_1FA", Label])),
              function(lbl){
                 
                 # Iterate through the individual files for the group
                 tab2 = lapply(setNames(gsub("1FA_n", "1FAn",
                                             gsub("/16", "_TRIMMED/16",
                                                  gsub("RNA-DNA", "Trimmed", sampleTable[Label%in%lbl, Path]))),
                                        basename(sampleTable[Label%in%lbl, Path])),
                               function(pth)
                                  check.paired.bed(pth, min.mapq=reads.quality.min, min.length=reads.length.min))
                 rbindlist(tab2, idcol="technical")
              })
trimmed = rbindlist(trimmed, idcol="replicate")

fwrite(trimmed, file=file.path(working.folder, "Quality_counts_trimmed.tsv"), append=FALSE, quote=FALSE, sep="\t")

t1 = data.table(sample = tab1[, replicate], tab1[, 3:6, with=FALSE]/tab1[, total])
t1 = t1[, list(both37 = mean(both37), rna37 = mean(rna37), dna37 = mean(dna37), none37 = mean(none37)), by="sample"]
t2 = data.table(sample = trimmed[, replicate], trimmed[, 3:6, with=FALSE]/trimmed[, total])
t2 = t2[, list(both37 = mean(both37), rna37 = mean(rna37), dna37 = mean(dna37), none37 = mean(none37)), by="sample"]

fwrite(t1, file=file.path(working.folder, "Quality_percentage_raw.tsv"), append=FALSE, quote=FALSE, sep="\t")
fwrite(t2, file=file.path(working.folder, "Quality_percentage_trimmed.tsv"), append=FALSE, quote=FALSE, sep="\t")
```

```{r readInteractions}
if( !dir.exists(paste0(working.folder, "processed")) )
   dir.create(paste0(working.folder, "processed"), recursive=TRUE)

# Iterate through the groups
for( lbl in unique(sampleTable[, Label]) ){
   
   # Skip the iteration if the final file already exists
   if( file.exists(paste0(working.folder, "processed/RNA-BIN_", lbl, "_", gencode_version, "_level_1_2_3_Unique_", bin_width, ".tsv.gz")) )
      next
   
   # Create an empty list
   interactions = list()
   
   # Iterate through the individual files for the group
   for( pth in sampleTable[Label%in%lbl, Path] ){
      
      bed = read.paired.bed(pth, min.mapq=reads.quality.min, min.length=reads.length.min)
      
      # RNA
      overlaps.rna = as.data.table(findOverlaps(bed[[1]], tx.regions, ignore.strand=FALSE))
      setkey(overlaps.rna, subjectHits)
      overlaps.rna = overlaps.rna[tx.regions.dt[, list(index, gene_id.R=gene_id, annotation.R=annotation, gene_type.R=gene_type, region.R=type)], nomatch=0][, subjectHits := NULL]
      setkey(overlaps.rna, queryHits)
      # Multigenic
      overlaps.rna[queryHits%in%overlaps.rna[, .N, by="queryHits"][N>1, queryHits], `:=`(gene_id.R="Multigenic", annotation.R="Multigenic", gene_type.R="Multigenic", region.R="Multigenic")]
      overlaps.rna = unique(overlaps.rna)
      # Intergenic
      missing.rna = data.table(queryHits=setdiff(1:length(bed[[1]]), overlaps.rna[, queryHits]))
      sel = overlapsAny(bed[[1]][missing.rna[, queryHits],], tx.regions)
      missing.rna[sel,  `:=`(gene_id.R="Genic_unassigned", annotation.R="Genic_unassigned", gene_type.R="Genic_unassigned", region.R="Genic_unassigned")]
      missing.rna[!sel, `:=`(gene_id.R="Intergenic", annotation.R="Intergenic", gene_type.R="Intergenic", region.R="Intergenic")]
      
      overlaps.rna = rbindlist(list(overlaps.rna, missing.rna))
      setkey(overlaps.rna, queryHits)
      
      # DNA
      overlaps.dna = data.table(as.data.frame(findOverlaps(bed[[2]], mm10, ignore.strand=TRUE)), key="subjectHits")
      overlaps.dna = overlaps.dna[mm10.dt[, list(index, gene_id.D=bin_id)], nomatch=0][, subjectHits := NULL]
      setkey(overlaps.dna, queryHits)
      # Multigenic
      overlaps.dna[queryHits%in%overlaps.dna[, .N, by="queryHits"][N>1, queryHits], gene_id.D := "Multigenic"]
      overlaps.dna = unique(overlaps.dna)
      # Intergenic
      missing.dna = data.table(queryHits=setdiff(1:length(bed[[2]]), overlaps.dna[, queryHits]))
      sel = overlapsAny(bed[[2]][missing.dna[, queryHits],], mm10)
      missing.dna[, gene_id.D := ifelse(sel, "Bin_unassigned", "Intergenic")]
      
      bed = lapply(bed, function(x) data.table(as.data.frame(x), key="index"))
      
      overlaps.rna = overlaps.rna[bed[[1]][, list(index, chr.R=seqnames, start.R=start, end.R=end, strand.R=strand)], nomatch=0]
      setcolorder(overlaps.rna, c(1, 6:9,  2:5))
      overlaps.dna = overlaps.dna[bed[[2]][, list(index, chr.D=seqnames, start.D=start, end.D=end)], nomatch=0]
      setcolorder(overlaps.dna, c(1, 3:5,  2))
      
      rm(bed); gc()
      
      overlaps = overlaps.rna[overlaps.dna, nomatch=0][, queryHits := NULL]
      overlaps[, region.R := droplevels(region.R)]
      
      rm(overlaps.rna, overlaps.dna); gc()
      
      interactions = append(interactions, list(overlaps))
      
      rm(overlaps)
      gc()
      
   }
   
   # Merge all the interactions of the group
   interactions = rbindlist(interactions)
   
   # Write all interactions
   writeGzip(interactions,
             file=paste0(working.folder, "processed/RNA-BIN_", lbl, "_", gencode_version, "_level_1_2_3_Unique_", bin_width, ".tsv"),
             col.names=TRUE, showProgress=TRUE, wait=TRUE, level=9)
   
   # Write genic (remove multi- and inter-genic RNAs) interactions
   writeGzip(interactions[!(gene_id.R%in%c("Multigenic", "Intergenic") | gene_id.D%in%c("Multigenic", "Intergenic"))],
             file=paste0(working.folder, "processed/RNA-BIN_", lbl, "_", gencode_version, "_level_1_2_3_Unique_", bin_width, "_GenicOnly.tsv"),
             col.names=TRUE, showProgress=TRUE, wait=TRUE, level=9)
}
```

```{r DNAcoverageFunction}
read.paired.bed.2 <- function(file, min.mapq=reads.quality.min, min.length=reads.length.min){
   column.names = paste(c("chr", "start", "stop", "score", "strand"), rep(c("R", "D"), each=5), sep=".")
   column.types = rep(c("character", "numeric", "numeric", "character", "numeric", "character"), 2)
   if( grepl("*.gz", file) ){
      bed = fread(cmd=paste("gunzip -c", file), select=c(1:3, 5:9, 11:12),
                  col.names=column.names,
                  colClasses=c(column.types, rep("character", 3)), showProgress=TRUE)
   }else{
      bed = fread(file, select=c(1:3, 5:9, 11:12),
                  col.names=column.names,
                  colClasses=c(column.types, rep("character", 3)), showProgress=TRUE)
   }
   
   bed = bed[score.R>=min.mapq & score.D>=min.mapq]
   bed = bed[(stop.R-start.R+1)<min.length & (stop.D-start.D+1)<min.length]
   return(bed)   
}
```

```{r subsampledMappedCoverage, eval=FALSE}
if( !dir.exists(file.path(working.folder, "step_coverage")) )
   dir.create(file.path(working.folder, "step_coverage"))

sample.names = setNames(unique(sampleTable[, Label]), unique(sampleTable[, Label]))

# Iterate through the groups
tab = rbindlist(lapply(sample.names,
                       function(lbl){
                          
                          print(lbl)
                          
                          if( !file.exists(file.path(working.folder, "step_coverage", paste0("step_coverage_", lbl, ".tsv"))) ){
                             # Iterate through the individual files for the group
                             interactions = lapply(sampleTable[Label%in%lbl, Path],
                                                   function(pth)
                                                      read.paired.bed.2(pth))
                             interactions = rbindlist(interactions)
                             interactions = interactions[chr.R%in%paste0("chr", c(1:19, "X", "Y")) & chr.D%in%paste0("chr", c(1:19, "X", "Y"))]
                             tmp.gr = with(interactions, GRanges(chr.R, IRanges(start.R, stop.R)))
                             interactions = interactions[!overlapsAny(tmp.gr, blacklist.gr),]
                             tmp.gr = with(interactions, GRanges(chr.D, IRanges(start.D, stop.D)))
                             interactions = interactions[!overlapsAny(tmp.gr, blacklist.gr),]
                             rm(tmp.gr)
                             dt = data.table(readsN=numeric(), covDNA=numeric())
                             for( i in seq(1e6, nrow(interactions), 1e6) ){
                                subset = interactions[sample(1:nrow(interactions), i),]
                                dt = rbind(dt, data.table(readsN=subset[, .N],
                                                          # uniRNA=subset[grepl("ENSMUS", gene_id.R), length(unique(gene_id.R))],
                                                          # uniInt=subset[, .N, by=c("gene_id.R", "gene_id.D")][, .N],
                                                          covDNA=with(subset, sum(sum(coverage(reduce(GRanges(chr.D, IRanges(start.D, stop.D)))))))))
                                rm(subset); gc()
                             }
                             dt = rbind(dt,
                                        data.table(readsN=interactions[, .N],
                                                   # uniRNA=interactions[grepl("ENSMUS", gene_id.R), length(unique(gene_id.R))],
                                                   # uniInt=interactions[, .N, by=c("gene_id.R", "gene_id.D")][, .N],
                                                   covDNA=with(interactions, sum(sum(coverage(reduce(GRanges(chr.D, IRanges(start.D, stop.D)))))))))
                             rm(interactions); gc()
                             fwrite(dt, file=file.path(working.folder, "step_coverage", paste0("step_coverage_", lbl, ".tsv")), append=FALSE, quote=FALSE, sep="\t")
                             
                          }else{
                           dt = fread(file.path(working.folder, "step_coverage", paste0("step_coverage_", lbl, ".tsv")))  
                           }
                          return(dt)
                       }), idcol="Set")

fwrite(tab, file=file.path(working.folder, "Step_coverage_all_datasets.tsv"), append=FALSE, quote=FALSE, sep="\t")

# tab = rbindlist(list(with(tab, expand.grid(Set=unique(Set), readsN=0, covDNA=1)), tab))
tab = fread(file.path(working.folder, "Step_coverage_all_datasets.tsv"))

tab[, c("Technique", "Cell", "Condition", "Replicate") := tstrsplit(gsub("4h_ActD", "4h-ActD", Set), "_")]
tab[, N := .N, by=c("Technique", "Cell", "Condition", "readsN")]

gg = ggplot(tab[Condition%in%c("GRIDseq", "1FA") & Cell%in%"mESC" & N>=2,
                list(covDNA = mean(covDNA), sdCov = sd(covDNA)),
                by=c("Technique", "Cell", "Condition", "readsN")],
            aes(x=readsN, y=covDNA, fill=Technique)) +
   geom_line(aes(colour=Technique)) +
   geom_errorbar(aes(ymin=(covDNA-sdCov), ymax=(covDNA+sdCov), col=Technique), width=.2) +
   geom_point(pch=21, colour="black") +
   scale_x_continuous("Number of reads") +
   scale_y_log10("DNA tags genomic coverage (bp)", labels=scientific) +
   scale_colour_manual(values=tableau_color_pal(palette="Tableau 20")(3)[c(3,1,2)]) +
   scale_fill_manual(values=tableau_color_pal(palette="Tableau 20")(3)[c(3,1,2)]) +
   theme_bw() + theme(legend.position=c(0.8, 0.2)) +
   guides(colour=guide_legend(title=element_blank()), fill=guide_legend(title=element_blank()))
ggsave(gg, filename=paste0(working.folder, "plots/DNA_coverage_1FA_log10.png"), width=6, height=5.5, dpi=300, family="Helvetica")
ggsave(gg, filename=paste0(working.folder, "plots/DNA_coverage_1FA_log10.pdf"), width=6, height=5.5, dpi=300, family="Helvetica")

gg = ggplot(tab[Condition%in%c("GRIDseq", "1FA") & Cell%in%"mESC" & N>=2,
                list(covDNA = mean(covDNA), sdCov = sd(covDNA)),
                by=c("Technique", "Cell", "Condition", "readsN")],
            aes(x=readsN, y=covDNA, fill=Technique)) +
   geom_line(aes(colour=Technique)) +
   geom_errorbar(aes(ymin=(covDNA-sdCov), ymax=(covDNA+sdCov), col=Technique), width=.2) +
   geom_point(pch=21, colour="black") +
   scale_x_continuous("Number of reads") +
   scale_y_continuous("DNA tags genomic coverage (bp)", labels=scientific) +
   scale_colour_manual(values=tableau_color_pal(palette="Tableau 20")(3)[c(3,1,2)]) +
   scale_fill_manual(values=tableau_color_pal(palette="Tableau 20")(3)[c(3,1,2)]) +
   theme_bw() + theme(legend.position=c(0.8, 0.2)) +
   guides(colour=guide_legend(title=element_blank()), fill=guide_legend(title=element_blank()))
ggsave(gg, filename=paste0(working.folder, "plots/DNA_coverage_1FA.png"), width=6, height=5.5, dpi=300, family="Helvetica")
ggsave(gg, filename=paste0(working.folder, "plots/DNA_coverage_1FA.pdf"), width=6, height=5.5, dpi=300, family="Helvetica")
```

```{r subsampledMappedCoverageRNA, eval=FALSE}
if( !dir.exists(file.path(working.folder, "step_coverage")) )
   dir.create(file.path(working.folder, "step_coverage"))

sample.names = setNames(unique(sampleTable[, Label]), unique(sampleTable[, Label]))

# Iterate through the groups
tab = rbindlist(lapply(sample.names,
                       function(lbl){
                          
                          print(lbl)
                          
                          if( !file.exists(file.path(working.folder, "step_coverage", paste0("RNA_step_coverage_", lbl, ".tsv"))) ){
                             # Iterate through the individual files for the group
                             interactions = lapply(sampleTable[Label%in%lbl, Path],
                                                   function(pth)
                                                      read.paired.bed.2(pth))
                             interactions = rbindlist(interactions)
                             interactions = interactions[chr.R%in%paste0("chr", c(1:19, "X", "Y")) & chr.D%in%paste0("chr", c(1:19, "X", "Y"))]
                             tmp.gr = with(interactions, GRanges(chr.R, IRanges(start.R, stop.R)))
                             interactions = interactions[!overlapsAny(tmp.gr, blacklist.gr),]
                             tmp.gr = with(interactions, GRanges(chr.D, IRanges(start.D, stop.D)))
                             interactions = interactions[!overlapsAny(tmp.gr, blacklist.gr),]
                             rm(tmp.gr)
                             dt = data.table(readsN=numeric(), covRNA=numeric())
                             for( i in seq(1e6, nrow(interactions), 1e6) ){
                                subset = interactions[sample(1:nrow(interactions), i),]
                                dt = rbind(dt, data.table(readsN=subset[, .N],
                                                          # uniRNA=subset[grepl("ENSMUS", gene_id.R), length(unique(gene_id.R))],
                                                          # uniInt=subset[, .N, by=c("gene_id.R", "gene_id.D")][, .N],
                                                          covRNA=with(subset, sum(sum(coverage(reduce(GRanges(chr.R, IRanges(start.R, stop.R)))))))))
                                rm(subset); gc()
                             }
                             dt = rbind(dt,
                                        data.table(readsN=interactions[, .N],
                                                   # uniRNA=interactions[grepl("ENSMUS", gene_id.R), length(unique(gene_id.R))],
                                                   # uniInt=interactions[, .N, by=c("gene_id.R", "gene_id.D")][, .N],
                                                   covRNA=with(interactions, sum(sum(coverage(reduce(GRanges(chr.R, IRanges(start.R, stop.R)))))))))
                             rm(interactions); gc()
                             fwrite(dt, file=file.path(working.folder, "step_coverage", paste0("RNA_step_coverage_", lbl, ".tsv")), append=FALSE, quote=FALSE, sep="\t")
                             
                          }else{
                           dt = fread(file.path(working.folder, "step_coverage", paste0("RNA_step_coverage_", lbl, ".tsv")))  
                           }
                          return(dt)
                       }), idcol="Set")

fwrite(tab, file=file.path(working.folder, "RNA_step_coverage_all_datasets.tsv"), append=FALSE, quote=FALSE, sep="\t")

# tab = rbindlist(list(with(tab, expand.grid(Set=unique(Set), readsN=0, covDNA=1)), tab))
tab = fread(file.path(working.folder, "RNA_step_coverage_all_datasets.tsv"))

tab[, c("Technique", "Cell", "Condition", "Replicate") := tstrsplit(gsub("4h_ActD", "4h-ActD", Set), "_")]
tab[, N := .N, by=c("Technique", "Cell", "Condition", "readsN")]

gg = ggplot(tab[Condition%in%c("GRIDseq", "1FA") & Cell%in%"mESC" & N>=2,
                list(covRNA = mean(covRNA), sdCov = sd(covRNA)),
                by=c("Technique", "Cell", "Condition", "readsN")],
            aes(x=readsN, y=covRNA, fill=Technique)) +
   geom_line(aes(colour=Technique)) +
   geom_errorbar(aes(ymin=(covRNA-sdCov), ymax=(covRNA+sdCov), col=Technique), width=.2) +
   geom_point(pch=21, colour="black") +
   scale_x_continuous("Number of reads") +
   scale_y_log10("RNA tags genomic coverage (bp)", labels=scientific) +
   # scale_y_continuous("RNA tags genomic coverage (bp)", labels=scientific) +
   scale_colour_manual(values=tableau_color_pal(palette="Tableau 20")(3)[c(3,1,2)]) +
   scale_fill_manual(values=tableau_color_pal(palette="Tableau 20")(3)[c(3,1,2)]) +
   theme_bw() + theme(legend.position=c(0.8, 0.2)) +
   guides(colour=guide_legend(title=element_blank()), fill=guide_legend(title=element_blank()))
ggsave(gg, filename=paste0(working.folder, "plots/RNA_coverage_1FA_log10.png"), width=6, height=5.5, dpi=300, family="Helvetica")
ggsave(gg, filename=paste0(working.folder, "plots/RNA_coverage_1FA_log10.pdf"), width=6, height=5.5, dpi=300, family="Helvetica")
```

```{r cutsiteAluI, eval=FALSE}
require("Biostrings")
dnaseq = DNAString("AGCT")

AluI.gr = vmatchPattern(dnaseq, Mmusculus, exclude = c("M", "_"))

dt = data.table(as.data.frame(distanceToNearest(AluI.gr)))

dt = dt[, .N, by="distance"][order(distance)]

fwrite(dt, file=paste(working.folder, "Distance_between_AluI_cutsites.tsv"), quote=FALSE, row.names=FALSE, sep="\t")

tab = setNames(unique(sampleTable[, Label]), unique(sampleTable[, Label]))

# Iterate through the groups
tab = rbindlist(lapply(tab[grepl("GRID", tab)|(grepl("1FA", tab))],
                       function(lbl){
                          
                          print(lbl)
                          # Iterate through the individual files for the group
                          bed = lapply(sampleTable[Label%in%lbl, Path],
                                       function(pth)
                                          read.paired.bed.2(pth))
                          bed = rbindlist(bed)
                          
                          bed = with(bed[chr.D%in%paste0("chr", c(1:19, "X", "Y")),], GRanges(chr.D, IRanges(start.D, stop.D)))
                          
                          bed = bed[!overlapsAny(bed, blacklist.gr),]
                          
                          dt = data.table(as.data.frame(distanceToNearest(bed, AluI.gr)))
                          
                          dt = dt[, .N, by="distance"][order(distance)]
                          
                          return(dt)
                          
                       }), idcol="Sample")

fwrite(tab, file=paste(working.folder, "DNA_reads_distance_to_AluI_cutsites.tsv"), quote=FALSE, row.names=FALSE, sep="\t")
```

```{r plotCutsiteAluI, eval=FALSE}
Alu = fread(paste(working.folder, "Distance_between_AluI_cutsites.tsv"))
Alu[, C := cumsum(N)/sum(N)]

tab = fread(paste(working.folder, "DNA_reads_distance_to_AluI_cutsites.tsv"))
tab[, `:=`(P = N/sum(N), C = cumsum(N)/sum(N)), by="Sample"]

tab[, c("Technique", "Cell", "Condition", "Replicate") := tstrsplit(Sample, "_")]
tab = tab[, list(C = mean(C)), by=c("Technique", "Cell", "distance")]

gg1 = ggplot(tab, aes(x=distance, y=C, col=paste(Technique, Cell))) +
   ggtitle("DNA tag distance to nearest AluI restriction site") +
   geom_line() +
   scale_x_continuous("Distance to nearest AluI restriction site (bp)") +
   scale_y_continuous("Percentage of DNA reads", breaks=pretty, labels=percent, limits=c(0,1)) +
   scale_colour_tableau("", palette = "Tableau 10") +
   facet_zoom(x=distance<1e3) +
   theme_light() + theme(legend.position="bottom")

gg2 = ggplot(Alu, aes(x=distance+1, y = ..density.., weight = N)) +
   ggtitle("Distance between AluI restriction sites") +
   geom_histogram(bins=25, col="black", fill="grey") +
   scale_x_log10("Distance to nearest AluI restriction sites (bp)") + scale_y_continuous("Density") + theme_bw()

tab2 = fread(paste(working.folder, "DNA_reads_genome_coverage_subsample_5e6.tsv"))
tab2 = tab2[grepl("GRID", Sample)|(grepl("1FA", Sample))]
tab2[, c("Technique", "Cell", "Condition", "Replicate") := tstrsplit(Sample, "_")]
tab2 = tab2[, list(mean(nrow_post/nrow_pre)), by=c("Technique", "Cell")]

gg3 = ggplot(tab2, aes(x=paste(Technique, Cell, sep="\n"), y=V1, fill=paste(Technique, Cell))) +
   ggtitle("Unique DNA positions recovered from a sub-sample of 5M tags") +
   geom_bar(stat="identity", position="identity", colour="black") +
   scale_y_continuous("Percentage of the sub-sample", labels=percent, limits=c(0,1)) +
   scale_x_discrete("") +
   scale_fill_tableau("", palette = "Tableau 10") +
   theme_light() + guides(fill=FALSE)

gg = ggarrange(gg1, ggarrange(gg2, gg3, nrow=2), ncol=2)
ggsave(gg, filename=paste0(working.folder, "plots/AluI_bias_and_DNA_tags.png"), width=12, height=8, dpi=300, family="Helvetica")
ggsave(gg, filename=paste0(working.folder, "plots/AluI_bias_and_DNA_tags.pdf"), width=12, height=8, dpi=300, family="Helvetica")
```

```{r less1Kb_analysis, eval=FALSE}
# Iterate through the groups
tab = rbindlist(lapply(setNames(unique(sampleTable[, Label]), unique(sampleTable[, Label]))[c(1:5)],
                       function(lbl){
                          
                          print(lbl)
                          # Iterate through the individual files for the group
                          bed = lapply(sampleTable[Label%in%lbl, Path],
                                       function(pth)
                                          read.paired.bed.2(pth))
                          bed = rbindlist(bed)
                          
                          bed[, `:=`(Total = .N, Intra = nrow(.SD[chr.R==chr.D]))]
                          bed = bed[chr.R==chr.D]
                          
                          bed[, list(perfectMatch = nrow(.SD[start.R==start.D & stop.R==stop.D]),
                                     sameStart    = nrow(.SD[start.R==start.D & !stop.R==stop.D]),
                                     sameEnd      = nrow(.SD[!start.R==start.D & stop.R==stop.D]),
                                     anyOverlaps  = nrow(.SD[(start.R<start.D & stop.R>start.D) | (start.R<stop.D & stop.R>stop.D) | (start.D<start.R & stop.D>stop.R)]),
                                     less1kb      = nrow(.SD[(((start.D-stop.R) <= 1e3) & ((start.D-stop.R) > 0)) | (((start.R-stop.D) < 1e3) & ((start.R-stop.D) > 0))]),
                                     Intra=unique(Intra), Total=unique(Total))]
                       }), idcol="Sample")
fwrite(tab, file=paste(working.folder, "RNA-DNA_reads_less1Kb_stats.tsv"), quote=FALSE, row.names=FALSE, sep="\t")
```

```{r runDNAcoverage, eval=FALSE}
# Iterate through the groups
tab = rbindlist(lapply(setNames(unique(sampleTable[, Label]), unique(sampleTable[, Label]))[c(1:5)],
                       function(lbl){
                          
                          print(lbl)
                          # Iterate through the individual files for the group
                          bed = lapply(sampleTable[Label%in%lbl, Path],
                                       function(pth)
                                          read.paired.bed.2(pth))
                          bed = rbindlist(bed)
                          
                          bed = with(bed[sample(.N, 1e7)], GRanges(chr.D, IRanges(start.D, stop.D)))
                          
                          data.table(nrow_pre = length(bed), nrow_post = length(reduce(bed)),
                                     expected=sum(width(bed)), coverage=sum(width(reduce(bed))))
                       }), idcol="Sample")

geno = seqinfo(BSgenome.Mmusculus.UCSC.mm10)
geno.size = sum(seqlengths(geno)[seqnames(geno)%in%paste0("chr", c(1:19, "X", "Y"))])

tab[, `:=`(General=NULL, post.pre=nrow_post/nrow_pre*100, obs.exp=coverage/expected*100, cov.geno=coverage/geno.size*100)]

fwrite(tab, file=paste(working.folder, "DNA_reads_genome_coverage_subsample_1e7.tsv"), quote=FALSE, row.names=FALSE, sep="\t")

# Iterate through the groups
tab = rbindlist(lapply(setNames(unique(sampleTable[, Label]), unique(sampleTable[, Label])),
                       function(lbl){
                          
                          print(lbl)
                          # Iterate through the individual files for the group
                          bed = lapply(sampleTable[Label%in%lbl, Path],
                                       function(pth)
                                          read.paired.bed.2(pth))
                          bed = rbindlist(bed)
                          
                          bed = with(bed[sample(.N, 5e6)], GRanges(chr.D, IRanges(start.D, stop.D)))
                          
                          data.table(nrow_pre = length(bed), nrow_post = length(reduce(bed)), coverage=sum(width(reduce(bed))))
                       }), idcol="Sample")

geno = seqinfo(BSgenome.Mmusculus.UCSC.mm10)
geno.size = sum(seqlengths(geno)[seqnames(geno)%in%paste0("chr", c(1:19, "X", "Y"))])

tab[, `:=`(General=NULL, post.pre=nrow_post/nrow_pre*100, cov.geno=coverage/geno.size*100)]

fwrite(tab, file=paste(working.folder, "DNA_reads_genome_coverage_subsample_5e6.tsv"), quote=FALSE, row.names=FALSE, sep="\t")
```

```{r plotDNAcoverage}
tab = fread(paste(working.folder, "DNA_reads_genome_coverage_subsample_1e7.tsv"))
tab[, c("Technique", "Cell", "Condition", "Replicate") := tstrsplit(gsub("4h_ActD", "4h-ActD", Sample), "_")]
tab[, Sample := gsub("_[123]$", "", Sample)]
tab1 = rbindlist(list(tab[, list(Sample, Replicate, cov.geno)], with(tab, expand.grid(Sample=unique(Sample), Replicate=1:3, cov.geno=NA))))
tab2 = rbindlist(list(tab[, list(Sample, Replicate, obs.exp)], with(tab, expand.grid(Sample=unique(Sample), Replicate=1:3, obs.exp=NA))))

gg1 = ggplot(tab1, aes(x=Sample, y=cov.geno/100, fill=Replicate)) +
   geom_bar(stat="identity", position="dodge", colour="black") +
   scale_fill_manual(values=rev(c("#74a9cf", "#2b8cbe", "#045a8d"))) +
   scale_x_discrete("") +
   scale_y_continuous("DNA tags genomic Coverage", labels=percent) +
   theme_light() + theme(axis.text.x=element_text(angle=45, hjust=1))
gg2 = ggplot(tab2, aes(x=Sample, y=obs.exp/100, fill=Replicate)) +
   geom_bar(stat="identity", position="dodge", colour="black") +
   scale_fill_manual(values=rev(c("#74a9cf", "#2b8cbe", "#045a8d"))) +
   scale_x_discrete("") +
   scale_y_continuous("DNA tags coverage (Observed/Expected)", labels=percent) +
   theme_light() + theme(axis.text.x=element_text(angle=45, hjust=1))
gg = ggarrange(gg1, gg2, ncol=2, common.legend=TRUE, legend="bottom")
ggsave(gg, filename=paste0(working.folder, "plots/Genomic_coverage_DNA_tags_subsampled.png"), width=12, height=6, dpi=300, family="Helvetica")
ggsave(gg, filename=paste0(working.folder, "plots/Genomic_coverage_DNA_tags_subsampled.pdf"), width=12, height=6, family="Helvetica")

tab = fread(paste(working.folder, "DNA_reads_genome_coverage_subsample_5e6.tsv"))
tab[, c("Technique", "Cell", "Condition", "Replicate") := tstrsplit(gsub("4h_ActD", "4h-ActD", Sample), "_")]
tab[, Sample := gsub("_[123]$", "", Sample)]
tab = rbindlist(list(tab[, list(Sample, Replicate, cov.geno)], with(tab, expand.grid(Sample=unique(Sample), Replicate=1:3, cov.geno=NA))))

gg = ggplot(tab, aes(x=Sample, y=cov.geno/100, fill=Replicate)) +
   geom_bar(stat="identity", position="dodge", colour="black") +
   scale_fill_manual(values=rev(c("#74a9cf", "#2b8cbe", "#045a8d"))) +
   scale_x_discrete("") +
   scale_y_continuous("DNA tags genomic Coverage", labels=percent) +
   theme_light() + theme(axis.text.x=element_text(angle=45, hjust=1))
ggsave(gg, filename=paste0(working.folder, "plots/Genomic_coverage_DNA_tags_subsampled.png"), width=8, height=6, dpi=300, family="Helvetica")
ggsave(gg, filename=paste0(working.folder, "plots/Genomic_coverage_DNA_tags_subsampled.pdf"), width=8, height=6, family="Helvetica")
```

```{r distanceToAluI, eval=FALSE}
dnaseq <- DNAString("AGCT")
AluI = vmatchPattern(dnaseq, BSgenome.Mmusculus.UCSC.mm10)

grp = "GRID_mESC_GRIDseq"
print(grp)


read.paired.bed.2 <- function(file, min.mapq=reads.quality.min, min.length=reads.length.min){
   column.names = paste(c("chr", "start", "stop", "score", "strand"), rep(c("R", "D"), each=5), sep=".")
   column.types = rep(c("character", "numeric", "numeric", "character", "numeric", "character"), 2)
   if( grepl("*.gz", file) ){
      bed = fread(cmd=paste("gunzip -c", file), select=c(1:3, 5:9, 11:12),
                  col.names=column.names,
                  colClasses=c(column.types, rep("character", 3)), showProgress=TRUE)
   }else{
      bed = fread(file, select=c(1:3, 5:9, 11:12),
                  col.names=column.names,
                  colClasses=c(column.types, rep("character", 3)), showProgress=TRUE)
   }
   
   bed = bed[score.R>=min.mapq & score.D>=min.mapq]
   bed = bed[(stop.R-start.R+1)<min.length & (stop.D-start.D+1)<min.length]
   return(bed)   
}


files.gseq = list.files(paste0(working.folder, "processed"), pattern=paste0(grp, ".*.gz"), full.names=TRUE)
files.gseq = files.gseq[grepl(bin_width, files.gseq) & !grepl("GenicOnly", files.gseq) & !grepl("significant", files.gseq)]
gseq = lapply(seq_along(files.gseq),
                        function(x){
                           tmp = fread(cmd=paste("gunzip -c",
                                                 files.gseq[[as.numeric(as.character(x))]]),
                                       showProgress=FALSE)
                           tmp[, replicate := as.numeric(as.character(x))]
                           tmp = tmp[chr.D%in%paste0("chr", c(1:19, "X", "Y")),]
                           tmp.gr = with(tmp, GRanges(chr.D, IRanges(start.D, end.D)))
                           tmp.gr = tmp.gr[!overlapsAny(tmp.gr, blacklist.gr),]
                           tmp.gr
                        })
gseq.dist = lapply(gseq, function(x) as.data.table(distanceToNearest(x, AluI)))

saveRDS(gseq.dist, file=paste0(working.folder, "RData/GRIDseq_distance_to_nearest_AluI.rds"))

gseq.bin = lapply(gseq.dist,
                  function(x){
                     x[, bin := findInterval(distance, c(-Inf, 0, 1, 5, 10, 20, 100, Inf), all.inside=FALSE, left.open=TRUE)-1]
                     x[, .N, by="bin"]
                  })
names(gseq.bin) = paste("Replicate", 1:2, sep="_")

gseq.bin = rbindlist(gseq.bin, idcol="Replicate")
gseq.bin[, P := N/sum(N), by="Replicate"]

ggplot(gseq.bin, aes(x=as.factor(bin), y=P, fill=Replicate)) +
   geom_bar(position="dodge", stat="identity", colour="black") +
   scale_x_discrete("Distance to nearest AluI site", breaks=0:6, labels=c("0", "1", "1-5", "5-10", "10-20", "20-100", "100+")) +
   scale_y_continuous("Dataset fraction", breaks=seq(0,1,0.1), labels=percent) +
   scale_fill_brewer() + theme_light() + theme(legend.position=c(0.8, 0.8))
```

```{r sameLocusInteractions, eval=FALSE}
sample.names = unique(sampleTable[, paste(Experiment, Condition, sep="_")])
sample.names = setNames(sample.names, sample.names)
stats = rbindlist(lapply(sample.names,
                         function(grp){
                            
                            print(grp)
                            
                            files = list.files(paste0(working.folder, "processed"), pattern=paste0(grp, ".*.gz"), full.names=TRUE)
                            files = files[grepl(bin_width, files) & grepl("GenicOnly", files) & !grepl("significant", files)]
                            interactions = rbindlist(lapply(seq_along(files),
                                                            function(x){
                                                               tmp = fread(cmd=paste("gunzip -c", files[[as.numeric(as.character(x))]]), showProgress=FALSE)
                                                               tmp[, replicate := as.numeric(as.character(x))]
                                                               tmp = tmp[chr.D%in%paste0("chr", c(1:19, "X", "Y")),]
                                                               tmp.gr = with(tmp, GRanges(chr.D, IRanges(start.D, end.D)))
                                                               tmp = tmp[!overlapsAny(tmp.gr, blacklist.gr),]
                                                               tmp.gr = tmp.gr[!overlapsAny(tmp.gr, blacklist.gr),]
                                                               overlaps = as.data.table(findOverlaps(tmp.gr, tx.regions))
                                                               overlaps = overlaps[!duplicated(queryHits),]
                                                               sel1 = overlaps[, queryHits]
                                                               sel2 = overlaps[, subjectHits]
                                                               tmp = tmp[sel1, gene_id.D := tx.regions[sel2,]$gene_id]
                                                               tmp[, list(N = .N), by=c("annotation.R", "gene_id.R", "gene_id.D")]
                                                            }))
                            interactions
                         }), idcol="Set")

sameStats = stats[, list(sameLoc = .SD[gene_id.R==gene_id.D, sum(N)], diffLoc = .SD[gene_id.R!=gene_id.D, sum(N)]), by=c("Set")]

sameStats[, `:=`(samePerc = sameLoc/(sameLoc+diffLoc), diffPerc = diffLoc/(sameLoc+diffLoc))]

fwrite(sameStats, file=paste0(working.folder, "Interactions_same_locus_stats.tsv"), append=FALSE, quote=FALSE, sep="\t")
```

```{r normalisedExpression, eval=FALSE}
sample.names = c("GRID_mESC_GRIDseq", "RADICL_mESC_1FA", "RADICL_mOPC_1FA")

stats = rbindlist(lapply(setNames(sample.names, sample.names),
                         function(grp){
                            
                            print(grp)
                            
                            files = list.files(paste0(working.folder, "processed"), pattern=paste0(grp, ".*.gz"), full.names=TRUE)
                            files = files[grepl(bin_width, files) & !grepl("GenicOnly", files) & !grepl("significant", files)]
                            interactions = rbindlist(lapply(seq_along(files),
                                                            function(x){
                                                               tmp = fread(cmd=paste("gunzip -c", files[[as.numeric(as.character(x))]]), showProgress=FALSE)
                                                               tmp[, replicate := as.numeric(as.character(x))]
                                                               tmp = tmp[chr.D%in%paste0("chr", c(1:19, "X", "Y")),]
                                                               tmp.gr = with(tmp, GRanges(chr.D, IRanges(start.D, end.D)))
                                                               tmp = tmp[!overlapsAny(tmp.gr, blacklist.gr),]
                                                               tmp
                                                            }))
                            interactions[, .N, by="gene_id.R"]
                         }), idcol="Set")
tab = dcast.data.table(stats[, list(gene_id.R, N=N/sum(N)*1e6), by="Set"][grep("ENSMUS", gene_id.R),], gene_id.R~Set, value.var="N", fill=0)
fwrite(tab, file=paste0(working.folder, "RNA_genes_normalised_counts.tsv"), append=FALSE, quote=FALSE, sep="\t")
```

```{r mOPCunique, eval=FALSE}
sample.names = c("RADICL_mOPC_1FA", "RADICL_mOPC_Control")

stats = rbindlist(lapply(setNames(sample.names, sample.names),
                         function(grp){
                            
                            print(grp)
                            
                            files = list.files(paste0(working.folder, "processed"), pattern=paste0(grp, ".*.gz"), full.names=TRUE)
                            files = files[grepl(bin_width, files) & !grepl("GenicOnly", files) & !grepl("significant", files)]
                            interactions = rbindlist(lapply(seq_along(files),
                                                            function(x){
                                                               tmp = fread(cmd=paste("gunzip -c", files[[as.numeric(as.character(x))]]), showProgress=FALSE)
                                                               tmp[, replicate := as.numeric(as.character(x))]
                                                               tmp = tmp[chr.D%in%paste0("chr", c(1:19, "X", "Y")),]
                                                               tmp.gr = with(tmp, GRanges(chr.D, IRanges(start.D, end.D)))
                                                               tmp = tmp[!overlapsAny(tmp.gr, blacklist.gr),]
                                                               tmp
                                                            }))
                            interactions[, .N, by="replicate"]
                         }), idcol="Set")

fwrite(stats, file=paste0(working.folder, "mOPC_uniquely_mapped_counts.tsv"), append=FALSE, quote=FALSE, sep="\t")
```

```{r TagsAnnotation, eval=FALSE}
sample.names = unique(paste(sampleTable[, Experiment], sampleTable[, Condition], sep="_"))
stats = rbindlist(lapply(setNames(sample.names, sample.names),
       function(grp){
   
   print(grp)
   
   files = list.files(paste0(working.folder, "processed"), pattern=paste0(grp, ".*.gz"), full.names=TRUE)
   files = files[grepl(bin_width, files) & !grepl("GenicOnly", files) & !grepl("significant", files)]
   interactions = rbindlist(lapply(seq_along(files),
                                   function(x){
                                      tmp = fread(paste("gunzip -c", files[[as.numeric(as.character(x))]]), showProgress=FALSE)
                                      tmp[, replicate := as.numeric(as.character(x))]
                                      tmp
                                   }))
   interactions[, index := seq_along(chr.D)]
   setkey(interactions, index)
   
   overlaps = as.data.table(findOverlaps(with(interactions, GRanges(chr.D, IRanges(start.D, end.D))), tx.regions))
   overlaps[, N := .N, by="queryHits"]
   setkey(overlaps, subjectHits)
   overlaps[tx.regions.dt, `:=`(gene_type.D = gene_type, annotation.D = annotation, region.D = type)]
   overlaps[N>1, `:=`(gene_type.D = "Multiple overlaps", annotation.D = "Multiple overlaps", region.D = "Multiple overlaps")]
   setkey(overlaps, queryHits)
   overlaps = overlaps[!duplicated(queryHits),]
   
   interactions[overlaps, `:=`(gene_type.D = gene_type.D, annotation.D = annotation.D, region.D = region.D)]
   tab = list(DNA=interactions[, .N, by=c("annotation.D", "gene_type.D", "region.D")],
              RNA=interactions[, .N, by=c("annotation.R", "gene_type.R", "region.R")])
   setnames(tab[[1]], c("Annotation", "Type", "Region", "N"))      
   setnames(tab[[2]], c("Annotation", "Type", "Region", "N"))      
   tab = rbindlist(tab, idcol="Tag")
                   
   tab[is.na(Annotation), `:=`(Annotation = "Intergenic", Type = "Intergenic", Region = "Intergenic")]
   
   tab[order(Tag, Annotation, Type, Region)]
    
}), idcol="Sample")

saveRDS(stats, file=paste0(working.folder, "RData/RNA-BIN_Tags_annotation.rds"))
```

```{r plotTagsAnnotation}
tab = readRDS(paste0(working.folder, "RData/RNA-BIN_Tags_annotation.rds"))

tab[Annotation%in%"Multiple overlaps", Annotation := "Multigenic"][Annotation%in%"other", Annotation := "other annotated"]
tab[Annotation%in%"long_ncRNA", Annotation := paste(Annotation, Region, sep="_")]
tab[Region%in%"CDS", Region := "exon"][Region%in%"five_prime_UTR", Region := "UTR5"][Region%in%"three_prime_UTR", Region := "UTR3"]
tab[Annotation%in%"protein_coding", Annotation := paste(Annotation, Region, sep="_")]

tab[, General := "Genic"]
tab[Annotation%in%"Intergenic", General := "Intergenic"]

tab = list(tab[, list(N=sum(N)), by=c("Sample", "Tag", "General")], tab[, list(N=sum(N)), by=c("Sample", "Tag", "Annotation")])
setnames(tab[[1]], names(tab[[2]]))
tab = rbindlist(tab, idcol="Group")

tab = tab[, Annotation := factor(Annotation, levels=c("Genic", "protein_coding_UTR5", "protein_coding_exon", "protein_coding_intron", "protein_coding_UTR3", "long_ncRNA_exon", "long_ncRNA_intron", "ncRNA", "NAST", "other annotated", "Multigenic", "Intergenic"))]
tab = tab[, list(N=sum(N)), by=c("Sample", "Tag", "Group", "Annotation")]
tab = tab[, list(Annotation, Value=N/sum(N)), by=c("Sample", "Tag", "Group")][order(Annotation), list(Annotation, Value, Percentage=round(Value*100, 1), Position = cumsum(Value) - 0.5*Value), by=c("Tag", "Group", "Sample")]
tab[, Tag := factor(Tag, levels=c("RNA", "DNA"))]

gg = ggplot(tab, aes(x=as.factor(Group), y=Value, fill=Annotation)) +
   geom_bar(position="stack", stat="identity", colour="black", width=0.6) +
   scale_fill_tableau(palette="Classic Cyclic") +
   coord_polar(theta="y", direction=-1) +
   facet_wrap(Tag~Sample, ncol=7) +
   theme_minimal() +
   geom_text(aes(x = Group+0.5, y = 1-Position, label = paste0(Percentage, "%")), col="black", size = 2) +
   theme(axis.text = element_blank(), axis.ticks = element_blank(), panel.grid  = element_blank(),
         axis.text.y = element_blank(), axis.text.x = element_blank(), axis.title=element_blank(),
         panel.background = element_blank(), strip.background = element_blank(), strip.text.x = element_text(size = 14),
         strip.text.y = element_text(size = 14), legend.title=element_text(size = 14), legend.text=element_text(size = 14))
ggsave(gg, filename=paste0(working.folder, "plots/Figure2AB.png"), width=24, height=10, dpi=300, family="Helvetica")
ggsave(gg, filename=paste0(working.folder, "plots/Figure2AB.pdf"), width=24, height=10, family="Helvetica")
```

```{r DNAtagsDensity, eval=FALSE}
stats = lapply(unique(paste(sampleTable[, Experiment], sampleTable[, Condition], sep="_")),
       function(grp){
   
   print(grp)
   
   files = list.files(paste0(working.folder, "processed"), pattern=paste0(grp, ".*.gz"), full.names=TRUE)
   files = files[grepl(bin_width, files) & !grepl("GenicOnly", files) & !grepl("significant", files)]
   interactions = rbindlist(lapply(seq_along(files),
                                   function(x){
                                      tmp = fread(paste("gunzip -c", files[[as.numeric(as.character(x))]]), showProgress=FALSE)
                                      tmp[, replicate := as.numeric(as.character(x))]
                                      tmp
                                   }))
   
   # Calculate the count per interaction pair
   setkeyv(interactions, c("chr.D", "start.D", "end.D", "gene_id.D", "replicate"))
   interactions = interactions[, .N, by=key(interactions)]

   interactions = interactions[, .N, by=c("replicate", "gene_id.D")]
   interactions[, list(Sample=grp, Median=as.numeric(median(N)), Mean=as.numeric(mean(N))), by="replicate"]
})

stats = rbindlist(stats)[, c(2,1,3,4)]
fwrite(stats, file=paste0(working.folder, "RNA-BIN_DNA_tags_density.tsv"), append=FALSE, quote=FALSE, sep="\t")
```

```{r checkOverlaps, eval=FALSE}
# interactions = rbindlist(lapply(setNames(c("GRID_mESC_GRIDseq", "RADICL_mESC_1FA", "RADICL_mESC_4h_ActD"),
#                                          c("GRID_mESC_GRIDseq", "RADICL_mESC_1FA", "RADICL_mESC_4h_ActD")),
interactions = rbindlist(lapply(setNames("RADICL_mESC_1FA", "RADICL_mESC_1FA"),
                                function(grp){
                                   
                                   files = list.files(paste0(working.folder, "processed"), pattern=paste0(grp, ".*.gz"), full.names=TRUE)
                                   files = files[grepl(bin_width, files) & !grepl("GenicOnly", files) & !grepl("significant", files)]
                                   tmp = rbindlist(lapply(seq_along(files),
                                                          function(x){
                                                             tmp = fread(cmd=paste("gunzip -c", files[[as.numeric(as.character(x))]]))
                                                             tmp[, replicate := as.numeric(as.character(x))]
                                                             tmp
                                                          }))
                                   return(tmp)
                                }), idcol="Condition")

interactions = interactions[grepl("ENSMUSG", gene_id.R), list(Condition, chr.R, start.R, strand.R, chr.D, start.D, gene_id.R)]

sel.R = !overlapsAny(with(interactions, GRanges(chr.R, IRanges(start.R, width=1))), blacklist.gr)
sel.D = !overlapsAny(with(interactions, GRanges(chr.D, IRanges(start.D, width=1))), blacklist.gr)

interactions = interactions[sel.R & sel.D,]

ge = genes[grepl("ENSMUSG", genes$gene_id),]

cnt = data.table(count = countOverlaps(with(interactions, GRanges(chr.R, IRanges(start.R, width=1), strand.R)), ge))

cnt = cnt[, .N, by="count"]

cnt[, P := N/sum(N)]
```

```{r interactionDistanceBins, eval=FALSE}
# interactions = rbindlist(lapply(setNames(c("GRID_mESC_GRIDseq", "RADICL_mESC_1FA", "RADICL_mESC_4h_ActD"),
#                                          c("GRID_mESC_GRIDseq", "RADICL_mESC_1FA", "RADICL_mESC_4h_ActD")),
interactions = rbindlist(lapply(setNames(c("GRID_mESC_GRIDseq", "RADICL_mESC_1FA"),
                                         c("GRID_mESC_GRIDseq", "RADICL_mESC_1FA")),
                                function(grp){
                                   
                                   files = list.files(paste0(working.folder, "processed"), pattern=paste0(grp, ".*.gz"), full.names=TRUE)
                                   files = files[grepl(bin_width, files) & !grepl("GenicOnly", files) & !grepl("significant", files)]
                                   tmp = rbindlist(lapply(seq_along(files),
                                                          function(x){
                                                             tmp = fread(cmd=paste("gunzip -c", files[[as.numeric(as.character(x))]]))
                                                             tmp[, replicate := as.numeric(as.character(x))]
                                                             tmp
                                                          }))
                                   return(tmp)
                                }), idcol="Condition")

setkeyv(interactions, c("Condition", "chr.R", "start.R", "chr.D", "start.D", "gene_id.R"))
interactions = interactions[, .N, by=key(interactions)]
setnames(interactions, "N", "Count")

sel.R = !overlapsAny(with(interactions, GRanges(chr.R, IRanges(start.R, width=1))), blacklist.gr)
sel.D = !overlapsAny(with(interactions, GRanges(chr.D, IRanges(start.D, width=1))), blacklist.gr)

interactions = interactions[sel.R & sel.D,]

# interactions[, Condition := gsub("4h_ActD", "4h-ActD", Condition)]

subset = interactions[gene_id.R!="Intergenic", list(Total = log10(sum(Count)+1),
                                                    Count = log10(.SD[chr.R!=chr.D, sum(Count)]+1),
                                                    Number = log10(.SD[chr.R!=chr.D, .N]+1)), by=c("Condition", "gene_id.R")]
gg = ggpairs(subset, columns=3:5, aes(colour=Condition), columnLabels=c("RNA expression", "Inter- (counts)", "Inter- (pairs)"))
save_plot(gg + theme_light(), filename=paste0(working.folder, "plots/RNA-BIN_interchromosomal_expression_counts_pairs.png"), base_height=4, base_aspect_ratio=1.1, family="Helvetica", ncol=3, nrow=3)

prom = promoters(genes, upstream=1000, downstream=1000)
prom = prom[prom$gene_type%in%"protein_coding",]

subset = interactions[gene_id.R%in%prom$gene_id,]
subset = subset[overlapsAny(with(subset, GRanges(chr.D, IRanges(start.D, width=1), strand="*")), prom),]

overlaps = as.data.table(findOverlaps(with(subset, GRanges(chr.D, IRanges(start.D, width=1), strand="*")), prom))
subset[, gene_id.D := prom[overlaps[!duplicated(queryHits), subjectHits],]$gene_id]

subset = subset[gene_id.R==gene_id.D]
subset[, distance := abs(start.R-start.D)]
subset[, bin := findInterval(distance, c(seq(0, 5, 1)*1e3, Inf), all.inside=TRUE)]

subset = subset[, list(Count = sum(Count)), by=c("Condition", "gene_id.R", "bin")]
subset[, Percentage := Count/sum(Count), by=c("Condition", "gene_id.R")]

data = subset[, list(Percentage = mean(Percentage)), by=c("Condition", "bin")]
gg = ggplot(data, aes(x=as.numeric(bin), y=Percentage, fill=Condition)) +
   ggtitle("Protein-coding RNA interactions with their promoter region (+/- 1Kb from TSS)") +
   geom_bar(stat="identity", position="dodge", colour="black") +
   scale_x_continuous("RNA and DNA tags distance (binned)",
                      breaks=1:6, labels=c("< 1Kb", "1-2Kb", "2-3Kb", "3-4Kb", "4-5Kb", "5Kb-gene end")) +
   scale_y_continuous("Averaga fraction of RNA tags") +
   scale_fill_tableau(palette = "Tableau 10") +
   theme_light()

# subset = interactions[chr.R==chr.D & abs(start.R-start.D)<=1e3,]
# subset[, distance := abs(start.R-start.D)]
# setkeyv(subset, c("Condition", "distance"))
# subset = subset[, list(Count = sum(Count)), by=key(subset)]
# subset = rbindlist(list(with(subset, expand.grid(Condition = unique(Condition), distance=0, Count=0)), subset))
# subset[, binStep := findInterval(distance, seq(0, 1e3, length.out=1000), all.inside=TRUE)]
# subset = subset[, list(Count = sum(Count)), by=c("Condition", "binStep")]
# subset[, CS := cumsum(Count), by=c("Condition")][, P := CS/max(CS), by=c("Condition")]
# gg = ggplot(subset, aes(x=binStep, y=P, colour=Condition)) +
#    geom_line() +
#    scale_x_continuous("Distance between RNA and DNA tags") +
#    scale_y_continuous("CDF") +
#    scale_colour_tableau(palette="Tableau 10") +
#    theme_light() + theme(legend.position=c(0.8, 0.2))

interactions[chr.R!=chr.D, Label := "Inter"]
interactions[is.na(Label) & abs(start.R-start.D)<1e3, Label := "<1Kb"]
interactions[is.na(Label) & abs(start.R-start.D)<1e4, Label := "1-10Kb"]
interactions[is.na(Label) & abs(start.R-start.D)<1e5, Label := "10-100Kb"]
interactions[is.na(Label) & abs(start.R-start.D)<1e6, Label := "100Kb-1Mb"]
interactions[is.na(Label) & abs(start.R-start.D)>=1e6, Label := ">=1Mb"]

tab = interactions[, list(Count = sum(Count)), by=c("Condition", "Label")]
tab[, Label := factor(Label, levels=c("<1Kb", "1-10Kb", "10-100Kb", "100Kb-1Mb", ">=1Mb", "Inter"))]
tab[, Percentage := Count/sum(Count), by="Condition"]

gg = ggplot(tab, aes(x=Label, y=Percentage, fill=Condition)) +
   geom_bar(stat="identity", position="dodge", col="black") +
   scale_x_discrete("Distance between RNA and DNA tags") +
   scale_y_continuous("Dataset fraction", breaks=pretty, labels=percent) +
   scale_fill_tableau() +
   theme_light() + theme(legend.position=c(0.15, 0.9)) + guides(fill=guide_legend(title=element_blank()))
ggsave(gg, filename=paste0(working.folder, "plots/RNA-BIN_interactionDistance_condition.png"), width=10, height=6, dpi=300, family="Helvetica")
ggsave(gg, filename=paste0(working.folder, "plots/RNA-BIN_interactionDistance_condition.pdf"), width=10, height=6, family="Helvetica")
```

```{r circos_pre_post, eval=FALSE}
sample.names = unique(sampleTable[, paste(Experiment, Condition, sep="_")])
sample.names = setNames(sample.names, sample.names)[1:2]

gene_ids = c("ENSMUSG00000030849.18",
             "ENSMUSG00000064899.1",
             "ENSMUSG00000065232.1",
             "ENSMUSG00000088088.1",
             "ENSMUSG00000092341.2",
             "ENSMUSG00000004637.15",
             "ENSMUSG00000069769.13",
             "ENSMUSG00000038518.15",
             "ENSMUSG00000021745.13",
             "ENSMUSG00000097039.8",
             "ENSMUSG00000085396.7",
             "ENSMUSG00000087259.7",
             "ENSMUSG00000057215.6",
             "ENSMUSG00000058589.14",
             "ENSMUSG00000032087.10",
             "ENSMUSG00000051910.13",
             "ENSMUSG00000106379.1",
             "ENSMUSG00000021010.8",
             "ENSMUSG00000106943.1",
             "ENSMUSG00000022607.14",
             "ENSMUSG00000024073.14",
             "ENSMUSG00000060579.12",
             "ENSMUSG00000022961.17",
             "ENSMUSG00000053414.7",
             "ENSMUSG00000037110.19",
             "ENSMUSG00000035392.17")

interactions = lapply(sample.names,
                      function(grp){
                         
                         print(grp)
                         
                         files.int = list.files(paste0(working.folder, "processed"), pattern=paste0(grp, ".*.gz"), full.names=TRUE)
                         files.int = files.int[grepl(bin_width, files.int) & !grepl("GenicOnly", files.int) & !grepl("significant", files.int)]
                         inter = rbindlist(lapply(seq_along(files.int),
                                        function(x){
                                           tmp = fread(cmd=paste("gunzip -c",
                                                                 files.int[[as.numeric(as.character(x))]]),
                                                       showProgress=FALSE)
                                           tmp[, replicate := as.numeric(as.character(x))]
                                           tmp = tmp[gene_id.R%in%gene_ids]
                                           tmp = tmp[chr.D%in%paste0("chr", c(1:19, "X", "Y")),]
                                           tmp.gr = with(tmp, GRanges(chr.D, IRanges(start.D, end.D)))
                                           tmp = tmp[!overlapsAny(tmp.gr, blacklist.gr),]
                                           tmp
                                        }))
                      })
interactions = rbindlist(interactions, idcol="Condition")

gene = "ENSMUSG00000021010.8"

subset = interactions[gene_id.R==gene, list(Count = .N), by=c("Condition", "gene_id.R", "gene_id.D")]

# files.sig = list.files(paste0(working.folder, "processed"), pattern="*_cnd_*", full.names=TRUE)
# 
# significant = rbindlist(lapply(files.sig,
#                                function(x)
#                                   fread(cmd=paste("gunzip -c", x), showProgress=FALSE)))
# setnames(significant, "replicate", "Replicate")
# 
# subset = significant[Condition%in%"RADICL_mESC_1FA", list(gene_id.R, chr.R, start.R, start.R, gene_id.D, chr.D, start.D, end.D)]
# 
# subset = significant[!grepl("chrM", gene_id.D), list(Count=.N), by=c("Condition", "gene_id.R", "gene_id.D")]

rna_ids = copy(genes)
rna_ids = keepSeqlevels(rna_ids, paste0("chr", c(1:19, "X", "Y")), pruning.mode="coarse")
rna_ids = data.table(as.data.frame(rna_ids), key="gene_id")

dna_ids = copy(mm10)
dna_ids = keepSeqlevels(dna_ids, paste0("chr", c(1:19, "X", "Y")), pruning.mode="coarse")
dna_ids = data.table(as.data.frame(dna_ids), key="bin_id")

gene_name = genes.dt[gene_id%in%gene, gene_name]
gene_chr = as.character(genes.dt[gene_id%in%gene, seqnames])

message(gene_name)

breaks = seq(0, ceiling(log2(max(subset[, Count]))), 1)
col_fun = colorRamp2(breaks, viridis(length(breaks)))
lgd_links = Legend(at = seq(0, max(round(range(breaks))), 2),
                   col_fun = col_fun, type="grid", title_position = "topcenter", title = "log2(Count)", direction = "horizontal")
lgd_list_vertical = packLegend(lgd_links)

myset = subset[gene_id.R%in%gene,]

myset = split(myset, myset$Condition)

bed1 = lapply(myset,
              function(x)
                 cbind(rna_ids[match(x[, gene_id.R], rna_ids[, gene_id]),], rou1=0.8))
for( i in seq_along(bed1) )
   setkey(bed1[[i]], gene_type)

bed2 = lapply(myset,
              function(x)
                 cbind(dna_ids[match(x[, gene_id.D], dna_ids[, bin_id]),], rou2=0.9))

bed2 = lapply(seq_along(bed2),
              function(x)
                 bed2[[as.numeric(as.character(x))]][, `:=`(value = log2(myset[[as.numeric(as.character(x))]][, Count]),
                                                            colour = col_fun(log2(myset[[as.numeric(as.character(x))]][, Count])))])

cytoband = read.cytoband(species="mm10")
cytoband_df = cytoband$df
chromosome = cytoband$chromosome

xrange = c(cytoband$chr.len, cytoband$chr.len[gene_chr])
normal_chr_index = 1:length(chromosome)
zoomed_chr_index = length(chromosome)+1

# normalize in normal chromsomes and zoomed chromosomes separately
sector.width = c(xrange[normal_chr_index] / sum(xrange[normal_chr_index]),
                 xrange[zoomed_chr_index] / sum(xrange[zoomed_chr_index]))

extend_chromosomes = function(bed, chromosome, prefix = "zoom_") {
   if( chromosome %in% as.data.frame(bed)[, 1] ){
      zoom_bed = bed[bed[[1]] %in% chromosome, , drop = FALSE]
      zoom_bed[[1]] = paste0(prefix, zoom_bed[[1]])
      return(rbind(bed, zoom_bed))
   }else{
      return(bed)
   }
}   

pdf(paste0(working.folder, "plots/circos_BIN_", gene_name, "_Lines.pdf"), width=18, height=10)
# png(paste0(working.folder, "plots/circos_BIN_", gene_name, "_Lines.png"), units="in", width=18, height=10, res=72)
vec = if( length(bed1)%%2==0 ){ c(1:length(bed1)) }else{ c(1:length(bed1), 0) }
layout(matrix(vec, 2, length(vec)/2))
for( i in seq_along(bed1) ){
   circos.clear()
   circos.par(start.degree = 90)
   circos.initializeWithIdeogram(extend_chromosomes(cytoband_df, gene_chr), sector.width = sector.width)
   if( diff(range(bed2[[i]][, value]))!=0 ){
      circos.genomicTrack(extend_chromosomes(bed2[[i]][, list(seqnames, start, end, value)], gene_chr),
                          panel.fun = function(region, value, ...) {
                             circos.genomicLines(region, value, type = "h")
                          })         
      circos.link(gene_chr, get.cell.meta.data("cell.xlim", sector.index = gene_chr),
                  paste0("zoom_", gene_chr), get.cell.meta.data("cell.xlim", sector.index = paste0("zoom_", gene_chr)),
                  col = "#00000020", border = NA)
      bed2[[i]][seqnames%in%gene_chr, seqnames := paste0("zoom_", seqnames)]
      circos.genomicLink(bed1[[i]], bed2[[i]], col=bed2[[i]]$colour)
      # circos.genomicLabels(extend_chromosomes(bed1[[i]][1,], gene_chr), side = "inside", labels=gene_name, connection_height = convert_height(3, "mm"))
   }
   text(0, 1.2, names(bed1)[i], cex=2, font=2)
   title(names(bed1)[i])
   circos.clear()
}
pushViewport(viewport(x = unit(2, "mm"), y = unit(6, "mm"),
                      width = grobWidth(lgd_list_vertical)*2,
                      height = grobHeight(lgd_list_vertical),
                      just = c("left", "bottom")))
grid.draw(lgd_list_vertical)
dev.off()
```

```{r setFDR, echo=FALSE}
if(!exists("fdr.th")) fdr.th = 0.05
```

The FDR is set to `fdr.th`.

_WARNING: This chunk is labour-intensive. If the outputs have already been generated, make sure they are in the correct folder and have the correct name to avoid regenerating them all._

```{r significance}
if( !dir.exists(paste0(working.folder, "RData")) )
   dir.create(paste0(working.folder, "RData"), recursive=TRUE)

for( grp in unique(paste(sampleTable[, Experiment], sampleTable[, Condition], sep="_")) ){
   
   # Skip the iteration if the final file already exists
   if( file.exists(paste0(working.folder, "processed/RNA-BIN_", grp, "_", gencode_version, "_level_1_2_3_Unique_", bin_width, "_significant", fdr.th, "_extended_tsc_noDistance.tsv.gz")) )
      next
   
   print(grp)
   
   files = list.files(paste0(working.folder, "processed"), pattern=paste0(grp, ".*.gz"), full.names=TRUE)
   files = files[grepl(bin_width, files) & !grepl("GenicOnly", files) & !grepl("significant", files)]
   interactions = rbindlist(lapply(seq_along(files),
                                   function(x){
                                      tmp = fread(paste("gunzip -c", files[[as.numeric(as.character(x))]]))
                                      tmp[, replicate := as.numeric(as.character(x))]
                                      tmp
                                   }))
   
   # Calculate the count per interaction pair
   setkeyv(interactions, c("gene_id.R", "gene_id.D"))
   subset = interactions[, .N, by=key(interactions)]
   setnames(subset, "N", "Count")
   
   # subset = subset[!(gene_id.R%in%c("Multigenic", "Intergenic") | gene_id.D%in%c("Multigenic", "Intergenic"))]
   
   # # Add the correction for the distance (ignore the multi- and inter-genic, they will be removed downstream anyway)
   # pairs.unique = unique(subset[!(gene_id.R%in%c("Multigenic", "Intergenic") | gene_id.D%in%c("Multigenic", "Intergenic")), list(gene_id.R, gene_id.D)])
   # # pairs.unique = unique(subset[, list(gene_id.R, gene_id.D)])
   # pairs.unique[, distance := distance(genes[match(pairs.unique[, gene_id.R], genes$gene_id)],
   #                                     mm10[match(pairs.unique[, gene_id.D], mm10$bin_id)],
   #                                     ignore.strand=TRUE)]
   # # Calculate the distance distribution and the weight factors
   # h = hist(pairs.unique[distance>0 & distance<=1e6, distance], 100, plot=FALSE)
   # h = data.table(breaks=h$breaks[-1], count=log10(h$counts))
   # h[, index := seq_along(breaks)][, scaled := (count-min(count))/(2*(max(count)-min(count)))+0.5]
   # setkey(h, index)
   # 
   # # Set a large number (it will not be used) for the interactions with no distance info
   # pairs.unique[is.na(distance), distance := 7e6]
   # # Assign the weight factors
   # pairs.unique[, interval := findInterval(distance, c(0, h$breaks))]
   # setkey(pairs.unique, interval)
   # # Interactions farther than 1e6 bp (or interchromosomal) will have the lowest weight
   # pairs.unique[, distance := as.double(distance)][distance>1e6, distance := min(h[,scaled])]
   # pairs.unique = merge(pairs.unique, h, by.x="interval", by.y="index", all.x=TRUE)
   # pairs.unique[!is.na(scaled), distance := scaled]
   # setkeyv(pairs.unique, c("gene_id.R", "gene_id.D"))
   
   frandom=1
   
   # Calculate the frequency per RNA
   setkeyv(subset, "gene_id.R")
   rj = subset[, list(coverage=sum(Count)), by=key(subset)]
   rj[, relative := coverage/sum(coverage)]
   setkeyv(rj, "gene_id.R")
   subset[rj, rj := relative]
   
   # Calculate the frequency per DNA (or BIN)
   setkeyv(subset, "gene_id.D")
   rh = subset[, list(coverage=sum(Count)), by=key(subset)]
   rh[, relative := coverage/sum(coverage)]
   setkeyv(rh, "gene_id.D")
   subset[rh, rh := relative]
   
   # Calculate the total number of interactions (needed for the binomial test)
   subset[, N := sum(Count)]
   
   # Remove the Multi- and Inter-genic
   subset = subset[!(gene_id.R%in%c("Multigenic", "Intergenic") | gene_id.D%in%c("Multigenic", "Intergenic"))]
   
   # # Assign the distance weight to each interaction pair
   # setkeyv(subset, c("gene_id.R", "gene_id.D"))
   # subset[pairs.unique, Distance := distance]
   
   # # Free some memory
   # rm(pairs.unique)
   # gc()
   
   # Calculate the conditional (and weighted) probability
   # subset[, `:=`(pjh = rj*rh*frandom*Distance)][, `:=`(Distance = NULL, rj = NULL, rh = NULL)]
   # subset[, `:=`(pjh = rj*rh*frandom)][, `:=`(rj = NULL, rh = NULL)]
   # subset[, `:=`(pjh = rj*(.N/sum(Count)), N = sum(Count)), by="gene_id.R"]
   subset[, `:=`(pjh = 1*(1/.N), N = sum(Count)), by="gene_id.R"]

   # Perform the binomial test
   setkeyv(subset, c("gene_id.R", "gene_id.D"))
   subset[, `:=`(pval=binom.test(Count, N, p=pjh, alternative="greater")$p.value), by=key(subset)]
   
   # Perform the correction (Benjamini-Hochberg and Benjamini-Yekutieli) per transcript
   # Calculate the total number of interacting bins (whether with genic or intergenic regions)
   # unique.bins = length(mm10)
   unique.bins = length(subset[, unique(gene_id.D)])
   
   setkeyv(subset, "gene_id.R")
   subset[, fdr.tsc := p.adjust(pval, method="BH"), by=key(subset)]
   # # Perform the correction (Benjamini-Hochberg and Benjamini-Yekutieli) per condition
   # # Calculate the total number of interacting bins (whether with genic or intergenic regions)
   # total.inter = as.numeric(length(subset[, unique(gene_id.R)]))*as.numeric(length(subset[, unique(gene_id.D)]))
   # subset[, fdr.cnd := p.adjust(pval, method="BH", n=total.inter)]
   subset[, fdr.cnd := p.adjust(pval, method="BH")]

   # Calculate the observed vs expected ratio
   subset[, `:=`(Rjh = log2(Count/(pjh*N)))]
   
   # Remove some columns
   subset[, `:=`(pjh = NULL, N = NULL)]
   
   # save the entire object
   saveRDS(subset,
           file=paste0(working.folder, "RData/RNA-BIN_", grp, "_", gencode_version, "_level_1_2_3_Unique_", bin_width, "_significant_noDistance.rds"))
   
   setkeyv(subset, c("gene_id.R", "gene_id.D"))
   setkeyv(interactions, key(subset))

   # print(nrow(subset[fdr.tsc<fdr.th & Count==1,]), nrow(subset[fdr.cnd<fdr.th & Count==1,]))
   
   output = interactions[subset[fdr.tsc<fdr.th, list(Condition=grp, gene_id.R, gene_id.D, Pvalue=pval, FDR=fdr.tsc)], nomatch=0]
   
   writeGzip(output, file=paste0(working.folder, "processed/RNA-BIN_", grp, "_", gencode_version, "_level_1_2_3_Unique_", bin_width, "_significant", fdr.th, "_extended_tsc_noDistance.tsv"),
             col.names=TRUE, showProgress=TRUE, wait=FALSE, level=9)
   
   rm(output); gc()
   
   output = interactions[subset[fdr.cnd<fdr.th, list(Condition=grp, gene_id.R, gene_id.D, Pvalue=pval, FDR=fdr.cnd)], nomatch=0]

   # print(nrow(subset[fdr.tsc<fdr.th & Count==1,]))

   writeGzip(output, file=paste0(working.folder, "processed/RNA-BIN_", grp, "_", gencode_version, "_level_1_2_3_Unique_", bin_width, "_significant", fdr.th, "_extended_cnd_noDistance.tsv"),
             col.names=TRUE, showProgress=TRUE, wait=FALSE, level=9)

   rm(interactions, subset, output); gc()
   
}
# # As required by Michiael
# setnames(subset, "fdr.tsc", "FDR")
# setnames(subset, "pval", "Pvalue")
# subset[, `:=`(fdr.cnd=NULL, Rjh=NULL)]
# for( cond in unique(subset[, Condition]) )
#    writeGzip(subset[Condition%in%cond],
#              file=paste0("RNA-BIN_counts_", gencode_version, "_level_1_2_3_AllOverlapsUnique_", bin_width, "_significant_", cond, "_noDistance.tsv"),
#              col.names=TRUE, showProgress=TRUE, wait=TRUE, level=9)
```

```{r normalisedExpressionSign, eval=FALSE}
files.sig = list.files(paste0(working.folder, "processed"), pattern="*_cnd_*", full.names=TRUE)

significant = rbindlist(lapply(files.sig,
                               function(x)
                                  fread(cmd=paste("gunzip -c", x), showProgress=FALSE)))
setnames(significant, "replicate", "Replicate")

significant = significant[chr.D%in%paste0("chr", c(1:19, "X", "Y")),]
significant.gr = with(significant, GRanges(chr.D, IRanges(start.D, end.D)))
significant = significant[!overlapsAny(significant.gr, blacklist.gr),]

significant = significant[, .N, by=c("Condition", "gene_id.R")]
significant = significant[grep("ENSMUS", gene_id.R),]
significant = significant[Condition%in%c("GRID_mESC_GRIDseq", "RADICL_mESC_1FA", "RADICL_mOPC_1FA"),]

tab = dcast.data.table(significant[, list(gene_id.R, N=N/sum(N)*1e6), by="Condition"], gene_id.R~Condition, value.var="N", fill=0)
fwrite(tab, file=paste0(working.folder, "RNA_genes_normalised_significant_counts.tsv"), append=FALSE, quote=FALSE, sep="\t")

setkey(significant, gene_id.R)
setkey(genes.dt, gene_id)

significant[genes.dt, width := width]
tab = dcast.data.table(significant[, list(gene_id.R, N=N/sum(N)*1e6/width*1e3), by="Condition"], gene_id.R~Condition, value.var="N", fill=0)
fwrite(tab, file=paste0(working.folder, "RNA_genes_normalised_perKb_significant_counts.tsv"), append=FALSE, quote=FALSE, sep="\t")
```

```{r counts_and_bins, eval=FALSE}
files.sig = list.files(paste0(working.folder, "processed"), pattern="*_cnd_*", full.names=TRUE)

significant = rbindlist(lapply(files.sig,
                               function(x)
                                  fread(cmd=paste("gunzip -c", x), showProgress=FALSE)))
setnames(significant, "replicate", "Replicate")

counts = significant[region.R%in%"CDS", region.R := "exon"][region.R%in%c("exon", "intron"), list(Bins=length(unique(gene_id.D)), Counts=.N), by=c("Condition", "gene_id.R", "region.R")]

ggl = lapply(counts[, unique(Condition)],
             function(x)
                ggplot(counts[Condition%in%as.character(x),], aes(x=Counts, y=Bins, col=region.R)) +
                ggtitle(as.character(x)) +
                geom_point(size=0.5, alpha=0.5) +
                scale_y_log10("Number of bound bins") + scale_x_log10("Counts") +
                scale_colour_brewer("Gene feature", palette="Set1") +
                theme_light() + theme(legend.position=c(0.2, 0.8))
             )

png(file=paste0(working.folder, "RNA-BIN_Bins_vs_Counts.png"), units="in", width=16, height=16, res=72, family="Helvetica")
ggarrange(plotlist=ggl, ncol=3, nrow=3)
dev.off()
```

```{r plotTagsAnnotationSig, eval=FALSE}
files.sig = list.files(paste0(working.folder, "processed"), pattern="*_cnd_*", full.names=TRUE)

significant = rbindlist(lapply(files.sig,
                               function(x)
                                  fread(paste("gunzip -c", x), showProgress=FALSE)))
setnames(significant, "replicate", "Replicate")

tab = significant[, list(Count = .N), by=c("Condition", "annotation.R", "region.R")]
setnames(tab, "annotation.R", "Annotation")
setnames(tab, "region.R", "Region")

tab[Annotation%in%"other", Annotation := "other annotated"]
tab[Annotation%in%"long_ncRNA", Annotation := paste(Annotation, Region, sep="_")]
tab[Region%in%"CDS", Region := "exon"][Region%in%"five_prime_UTR", Region := "UTR5"][Region%in%"three_prime_UTR", Region := "UTR3"]
tab[Annotation%in%"protein_coding", Annotation := paste(Annotation, Region, sep="_")]

tab[, Annotation := factor(Annotation, levels=c("protein_coding_UTR5", "protein_coding_exon", "protein_coding_intron", "protein_coding_UTR3", "long_ncRNA_exon", "long_ncRNA_intron", "ncRNA", "NAST", "other annotated"))]
tab = tab[, list(Count=sum(Count)), by=c("Condition", "Annotation")]
tab = tab[, list(Annotation, Value=Count/sum(Count)), by="Condition"][order(Annotation),]
tab = tab[,list(Annotation, Value, Percentage=round(Value*100, 1), Position = cumsum(Value) - 0.5*Value), by="Condition"]

gg = ggplot(tab, aes(x=as.factor(1), y=Value, fill=Annotation)) +
   geom_bar(position="stack", stat="identity", colour="black", width=0.6) +
   scale_fill_tableau(palette="Classic Cyclic") +
   coord_polar(theta="y", direction=-1) +
   facet_wrap(~Condition, ncol=4) +
   theme_minimal() +
   geom_text(aes(x = 1+0.5, y = 1-Position, label = paste0(Percentage, "%")), col="black", size = 2) +
   theme(axis.text = element_blank(), axis.ticks = element_blank(), panel.grid  = element_blank(),
         axis.text.y = element_blank(), axis.text.x = element_blank(), axis.title=element_blank(),
         panel.background = element_blank(), strip.background = element_blank(), strip.text.x = element_text(size = 14),
         strip.text.y = element_text(size = 14), legend.title=element_text(size = 14), legend.text=element_text(size = 14))
ggsave(gg, filename=paste0(working.folder, "plots/FigureS2A.png"), width=16, height=8, dpi=300, family="Helvetica")
ggsave(gg, filename=paste0(working.folder, "plots/FigureS2A.pdf"), width=16, height=8, family="Helvetica")
```

```{r interactionDistanceBins, eval=FALSE}
files.sig = list.files(paste0(working.folder, "processed"), pattern="*_cnd_*", full.names=TRUE)

significant = rbindlist(lapply(files.sig,
                               function(x)
                                  fread(cmd=paste("gunzip -c", x), showProgress=FALSE)))
setnames(significant, "replicate", "Replicate")

setkeyv(significant, c("Condition", "chr.R", "start.R", "chr.D", "start.D", "gene_id.R"))
significant = significant[, .N, by=key(significant)]
setnames(significant, "N", "Count")

sel.R = !overlapsAny(with(significant, GRanges(chr.R, IRanges(start.R, width=1))), blacklist.gr)
sel.D = !overlapsAny(with(significant, GRanges(chr.D, IRanges(start.D, width=1))), blacklist.gr)

significant = significant[sel.R & sel.D,]

# significant[, Condition := gsub("4h_ActD", "4h-ActD", Condition)]

subset = significant[, list(Total = log10(sum(Count)+1),
                            Count = log10(.SD[chr.R!=chr.D, sum(Count)]+1),
                            Number = log10(.SD[chr.R!=chr.D, .N]+1)), by=c("Condition", "gene_id.R")]
gg = ggpairs(subset, columns=3:5, aes(colour=Condition), columnLabels=c("RNA expression", "Inter- (counts)", "Inter- (pairs)"))
save_plot(gg + theme_light(), filename=paste0(working.folder, "plots/RNA-BIN_interchromosomal_expression_counts_pairs_significant.png"), base_height=4, base_aspect_ratio=1.1, family="Helvetica", ncol=3, nrow=3)

prom = promoters(genes, upstream=1000, downstream=1000)
prom = prom[prom$gene_type%in%"protein_coding",]

subset = significant[gene_id.R%in%prom$gene_id,]
subset = subset[overlapsAny(with(subset, GRanges(chr.D, IRanges(start.D, width=1), strand="*")), prom),]

overlaps = as.data.table(findOverlaps(with(subset, GRanges(chr.D, IRanges(start.D, width=1), strand="*")), prom))
subset[, gene_id.D := prom[overlaps[!duplicated(queryHits), subjectHits],]$gene_id]

subset = subset[gene_id.R==gene_id.D]
subset[, distance := abs(start.R-start.D)]
subset[, bin := findInterval(distance, c(seq(0, 5, 1)*1e3, Inf), all.inside=TRUE)]

subset = subset[, list(Count = sum(Count)), by=c("Condition", "gene_id.R", "bin")]
subset[, Percentage := Count/sum(Count), by=c("Condition", "gene_id.R")]

data = subset[, list(Percentage = mean(Percentage)), by=c("Condition", "bin")]
gg = ggplot(data, aes(x=as.numeric(bin), y=Percentage, fill=Condition)) +
   ggtitle("Protein-coding RNA interactions with their promoter region (+/- 1Kb from TSS)") +
   geom_bar(stat="identity", position="dodge", colour="black") +
   scale_x_continuous("RNA and DNA tags distance (binned)",
                      breaks=1:6, labels=c("< 1Kb", "1-2Kb", "2-3Kb", "3-4Kb", "4-5Kb", "5Kb-gene end")) +
   scale_y_continuous("Averaga fraction of RNA tags") +
   scale_fill_tableau(palette = "Tableau 10") +
   theme_light()

significant[chr.R!=chr.D, Label := "Inter"]
significant[is.na(Label) & abs(start.R-start.D)<1e3, Label := "<1Kb"]
significant[is.na(Label) & abs(start.R-start.D)<1e4, Label := "1-10Kb"]
significant[is.na(Label) & abs(start.R-start.D)<1e5, Label := "10-100Kb"]
significant[is.na(Label) & abs(start.R-start.D)<1e6, Label := "100Kb-1Mb"]
significant[is.na(Label) & abs(start.R-start.D)>=1e6, Label := ">=1Mb"]

tab = significant[, list(Count = sum(Count)), by=c("Condition", "Label")]
tab[, Label := factor(Label, levels=c("<1Kb", "1-10Kb", "10-100Kb", "100Kb-1Mb", ">=1Mb", "Inter"))]
tab[, Percentage := Count/sum(Count), by="Condition"]

gg = ggplot(tab, aes(x=Label, y=Percentage, fill=Condition)) +
   geom_bar(stat="identity", position="dodge", col="black") +
   scale_x_discrete("Distance between RNA and DNA tags") +
   scale_y_continuous("Dataset fraction", breaks=pretty, labels=percent) +
   scale_fill_tableau() +
   theme_light() + theme(legend.position=c(0.8, 0.8)) + guides(fill=guide_legend(title=element_blank()))
ggsave(gg, filename=paste0(working.folder, "plots/RNA-BIN_interactionDistance_condition_significant.png"), width=10, height=6, dpi=300, family="Helvetica")
ggsave(gg, filename=paste0(working.folder, "plots/RNA-BIN_interactionDistance_condition_significant.pdf"), width=10, height=6, family="Helvetica")

gg = ggplot(tab[Condition%in%c("RADICL_mESC_1FA", "RADICL_mOPC_1FA", "RADICL_mESC_Control", "RADICL_mOPC_Control"),], aes(x=Label, y=Percentage, fill=Condition)) +
   geom_bar(stat="identity", position="dodge", col="black") +
   scale_x_discrete("Distance between RNA and DNA tags") +
   scale_y_continuous("Dataset fraction", breaks=pretty, labels=percent) +
   scale_fill_tableau(palette="Tableau 20") +
   theme_light() + theme(legend.position=c(0.8, 0.8)) + guides(fill=guide_legend(title=element_blank()))
ggsave(gg, filename=paste0(working.folder, "plots/RNA-BIN_interactionDistance_condition_significant_RADICL.png"), width=10, height=6, dpi=300, family="Helvetica")
ggsave(gg, filename=paste0(working.folder, "plots/RNA-BIN_interactionDistance_condition_significant_RADICL.pdf"), width=10, height=6, family="Helvetica")

subset = significant[Condition%in%c("RADICL_mESC_1FA", "RADICL_mOPC_1FA", "RADICL_mESC_Control", "RADICL_mOPC_Control", "RADICL_mESC_4h_ActD") & Label=="<1Kb",]
subset[, Distance := abs(start.R-start.D)]

gg = ggplot(subset[, list(Count = sum(Count)), by=c("Condition", "Distance")], aes(x=Distance, y=Count, col=Condition)) + scale_x_continuous() + scale_y_continuous() + geom_line() + theme_light()
ggsave(gg, filename=paste0(working.folder, "plots/RNA-BIN_interactionDistance_condition_significant_under1kb.pdf"), units="in", width=8, height=6)
# sel.R = !overlapsAny(with(significant, GRanges(chr.R, IRanges(start.R, width=1))), blacklist.gr)
# sel.D = !overlapsAny(with(significant, GRanges(chr.D, IRanges(start.D, width=1))), blacklist.gr)
# 
# significant = significant[sel.R & sel.D,]
# 
# significant[, Condition := gsub("4h_ActD", "4h-ActD", Condition)]
# 
# subset = significant[gene_id.R!="Intergenic", list(Total = log10(sum(Count)+1),
#                                                     Count = log10(.SD[chr.R!=chr.D, sum(Count)]+1),
#                                                     Number = log10(.SD[chr.R!=chr.D, .N]+1)), by=c("Condition", "gene_id.R")]
# gg = ggpairs(subset, columns=3:5, aes(colour=Condition), columnLabels=c("RNA expression", "Inter- (counts)", "Inter- (pairs)"))
# save_plot(gg + theme_light(), filename=paste0(working.folder, "plots/RNA-BIN_interchromosomal_expression_counts_pairs_significant.png"), base_height=4, base_aspect_ratio=1.1, family="Helvetica", ncol=3, nrow=3)
```

```{r enhancedRegions, eval=FALSE}
# mESC
files.mOPC = list.files(file.path(working.folder, "mOPC_CAGE_mm10"), full.names=TRUE)

cage.mOPC = lapply(files.mOPC, function(x) with(fread(x, showProgress=FALSE), GRanges(V1, IRanges(V2, width=1), V6, score=V5)))

cage.mOPC = sapply(cage.mOPC, function(x) countOverlapsWeighted(mm10, x, ignore.strand=TRUE))

cage.mOPC = data.table(as.data.frame(mm10), CAGE=rowMeans(t(t(cage.mOPC)/colSums(cage.mOPC)*1e6)))

fwrite(cage.mOPC, file=file.path(working.folder, "mOPC_CAGE_mm10_binned_count.tsv"), sep="\t", row.names=FALSE, quote=FALSE)

files.sig = list.files(paste0(working.folder, "processed"), pattern="*_cnd_*", full.names=TRUE)

significant = fread(cmd=paste("gunzip -c", file.path(working.folder, "processed", "RNA-BIN_RADICL_mOPC_1FA_m14_level_1_2_3_Unique_25KB_significant0.05_extended_cnd_noDistance.tsv.gz")),
                    showProgress=FALSE)
setnames(significant, "replicate", "Replicate")
significant = dcast.data.table(significant[, .N, by=c("gene_id.R", "gene_id.D", "Replicate")], gene_id.R+gene_id.D~Replicate, value.var="N")
significant = significant[rowSums(significant[, 3:5, with=FALSE]>1)==3,]
significant[, Count := `1`+`2`+`3`]

# cage.mOPC = cage.mOPC[cage.mOPC$CAGE>0,]

res.mOPC = significant[, list(p.val=wilcox.test(cage.mOPC[bin_id%in%.SD[, gene_id.D], CAGE], cage.mOPC[, CAGE])$p.value), by=c("gene_id.R")]

res.mOPC[, p.adj := p.adjust(p.val, method="BH")]

fwrite(res.mOPC, file=file.path(working.folder, "mOPC_CAGE_RADICL_results.tsv"), sep="\t", row.names=FALSE, quote=FALSE)

res.mOPC = significant[, list(p.val=wilcox.test(cage.mOPC[bin_id%in%.SD[, gene_id.D], CAGE], cage.mOPC[, CAGE], alternative="less")$p.value), by=c("gene_id.R")]

res.mOPC[, p.adj := p.adjust(p.val, method="BH")]

fwrite(res.mOPC, file=file.path(working.folder, "mOPC_CAGE_RADICL_results_lowerTail.tsv"), sep="\t", row.names=FALSE, quote=FALSE)

# mESC
files.mESC = list.files(file.path(working.folder, "mESC_CAGE_mm10"), full.names=TRUE)

cage.mESC = lapply(files.mESC, function(x) with(fread(x), GRanges(V1, IRanges(V2, width=1), V6, score=V5)))

cage.mESC = sapply(cage.mESC, function(x) countOverlapsWeighted(mm10, x, ignore.strand=TRUE))

cage.mESC = data.table(as.data.frame(mm10), CAGE=rowMeans(t(t(cage.mESC)/colSums(cage.mESC)*1e6)))

fwrite(cage.mESC, file=file.path(working.folder, "mESC_CAGE_mm10_binned_count.tsv"), sep="\t", row.names=FALSE, quote=FALSE)

significant = fread(cmd=paste("gunzip -c", file.path(working.folder, "processed", "RNA-BIN_RADICL_mESC_1FA_m14_level_1_2_3_Unique_25KB_significant0.05_extended_cnd_noDistance.tsv.gz")),
                    showProgress=FALSE)
setnames(significant, "replicate", "Replicate")
significant = dcast.data.table(significant[, .N, by=c("gene_id.R", "gene_id.D", "Replicate")], gene_id.R+gene_id.D~Replicate, value.var="N")
significant = significant[rowSums(significant[, 3:5, with=FALSE]>1)==3,]
significant[, Count := `1`+`2`+`3`]

# cage.mESC = cage.mESC[cage.mESC$CAGE>0,]

res.mESC = significant[, list(p.val=wilcox.test(cage.mESC[bin_id%in%.SD[, gene_id.D], CAGE], cage.mESC[, CAGE])$p.value), by=c("gene_id.R")]

res.mESC[, p.adj := p.adjust(p.val, method="BH")]

fwrite(res.mESC, file=file.path(working.folder, "mESC_CAGE_RADICL_results.tsv"), sep="\t", row.names=FALSE, quote=FALSE)

res.mESC = significant[, list(p.val=wilcox.test(cage.mESC[bin_id%in%.SD[, gene_id.D], CAGE], cage.mESC[, CAGE], alternative="less")$p.value), by=c("gene_id.R")]

res.mESC[, p.adj := p.adjust(p.val, method="BH")]

fwrite(res.mESC, file=file.path(working.folder, "mESC_CAGE_RADICL_results_lowerTail.tsv"), sep="\t", row.names=FALSE, quote=FALSE)
```

```{r, eval=FALSE}
significant = significant[, .N, by=c("Condition", "chr.R", "start.R", "gene_id.R", "chr.D", "start.D")]
setkeyv(significant, c("Condition", "gene_id.R"))

tab = significant[, list(N=sum(N)), by=key(significant)]
tab[, quartile := findInterval(N, quantile(N, seq(0, 1, 0.25)), all.inside=TRUE), by="Condition"]

significant[tab, quartile := quartile]

significant[chr.R!=chr.D, Label := "Inter"]
significant[is.na(Label) & abs(start.R-start.D)<1e3, Label := "<1Kb"]
significant[is.na(Label) & abs(start.R-start.D)<1e4, Label := "1-10Kb"]
significant[is.na(Label) & abs(start.R-start.D)<1e5, Label := "10-100Kb"]
significant[is.na(Label) & abs(start.R-start.D)<1e6, Label := "100Kb-1Mb"]
significant[is.na(Label) & abs(start.R-start.D)>=1e6, Label := ">=1Mb"]

tab = significant[, list(N=sum(N)), by=c("Condition", "quartile", "Label", "gene_id.R")]
tab[, Label := factor(Label, levels=c("<1Kb", "1-10Kb", "10-100Kb", "100Kb-1Mb", ">=1Mb", "Inter"))]

gg = ggplot(tab, aes(x=Label, y=log2(N), fill=as.factor(quartile))) +
   ggtitle("Expression quartiles were calculated individually per condition") +
   geom_boxplot(outlier.size=0.5) +
   scale_x_discrete("Distance between RNA and DNA tags") +
   scale_y_continuous("Interactions count (log2)") +
   scale_fill_viridis(option="inferno", begin=0.2, end=0.8, discrete=TRUE) +
   facet_wrap(~Condition, ncol=7) +
   theme_light() + theme(legend.position="bottom") + guides(fill=guide_legend(title="Expression quartile"))
ggsave(gg, filename=paste0(working.folder, "plots/RNA-BIN_interactionDistance_significantQuartiles.png"), width=28, height=5, dpi=300, family="Helvetica")
ggsave(gg, filename=paste0(working.folder, "plots/RNA-BIN_interactionDistance_significantQuartiles.pdf"), width=28, height=5, family="Helvetica")
```

```{r stats_Biotypes, eval=FALSE}
raw = list()
sig = list()
for( grp in unique(paste(sampleTable[, Experiment], sampleTable[, Condition], sep="_")) ){
   
   print(grp)
   
   files = list.files(paste0(working.folder, "processed"), pattern=paste0(grp, ".*.gz"), full.names=TRUE)
   files = files[grepl(bin_width, files) & grepl("GenicOnly", files) & !grepl("significant", files)]
   raw[[grp]] = rbindlist(lapply(seq_along(files),
                                   function(x){
                                      tmp = fread(paste("gunzip -c", files[[as.numeric(as.character(x))]]), showProgress=FALSE)
                                      tmp[, replicate := as.numeric(as.character(x))]
                                      tmp[, .N, by=c("replicate", "gene_id.R", "annotation.R", "gene_type.R")]
                                   }))

   files.sig = list.files(paste0(working.folder, "processed"), pattern="*_cnd_*", full.names=TRUE)
   files.sig = files.sig[grepl(grp, files.sig)]
   tmp = fread(paste("gunzip -c", files.sig), showProgress=FALSE)
   sig[[grp]] = tmp[, .N, by=c("replicate", "gene_id.R", "annotation.R", "gene_type.R")]
   rm(tmp); gc()
}

raw = rbindlist(raw, idcol="sample")
sig = rbindlist(sig, idcol="sample")

tab = rbindlist(list(raw=raw, sig=sig), idcol="set")

tab.rep = tab[, .N, by=c("set", "sample", "replicate", "annotation.R")]
tab.rep = rbindlist(list(tab.rep,
                         with(tab.rep, expand.grid(set=unique(set), sample=unique(sample), replicate=1:3,
                                                   annotation.R=unique(annotation.R), N=NA))))
tab.rep[, annotation.R := factor(annotation.R, levels=c("protein_coding", "long_ncRNA", "ncRNA", "other", "NAST"))]

gg = ggplot(tab.rep, aes(x = annotation.R, y = N, fill = as.factor(replicate))) +
   geom_bar(position="dodge", stat="identity", col="black") +
   scale_y_continuous("Count") + scale_x_discrete("") +
   scale_fill_manual(values=rev(c("#74a9cf", "#2b8cbe", "#045a8d"))) +
   facet_grid(set~sample, scales="free_y") +
   theme_light() + theme(axis.text.x=element_text(angle=45, hjust=1)) + guides(fill=guide_legend(title="Replicate"))
ggsave(gg, filename=paste0(working.folder, "plots/RNA-BIN_RNAbiotypes_unique_replicate.png"), width=14, height=6, dpi=300, family="Helvetica")
ggsave(gg, filename=paste0(working.folder, "plots/RNA-BIN_RNAbiotypes_unique_replicate.pdf"), width=14, height=6, family="Helvetica")

tab.cnd = tab[, list(N=length(unique(gene_id.R))), by=c("set", "sample", "annotation.R")]
tab.cnd = rbindlist(list(tab.cnd,
                         with(tab.cnd, expand.grid(set=unique(set), sample=unique(sample),
                                                   annotation.R=unique(annotation.R), N=NA))))
tab.cnd[, annotation.R := factor(annotation.R, levels=c("protein_coding", "long_ncRNA", "ncRNA", "other", "NAST"))]

gg = ggplot(tab.cnd, aes(x = annotation.R, y = N, fill = set)) +
   geom_bar(position="dodge", stat="identity", col="black") +
   scale_y_continuous("Count") + scale_x_discrete("") +
   scale_fill_tableau(palette="Classic Blue-Red 6") +
   facet_grid(~sample, scales="free_y") +
   theme_light() + theme(axis.text.x=element_text(angle=45, hjust=1)) + guides(fill=guide_legend(title="Set"))
ggsave(gg, filename=paste0(working.folder, "plots/RNA-BIN_RNAbiotypes_unique_condition.png"), width=14, height=4, dpi=300, family="Helvetica")
ggsave(gg, filename=paste0(working.folder, "plots/RNA-BIN_RNAbiotypes_unique_condition.pdf"), width=14, height=4, family="Helvetica")

tab.rep = tab[, list(N=sum(N)), by=c("set", "sample", "replicate", "annotation.R")]
tab.rep = rbindlist(list(tab.rep,
                         with(tab.rep, expand.grid(set=unique(set), sample=unique(sample), replicate=1:3,
                                                   annotation.R=unique(annotation.R), N=NA))))
tab.rep[, annotation.R := factor(annotation.R, levels=c("protein_coding", "long_ncRNA", "ncRNA", "other", "NAST"))]

gg = ggplot(tab.rep, aes(x = annotation.R, y = N, fill = as.factor(replicate))) +
   geom_bar(position="dodge", stat="identity", col="black") +
   scale_y_log10("Count", breaks=10^c(0:8)) + scale_x_discrete("") +
   scale_fill_manual(values=rev(c("#74a9cf", "#2b8cbe", "#045a8d"))) +
   facet_grid(set~sample, scales="free_y") +
   theme_light() + theme(axis.text.x=element_text(angle=45, hjust=1)) + guides(fill=guide_legend(title="Replicate"))
ggsave(gg, filename=paste0(working.folder, "plots/RNA-BIN_RNAbiotypes_count_replicate.png"), width=14, height=6, dpi=300, family="Helvetica")
ggsave(gg, filename=paste0(working.folder, "plots/RNA-BIN_RNAbiotypes_count_replicate.pdf"), width=14, height=6, family="Helvetica")


tab.cnd = tab[, list(N=sum(N)), by=c("set", "sample", "annotation.R")]
tab.cnd = rbindlist(list(tab.cnd,
                         with(tab.cnd, expand.grid(set=unique(set), sample=unique(sample),
                                                   annotation.R=unique(annotation.R), N=NA))))
tab.cnd[, annotation.R := factor(annotation.R, levels=c("protein_coding", "long_ncRNA", "ncRNA", "other", "NAST"))]

gg = ggplot(tab.cnd, aes(x = annotation.R, y = N, fill = set)) +
   geom_bar(position="dodge", stat="identity", col="black") +
   scale_y_log10("Count", breaks=10^c(0:8)) + scale_x_discrete("") +
   scale_fill_tableau(palette="Classic Blue-Red 6") +
   facet_grid(~sample, scales="free_y") +
   theme_light() + theme(axis.text.x=element_text(angle=45, hjust=1)) + guides(fill=guide_legend(title="Set"))
ggsave(gg, filename=paste0(working.folder, "plots/RNA-BIN_RNAbiotypes_count_condition.png"), width=14, height=4, dpi=300, family="Helvetica")
ggsave(gg, filename=paste0(working.folder, "plots/RNA-BIN_RNAbiotypes_count_condition.pdf"), width=14, height=4, family="Helvetica")
```

```{r interactions_vs_counts, eval=FALSE}
sig = list()
for( grp in unique(paste(sampleTable[, Experiment], sampleTable[, Condition], sep="_")) ){
   
   print(grp)

   files.sig = list.files(paste0(working.folder, "processed"), pattern="*_cnd_*", full.names=TRUE)
   files.sig = files.sig[grepl(grp, files.sig)]
   tmp = fread(paste("gunzip -c", files.sig), showProgress=FALSE)
   sig[[grp]] = tmp[, list(.N), by=c("gene_id.R", "gene_id.D", "annotation.R")]
   rm(tmp); gc()
}

tab = rbindlist(sig[c("GRID_mESC_GRIDseq", "RADICL_mESC_1FA", "RADICL_mOPC_1FA")], idcol="sample")
tab = tab[, list(N = log10(.N)), by=c("sample", "gene_id.R", "annotation.R")] 
tab = dcast.data.table(tab, gene_id.R+annotation.R~sample, value.var="N", fill=0)
tab = tab[rowSums(tab[, 3:ncol(tab), with=FALSE]>0)>1,]

tab[, annotation.R := factor(annotation.R, levels=c("protein_coding", "long_ncRNA", "ncRNA", "other", "NAST"))]
tab = tab[order(annotation.R)]

# upperfun <- function(data,mapping){
#   ggplot(data = data, mapping = mapping)+
#     geom_density2d()+
#     scale_x_continuous(limits = c(0,4))+
#     scale_y_continuous(limits = c(0,4))
# }   

lowerfun <- function(data,mapping){
  ggplot(data = data, mapping = mapping) +
    geom_abline(intercept = 0, slope = 1) +
    geom_point(size=0.25) +
    scale_x_continuous(limits = c(0,4)) +
    scale_y_continuous(limits = c(0,4))
}  

p = ggpairs(tab, columns=3:5, mapping=ggplot2::aes(colour = annotation.R),
            # upper = list(continuius = wrap(upperfun)),
            lower = list(continuous = wrap(lowerfun)),
            diag = list(continuous = wrap("densityDiag", alpha=0.5))) +
   theme_light()

for(i in 1:p$nrow) {
   for(j in 1:p$ncol){
      p[i,j] <- p[i,j] + 
         scale_fill_tableau(palette = "Tableau 10") +
         scale_colour_tableau(palette = "Tableau 10")  
   }
}

# stat_density_2d(aes(fill=CAV,alpha=..level..), geom="polygon")

save_plot(p, filename=paste0(working.folder, "plots/RNA-BIN_", bin_width, "_interactingBins_perTranscript_significant.png"), base_height=8)
save_plot(p, filename=paste0(working.folder, "plots/RNA-BIN_", bin_width, "_interactingBins_perTranscript_significant.pdf"), base_height=8)

p = ggplot(tab, aes(x=RADICL_mESC_1FA, y=GRID_mESC_GRIDseq, col=annotation.R, fill=annotation.R)) +
   geom_abline(intercept = 0, slope = 1) +
   stat_density_2d(aes(alpha=..level..), geom="polygon", ) +
   scale_fill_tableau(palette = "Tableau 10") +
   scale_colour_tableau(palette = "Tableau 10") +
   facet_grid(~annotation.R) +
   theme_light() + guides(alpha = FALSE, colour = FALSE, fill = FALSE)
save_plot(p, filename=paste0(working.folder, "plots/RNA-BIN_", bin_width, "_interactingBins_perTranscript_significant_GRID-RADICL.png"), base_height=4, ncol=5, base_aspect_ratio=0.9)
save_plot(p, filename=paste0(working.folder, "plots/RNA-BIN_", bin_width, "_interactingBins_perTranscript_significant_GRID-RADICL.pdf"), base_height=5, ncol=5, base_aspect_ratio=0.9)


sig = rbindlist(lapply(sig,
                       function(x)
                          x[, list(Interactions = .N, Sum_count = sum(N), Median_count = median(N)), by=c("gene_id.R", "annotation.R")]),
                idcol="sample")

sig = melt.data.table(sig, id.vars=c("sample", "gene_id.R", "annotation.R", "Interactions"), measure.vars=c("Sum_count", "Median_count"))

gg = ggplot(sig, aes(x=Interactions, y=value, col=annotation.R)) +
   geom_point(size=0.5) +
   scale_x_log10() + scale_y_log10() +
   scale_colour_tableau() +
   facet_grid(variable~sample, scales="free_y") +
   theme_light()
ggsave(gg, filename=paste0(working.folder, "plots/RNA-BIN_Interactions_vs_counts_significant.png"), width=14, height=4, dpi=300, family="Helvetica")
ggsave(gg, filename=paste0(working.folder, "plots/RNA-BIN_Interactions_vs_counts_significant.pdf"), width=14, height=4, family="Helvetica")
```

```{r transIntersect, eval=FALSE}
files.sig = list.files(paste0(working.folder, "processed"), pattern="*_cnd_*", full.names=TRUE)

significant = rbindlist(lapply(files.sig,
                               function(x)
                                  fread(cmd=paste("gunzip -c", x), showProgress=FALSE)))
setnames(significant, "replicate", "Replicate")

setkeyv(significant, c("Condition", "chr.R", "start.R", "chr.D", "start.D", "gene_id.R", "gene_id.D"))
significant = significant[, .N, by=key(significant)]
setnames(significant, "N", "Count")

sel.R = !overlapsAny(with(significant, GRanges(chr.R, IRanges(start.R, width=1))), blacklist.gr)
sel.D = !overlapsAny(with(significant, GRanges(chr.D, IRanges(start.D, width=1))), blacklist.gr)

significant = significant[sel.R & sel.D,]

esc = significant[Condition=="RADICL_mESC_1FA",]
opc = significant[Condition=="RADICL_mOPC_1FA",]

vennlist = list(mESC=unique(esc[chr.R!=chr.D, gene_id.R]), mOPC=unique(opc[chr.R!=chr.D, gene_id.R]))
venn.plot.R = venn.diagram(vennlist, filename=NULL, fill=c("dodgerblue3", "firebrick3", "grey3")[1:length(vennlist)],
                               resolution=300, imagetype="png", units="in", height=10, width=10,
                               print.mode = c("raw", "percent"),
                           fontfamily = "Helvetica", main.fontfamily = "Helvetica", cat.fontfamily = "Helvetica")
pdf(file=paste0(working.folder, "plots/RNA-DNA_mESC-mOPC_trans_RNA_significant.pdf"), width=6, height=6)
   grid.arrange(gTree(children=venn.plot.R), ncol=1)
dev.off()

vennlist = list(mESC=unique(esc[chr.R!=chr.D, gene_id.D]), mOPC=unique(opc[chr.R!=chr.D, gene_id.D]))
venn.plot.D = venn.diagram(vennlist, filename=NULL, fill=c("dodgerblue3", "firebrick3", "grey3")[1:length(vennlist)],
                               resolution=300, imagetype="png", units="in", height=10, width=10,
                               print.mode = c("raw", "percent"),
                           fontfamily = "Helvetica", main.fontfamily = "Helvetica", cat.fontfamily = "Helvetica")
pdf(file=paste0(working.folder, "plots/RNA-DNA_mESC-mOPC_trans_DNA_significant.pdf"), width=6, height=6)
   grid.arrange(gTree(children=venn.plot.D), ncol=1)
dev.off()

vennlist = list(mESC=unique(esc[chr.R!=chr.D, paste(gene_id.R, gene_id.D)]), mOPC=unique(opc[chr.R!=chr.D, paste(gene_id.R, gene_id.D)]))
venn.plot.RD = venn.diagram(vennlist, filename=NULL, fill=c("dodgerblue3", "firebrick3", "grey3")[1:length(vennlist)],
                               resolution=300, imagetype="png", units="in", height=10, width=10,
                               print.mode = c("raw", "percent"),
                           fontfamily = "Helvetica", main.fontfamily = "Helvetica", cat.fontfamily = "Helvetica")
pdf(file=paste0(working.folder, "plots/RNA-DNA_mESC-mOPC_trans_RNA-DNA_significant.pdf"), width=6, height=6)
   grid.arrange(gTree(children=venn.plot.RD), ncol=1)
dev.off()

id.coutns = data.table(as.data.frame(mm10), key="seqnames")[, .N, by="seqnames"]
setkey(significant, chr.D)
significant[id.coutns, gene_id.D.counts := N]

covchr = significant[chr.D!="chrM", list(length(unique(gene_id.D))/unique(gene_id.D.counts)),
                     by=c("Condition", "chr.D", "gene_id.R")]

covchr[, gene_id.R := factor(gene_id.R, levels=covchr[order(-V1), unique(gene_id.R)])]

covchr = dcast.data.table(covchr, gene_id.R+Condition~chr.D, value.var="V1", fill=0)

covchr = covchr[rowMax(as.matrix(covchr[, 3:ncol(covchr), with=FALSE]))>0.1,]

fwrite(covchr, file=file.path(working.folder, "RNA-DNA_Chromosome_coverage_above10percent_significant.tsv"), quote=FALSE, sep="\t")


esc.m = significant[Condition=="RADICL_mESC_1FA" & gene_id.R=="ENSMUSG00000092341.2",]
esc.n = significant[Condition=="RADICL_mESC_1FA" & gene_id.R=="ENSMUSG00000092274.2",]
opc.m = significant[Condition=="RADICL_mOPC_1FA" & gene_id.R=="ENSMUSG00000092341.2",]
opc.n = significant[Condition=="RADICL_mOPC_1FA" & gene_id.R=="ENSMUSG00000092274.2",]

vennlist = list(Malat1=unique(esc.m[, gene_id.D]), Neat1=unique(esc.n[, gene_id.D]))
venn.plot.D = venn.diagram(vennlist, filename=NULL, fill=c("dodgerblue3", "firebrick3", "grey3")[1:length(vennlist)],
                               resolution=300, imagetype="png", units="in", height=10, width=10,
                               print.mode = c("raw", "percent"),
                           fontfamily = "Helvetica", main.fontfamily = "Helvetica", cat.fontfamily = "Helvetica")
pdf(file=paste0(working.folder, "plots/RNA-DNA_Malat1-Neat1_mESC_significant.pdf"), width=6, height=6)
   grid.arrange(gTree(children=venn.plot.D), ncol=1)
dev.off()

vennlist = list(Malat1=unique(opc.m[, gene_id.D]), Neat1=unique(opc.n[, gene_id.D]))
venn.plot.D = venn.diagram(vennlist, filename=NULL, fill=c("dodgerblue3", "firebrick3", "grey3")[1:length(vennlist)],
                               resolution=300, imagetype="png", units="in", height=10, width=10,
                               print.mode = c("raw", "percent"),
                           fontfamily = "Helvetica", main.fontfamily = "Helvetica", cat.fontfamily = "Helvetica")
pdf(file=paste0(working.folder, "plots/RNA-DNA_Malat1-Neat1_mOPC_significant.pdf"), width=6, height=6)
   grid.arrange(gTree(children=venn.plot.D), ncol=1)
dev.off()


opc.n.gr = subsetByOverlaps(with(opc.n, GRanges(chr.D, IRanges(start=start.D, width=1), score = Count)), resize(genes, width(genes)+500, fix="end"))
opc.n.dt = data.table(as.data.frame(distanceToNearest(opc.n.gr, resize(genes, width(genes)+500, fix="end"))))
opc.n.dt[, score := opc.n.gr[queryHits,]$score]
opc.n.dt[, distance := distance(opc.n.gr[queryHits,], promoters(resize(genes, width(genes)+500, fix="end")[subjectHits,], 0, 1))]
opc.n.dt[, width := width(genes[subjectHits,])]
opc.n.dt[, position := round((distance-500)/width, 2)]
opc.n.dt = opc.n.dt[width>1000,]

gg = ggplot(opc.n.dt[, list(score = sum(score)), by="position"], aes(x=position, y=score)) + geom_vline(xintercept=c(0,1), lty=2, col="grey") + geom_line() + scale_x_continuous("Position (bp)", labels=scales::percent) + scale_y_continuous("DNA tags count") + theme_bw()
ggsave(gg, filename=paste0(working.folder, "plots/Neat1_mOPC_DNA_tags_gene_body_significant.pdf"), width=12, height=6, family="Helvetica")

fwrite(with(opc.n.dt,
            data.table(gene_id=genes[subjectHits,]$gene_id, score = score))[, list(score = sum(score)), by="gene_id"][order(-score),],
       file=paste0(working.folder, "Neat1_mOPC_DNA_tags_gene_targets.tsv"), sep="\t", quote=FALSE)

esc.n.gr = subsetByOverlaps(with(esc.n, GRanges(chr.D, IRanges(start=start.D, width=1), score = Count)), resize(genes, width(genes)+500, fix="end"))
esc.n.dt = data.table(as.data.frame(distanceToNearest(esc.n.gr, resize(genes, width(genes)+500, fix="end"))))
esc.n.dt[, score := esc.n.gr[queryHits,]$score]
esc.n.dt[, distance := distance(esc.n.gr[queryHits,], promoters(resize(genes, width(genes)+500, fix="end")[subjectHits,], 0, 1))]
esc.n.dt[, width := width(genes[subjectHits,])]
esc.n.dt[, position := round((distance-500)/width, 2)]
esc.n.dt = esc.n.dt[width>1000,]

gg = ggplot(esc.n.dt[, list(score = sum(score)), by="position"], aes(x=position, y=score)) + geom_vline(xintercept=c(0,1), lty=2, col="grey") + geom_line() + scale_x_continuous("Position (bp)", labels=scales::percent) + scale_y_continuous("DNA tags count") + theme_bw()
ggsave(gg, filename=paste0(working.folder, "plots/Neat1_mESC_DNA_tags_gene_body_significant.pdf"), width=12, height=6, family="Helvetica")

fwrite(with(esc.n.dt,
            data.table(gene_id=genes[subjectHits,]$gene_id, score = score))[, list(score = sum(score)), by="gene_id"][order(-score),],
       file=paste0(working.folder, "Neat1_mESC_DNA_tags_gene_targets.tsv"), sep="\t", quote=FALSE)
```

```{r cellSpecificInteractions, eval=FALSE}
files.sig = list.files(paste0(working.folder, "processed"), pattern="*_cnd_*", full.names=TRUE)

significant = rbindlist(lapply(files.sig,
                               function(x)
                                  fread(cmd=paste("gunzip -c", x), showProgress=FALSE)))
setnames(significant, "replicate", "Replicate")

setkeyv(significant, c("Condition", "chr.R", "start.R", "chr.D", "start.D", "gene_id.R", "gene_id.D"))
significant = significant[, .N, by=key(significant)]
setnames(significant, "N", "Count")

sel.R = !overlapsAny(with(significant, GRanges(chr.R, IRanges(start.R, width=1))), blacklist.gr)
sel.D = !overlapsAny(with(significant, GRanges(chr.D, IRanges(start.D, width=1))), blacklist.gr)

significant = significant[sel.R & sel.D,]

esc = significant[Condition=="RADICL_mESC_1FA",]
setkey(esc, gene_id.R)
opc = significant[Condition=="RADICL_mOPC_1FA",]
setkey(opc, gene_id.R)

# Load Cell-specific markers
esc_markers = fread(file.path(working.folder, "ESC_markers.txt"),
                    select=1:4, col.names=c("gene_name", "gene_id", "CAGE_mESC", "CAGE_mOPC"))
esc_markers[, Marker := factor("mESC", levels=c("mESC", "mOPC"))]
opc_markers = fread(file.path(working.folder, "OPC_markers.txt"),
                    select=c(1, 4:6), col.names=c("gene_name", "gene_id", "CAGE_mESC", "CAGE_mOPC"))
opc_markers = opc_markers[!(is.na(gene_id)|gene_id==""),]
opc_markers[, Marker := factor("mOPC", levels=c("mESC", "mOPC"))]

sel_markers = fread(file.path(working.folder, "mOPC_selected_markers.txt"), header=FALSE)
opc_markers = opc_markers[gene_name%in%sel_markers[,V1],]

markers = rbindlist(list(esc_markers, opc_markers))
markers[, gene_id := sapply(markers$gene_id, function(x) genes.dt[grepl(as.character(x), gene_id), as.character(gene_id)])]
setkey(markers, gene_id)

sample.names = unique(paste(sampleTable[Condition=="1FA", Experiment], sampleTable[Condition=="1FA", Condition], sep="_"))
librarySize = lapply(setNames(sample.names, sample.names),
                     function(grp){
                        
                        print(grp)
                        
                        files = list.files(paste0(working.folder, "processed"), pattern=paste0(grp, ".*.gz"), full.names=TRUE)
                        files = files[grepl(bin_width, files) & !grepl("GenicOnly", files) & !grepl("significant", files)]
                        sum(sapply(seq_along(files),
                                   function(x){
                                      tmp = fread(cmd=paste("gunzip -c", files[[as.numeric(as.character(x))]]), showProgress=FALSE)
                                      tmp[, replicate := as.numeric(as.character(x))]
                                      nrow(tmp)
                                   }))})

esc[, Normalised := Count/librarySize[["RADICL_mESC_1FA"]]*1e6]
opc[, Normalised := Count/librarySize[["RADICL_mOPC_1FA"]]*1e6]

esc.norm = esc[, sum(Normalised), by="gene_id.R"]
opc.norm = opc[, sum(Normalised), by="gene_id.R"]

merged = merge(esc.norm, opc.norm, all=TRUE)
merged[is.na(V1.x), V1.x := min(merged[is.finite(V1.x), V1.x], na.rm=TRUE)]
merged[is.na(V1.y), V1.y := min(merged[is.finite(V1.y), V1.y], na.rm=TRUE)]
merged[, Ratio := log2(V1.x)-log2(V1.y)]
merged[, EDF := rank(Ratio)/length(Ratio)]

markers[merged, `:=`(Ratio = Ratio, EDF = EDF)]
markers = markers[!is.na(EDF),]

fwrite(merged, file=paste0(working.folder, "RNA_Ratio_mESC_over_mOPC.tsv"), sep="\t", row.names=FALSE)

# fisher.test(matrix(c(rev(table(markers[Marker=="mESC",][,EDF>=0.95])),
#        rev(table(merged[!gene_id.R%in%markers[Marker=="mESC", gene_id], EDF>=0.95]))), 2, 2, byrow=TRUE), alternative="greater")
# 
# fisher.test(matrix(c(rev(table(markers[Marker=="mOPC",][,EDF<=0.05])),
#        rev(table(merged[!gene_id.R%in%markers[Marker=="mOPC", gene_id], EDF<=0.05]))), 2, 2, byrow=TRUE), alternative="greater")
# 

mRNA = merged[gene_id.R%in%markers[, gene_id],][order(-Ratio), list(gene_id.R, Ratio)]
mRNA[, Marker := markers[match(gene_id.R, markers[, gene_id]), Marker]]
fwrite(mRNA, file=paste0(working.folder, "Markers_RNA_Ratio_mESC_over_mOPC.tsv"), sep="\t", row.names=FALSE)

mRNA = merged[gene_id.R%in%markers[Marker=="mOPC", gene_id],][order(-Ratio),][Ratio>0, list(gene_id.R, Ratio)]
fwrite(mRNA, file=paste0(working.folder, "Markers_RNA_OPC_higher_in_ESC.tsv"), sep="\t", row.names=FALSE)

gg1 = ggplot(merged, aes(x=Ratio)) + ggtitle("mESC markers (RNA)") + geom_rug(data=markers[Marker=="mESC",], aes(x=Ratio), col="red") + geom_histogram(bins=100) + scale_x_continuous("RADICL mESC/mOPC (normalised interactions)", limits=c(-15, 16)) + theme_bw()
gg2 = ggplot(merged, aes(x=Ratio)) + ggtitle("mOPC markers (RNA)") + geom_rug(data=markers[Marker=="mOPC",], aes(x=Ratio), col="blue") + geom_histogram(bins=100) + scale_x_continuous("RADICL mESC/mOPC (normalised interactions)", limits=c(-15, 16)) + theme_bw()
gg = ggarrange(gg1, gg2, nrow=2)
save_plot(gg, filename=paste0(working.folder, "plots/RNA-BIN_", bin_width, "_cell_type_specific_RNAmarkers_RADICL_ratio.pdf"), base_height=6, base_aspect_ratio=2, ncol=1, nrow=2)

gg = ggplot(merged, aes(x=Ratio, y=EDF)) + ggtitle("RNA markers") + geom_line() + geom_point(data=markers[Marker=="mESC",], aes(x=Ratio, y=EDF), col="red", size=0.75) + geom_point(data=markers[Marker=="mOPC",], aes(x=Ratio, y=EDF), col="blue", size=0.75) + scale_x_continuous("RADICL mESC/mOPC (normalised interactions)", limits=c(-15, 16)) + theme_bw()
save_plot(gg, filename=paste0(working.folder, "plots/RNA-BIN_", bin_width, "_cell_type_specific_RNAmarkers_RADICL_ratio_CDF.pdf"), base_height=6)

# Analysis on RNA markers
selected = rbindlist(lapply(list(mESC=esc, mOPC=opc),
                            function(x){
                               tmp = x[markers,]
                               tmp[!is.na(Count), list(Unique = length(unique(gene_id.D)), Count = sum(Count)),
                                 by=c("Marker", "gene_id.R", "gene_name", "CAGE_mESC", "CAGE_mOPC")]
                            }), idcol="RADICL")
selected[order(Count), Index := seq_along(gene_id.R), by="Marker"]
selected[!gene_id.R%in%selected[, .N, by="gene_id.R"][N>1, gene_id.R], gene_name := NA]

require(ggrepel)
gg = ggplot(selected, aes(x=Index, y=log10(Count), colour=RADICL, fill=RADICL)) +
   geom_point(pch=21, colour="grey") +
   scale_x_continuous("Interaction rank") +
   scale_y_continuous("Transcript interactions (log10)") +
   scale_fill_brewer(palette="Set1")  +
   facet_wrap(~Marker, scales="free") +
   theme_light()
save_plot(gg, filename=paste0(working.folder, "plots/RNA-BIN_", bin_width, "_cell_type_specific_RNAmarkers.pdf"), base_height=6, base_aspect_ratio=1.1, ncol=2, nrow=1)

gg = ggplot(selected, aes(x=Index, y=log10(Count), colour=RADICL, fill=RADICL, label=gene_name)) +
   geom_point(pch=21, colour="grey") +
   scale_x_continuous("Interaction rank") +
   scale_y_continuous("Transcript interactions (log10)") +
   scale_colour_brewer(palette="Set1", guide=FALSE)  +
   scale_fill_brewer(palette="Set1")  +
   geom_text_repel(size=3) +
   facet_wrap(~Marker, scales="free") +
   theme_light()
save_plot(gg, filename=paste0(working.folder, "plots/RNA-BIN_", bin_width, "_cell_type_specific_RNAmarkers_labelled.pdf"), base_height=6, base_aspect_ratio=1.1, ncol=2, nrow=1)

sel = copy(selected)
sel[is.na(Count), Count := 0]
ggplot(dcast.data.table(sel, Marker+gene_id.R~RADICL, value.var="Count", fill=0), aes(x=log2(mESC), y=log2(mOPC), col=Marker)) + geom_point() + coord_fixed(xlim=c(0, 15), ylim=c(0, 15))

# selected[Marker=="mESC", cor(Count, CAGE_mESC, method="spearman", use="pairwise.complete.obs"), by="RADICL"]
# selected[Marker=="mOPC", cor(Count, CAGE_mOPC, method="spearman", use="pairwise.complete.obs"), by="RADICL"]
ggplot() +
    geom_point(data=selected[Marker=="mESC"], aes(x=log10(CAGE_mESC+1), y=log10(Count), colour=RADICL, fill=RADICL), pch=21, colour="grey") +
    geom_point(data=selected[Marker=="mOPC"], aes(x=log10(CAGE_mOPC+1), y=log10(Count), colour=RADICL, fill=RADICL), pch=21, colour="grey") +
    scale_x_continuous("CAGE expression (log10)") +
    scale_y_continuous("Transcript interactions (log10)") +
    scale_fill_brewer(palette="Set1")  +
    facet_wrap(~Marker, scales="free") +
    theme_light()

# Analysis on RNA markers without self-interactions
selected = rbindlist(lapply(setNames(markers[, gene_id], markers[, gene_id]),
                            function(x){
                               tmp = with(genes.dt[gene_id==as.character(x),],
                                          rbindlist(list(esc, opc))[gene_id.R==as.character(x) & !(chr.D==seqnames & start.D>(start-2e3) & start.D<(end+2e3)),
                                                                    list(Count = sum(Count)),
                                                                    by=c("Condition", "gene_id.R")])
                               setkey(tmp, gene_id.R)
                               tmp[markers,]
                            }),
                     idcol="gene_id")
setkey(selected, gene_id)

selected = selected[!is.na(Count) & gene_id==gene_id.R,]
selected[order(Count), Index := seq_along(gene_id.R), by="Marker"]
selected[!gene_id.R%in%selected[, .N, by="gene_id.R"][N>1, gene_id.R], gene_name := NA]

require(ggrepel)
gg = ggplot(selected, aes(x=Index, y=log10(Count), colour=Condition, fill=Condition)) +
   geom_point(pch=21, colour="grey") +
   scale_x_continuous("Interaction rank") +
   scale_y_continuous("Transcript interactions (log10)") +
   scale_fill_brewer(palette="Set1")  +
   facet_wrap(~Marker, scales="free") +
   theme_light()
save_plot(gg, filename=paste0(working.folder, "plots/RNA-BIN_", bin_width, "_cell_type_specific_RNAmarkers_noSelf.pdf"), base_height=6, base_aspect_ratio=1.1, ncol=2, nrow=1)

gg = ggplot(selected, aes(x=Index, y=log10(Count), colour=Condition, fill=Condition, label=gene_name)) +
   geom_point(pch=21, colour="grey") +
   scale_x_continuous("Interaction rank") +
   scale_y_continuous("Transcript interactions (log10)") +
   scale_colour_brewer(palette="Set1", guide=FALSE)  +
   scale_fill_brewer(palette="Set1")  +
   geom_text_repel(size=3) +
   facet_wrap(~Marker, scales="free") +
   theme_light()
save_plot(gg, filename=paste0(working.folder, "plots/RNA-BIN_", bin_width, "_cell_type_specific_RNAmarkers_noSelf_labelled.pdf"), base_height=6, base_aspect_ratio=1.1, ncol=2, nrow=1)


# Analysis on Chromatin markers (region)
selected = rbindlist(lapply(setNames(markers[, gene_id], markers[, gene_id]),
                            function(x)
                               with(genes.dt[gene_id==as.character(x),],
                                    rbindlist(list(esc, opc))[chr.D==seqnames & start.D>(start-2e3) & start.D<(end+2e3),
                                                              list(uRNA = length(unique(gene_id.R)), Count = sum(Count)),
                                                              by=c("Condition")])),
                     idcol="gene_id")
setkey(selected, gene_id)

selected = selected[markers,]
selected[order(uRNA), Index := seq_along(gene_id), by="Marker"]

ggplot(selected, aes(x=Index, y=log10(Count), colour=Condition, fill=Condition)) +
   geom_point(pch=21, colour="grey") +
   scale_x_continuous("Rank") +
   scale_y_continuous("Chromatin interactions (log10)") +
   scale_fill_brewer(palette="Set1")  +
   facet_wrap(~Marker, scales="free") +
   theme_light()

gg = ggplot(selected, aes(x=log10(uRNA), y=log10(Count), colour=Condition, fill=Condition)) +
   geom_point(pch=21, colour="grey") +
   scale_x_continuous("Unique interacting RNAs") +
   scale_y_continuous("Chromatin interactions (log10)") +
   scale_fill_brewer(palette="Set1")  +
   facet_wrap(~Marker, scales="free") +
   theme_light()
save_plot(gg, filename=paste0(working.folder, "plots/RNA-BIN_", bin_width, "_cell_type_specific_DNAmarkers.pdf"), base_height=6, base_aspect_ratio=1.1, ncol=2, nrow=1)

gg = ggplot() +
   geom_point(data=selected[Condition=="RADICL_mESC_1FA",], aes(x=log10(CAGE_mESC+1), y=log10(Count), colour=Condition, fill=Condition), pch=21, colour="grey") +
   geom_point(data=selected[Condition=="RADICL_mOPC_1FA",], aes(x=log10(CAGE_mOPC+1), y=log10(Count), colour=Condition, fill=Condition), pch=21, colour="grey") +
   scale_x_continuous("CAGE expression (log10)") +
   scale_y_continuous("Chromatin interactions (log10)") +
   scale_fill_brewer(palette="Set1")  +
   facet_wrap(~Marker, scales="free") +
   theme_light()
save_plot(gg, filename=paste0(working.folder, "plots/RNA-BIN_", bin_width, "_cell_type_specific_DNAmarkers_expression.pdf"), base_height=6, base_aspect_ratio=1.1, ncol=2, nrow=1)

# Analysis on Chromatin markers (region) without self-interactions
selected = rbindlist(lapply(setNames(markers[, gene_id], markers[, gene_id]),
                            function(x)
                               with(genes.dt[gene_id==as.character(x),],
                                    rbindlist(list(esc, opc))[chr.D==seqnames & start.D>(start-2e3) & start.D<(end+2e3) & gene_id.R!=as.character(x),
                                                              list(uRNA = length(unique(gene_id.R)), Count = sum(Count)),
                                                              by=c("Condition")])),
                     idcol="gene_id")
setkey(selected, gene_id)

selected = selected[markers,]

gg = ggplot(selected, aes(x=log10(uRNA), y=log10(Count), colour=Condition, fill=Condition)) +
   geom_point(pch=21, colour="grey") +
   scale_x_continuous("Unique interacting RNAs") +
   scale_y_continuous("Chromatin interactions (log10)") +
   scale_fill_brewer(palette="Set1")  +
   facet_wrap(~Marker, scales="free") +
   theme_light()
save_plot(gg, filename=paste0(working.folder, "plots/RNA-BIN_", bin_width, "_cell_type_specific_DNAmarkers_noSelf.pdf"), base_height=6, base_aspect_ratio=1.1, ncol=2, nrow=1)

gg = ggplot() +
   geom_point(data=selected[Condition=="RADICL_mESC_1FA",], aes(x=log10(CAGE_mESC+1), y=log10(Count), colour=Condition, fill=Condition), pch=21, colour="grey") +
   geom_point(data=selected[Condition=="RADICL_mOPC_1FA",], aes(x=log10(CAGE_mOPC+1), y=log10(Count), colour=Condition, fill=Condition), pch=21, colour="grey") +
   scale_x_continuous("CAGE expression (log10)") +
   scale_y_continuous("Chromatin interactions (log10)") +
   scale_fill_brewer(palette="Set1")  +
   facet_wrap(~Marker, scales="free") +
   theme_light()
save_plot(gg, filename=paste0(working.folder, "plots/RNA-BIN_", bin_width, "_cell_type_specific_DNAmarkers_noSelf_expression.pdf"), base_height=6, base_aspect_ratio=1.1, ncol=2, nrow=1)


# Table top10 RNA per DNA locus
genes.sel = as.character(genes[countOverlaps(resize(genes, width=width(genes)+2e3, fix="center"),
                              with(significant, GRanges(chr.D, IRanges(start.D, width=1))))>=10,]$gene_id)
# selected = pblapply(setNames(genes.sel, genes.sel),
#                             function(x){
#                                tmp = with(genes.dt[gene_id==as.character(x),],
#                                     significant[gene_id.R!=as.character(x) & chr.D==seqnames & start.D>(start-2e3) & start.D<(end+2e3),
#                                                               list(RNAcount = sum(Count)), by=c("Condition", "gene_id.R")])
#                                tmp[order(-RNAcount),
#                                    list(Count = sum(RNAcount), Unique = .N,
#                                         RNAid = .SD[, head(gene_id.R, 10)], RNAcount = .SD[, head(RNAcount, 10)]), by="Condition"]
#                                })
# selected = rbindlist(selected, idcol="gene_id")
# fwrite(selected, file=paste0(working.folder, "DNAinteractions_perGeneRegion_2kb_RNAcounts.tsv"), sep="\t", row.names=FALSE)
# fwrite(selected[Condition%in%c("RADICL_mESC_1FA", "RADICL_mOPC_1FA"),], file=paste0(working.folder, "DNAinteractions_perGeneRegion_2kb_RNAcounts_1FA_mESC_mOPC.tsv"), sep="\t", row.names=FALSE)
selected = fread(paste0(working.folder, "DNAinteractions_perGeneRegion_2kb_RNAcounts.tsv"))

gg = ggplot(unique(selected[Condition%in%c("RADICL_mESC_1FA", "RADICL_mOPC_1FA"), list(Condition, gene_id, Count, Unique)]), aes(x=log2(Unique), y=log2(Count))) + geom_point() + scale_x_continuous("Unique interacting RNAs") + scale_y_continuous("Interactions per locus") + facet_wrap(~Condition)
save_plot(gg, filename=paste0(working.folder, "plots/RNA-BIN_", bin_width, "_cell_type_unique_RNAs_vs_DNA_interactions.pdf"), base_height=6, base_aspect_ratio=1.1, ncol=2, nrow=1)

esc = selected[Condition=="RADICL_mESC_1FA", list(Count=unique(Count)), by="gene_id"]
opc = selected[Condition=="RADICL_mOPC_1FA", list(Count=unique(Count)), by="gene_id"]

esc[, Normalised := Count/librarySize[["RADICL_mESC_1FA"]]*1e6]
opc[, Normalised := Count/librarySize[["RADICL_mOPC_1FA"]]*1e6]

esc.norm = esc[, sum(Normalised), by="gene_id"]
opc.norm = opc[, sum(Normalised), by="gene_id"]

merged = merge(esc.norm, opc.norm, all=TRUE, by="gene_id")
merged[is.na(V1.x), V1.x := min(merged[is.finite(V1.x), V1.x], na.rm=TRUE)]
merged[is.na(V1.y), V1.y := min(merged[is.finite(V1.y), V1.y], na.rm=TRUE)]
merged[, Ratio := log2(V1.x)-log2(V1.y)]
merged[, EDF := rank(Ratio)/length(Ratio)]

fwrite(merged, file=paste0(working.folder, "DNA_Ratio_mESC_over_mOPC.tsv"), sep="\t", row.names=FALSE)

mDNA = merged[gene_id%in%markers[, gene_id],][order(-Ratio), list(gene_id, Ratio)]
mDNA[, Marker := markers[match(mDNA[,gene_id], markers[, gene_id]), Marker]]
fwrite(mDNA, file=paste0(working.folder, "Markers_DNA_Ratio_mESC_over_mOPC.tsv"), sep="\t", row.names=FALSE)

mDNA = merged[gene_id%in%markers[Marker=="mOPC", gene_id],][order(-Ratio),][Ratio>0, list(gene_id, Ratio)]
fwrite(mDNA, file=paste0(working.folder, "Markers_DNA_OPC_higher_in_ESC.tsv"), sep="\t", row.names=FALSE)

selected[, distance := distance(genes[match(selected[, gene_id], genes$gene_id),],
                                genes[match(selected[, RNAid], genes$gene_id),],
                                ignore.strand=TRUE)]
selected[!is.na(distance), distance2 := as.character(cut(distance,
                                                        breaks=c(0, 1e3, 1e4, 1e5, 1e6, Inf),
                                                        labels=c("<1Kb", "1-10Kb", "10-100Kb", "100Kb-1Mb", ">1Mb"),
                                                        include.lowest=TRUE))]
selected[is.na(distance2), distance2 := "Interchromosomal"]
selected[, distance := distance2][, distance2 := NULL]
fwrite(selected[Condition%in%c("RADICL_mESC_1FA", "RADICL_mOPC_1FA"),], file=paste0(working.folder, "DNAinteractions_perGeneRegion_2kb_RNAcounts_1FA_mESC_mOPC_withDistance.tsv"), sep="\t", row.names=FALSE)

gg1 = ggplot(merged, aes(x=Ratio)) + ggtitle("mESC markers (DNA)") + geom_rug(data=markers[Marker=="mESC",], aes(x=Ratio), col="red") + geom_histogram(bins=100) + scale_x_continuous("RADICL mESC/mOPC (normalised interactions)", limits=c(-12, 12)) + theme_bw()
gg2 = ggplot(merged, aes(x=Ratio)) + ggtitle("mOPC markers (DNA)") + geom_rug(data=markers[Marker=="mOPC",], aes(x=Ratio), col="blue") + geom_histogram(bins=100) + scale_x_continuous("RADICL mESC/mOPC (normalised interactions)", limits=c(-12, 12)) + theme_bw()
gg = ggarrange(gg1, gg2, nrow=2)
save_plot(gg, filename=paste0(working.folder, "plots/RNA-BIN_", bin_width, "_cell_type_specific_DNAmarkers_RADICL_ratio.pdf"), base_height=6, base_aspect_ratio=2, ncol=1, nrow=2)

gg = ggplot(merged, aes(x=Ratio, y=EDF)) + ggtitle("DNA markers") + geom_line() + geom_point(data=merged[gene_id%in%markers[Marker=="mESC", gene_id],], aes(x=Ratio, y=EDF), col="red", size=0.75) + geom_point(data=merged[gene_id%in%markers[Marker=="mOPC", gene_id],], aes(x=Ratio, y=EDF), col="blue", size=0.75) + scale_x_continuous("RADICL mESC/mOPC (normalised interactions)", limits=c(-12, 12)) + theme_bw()
save_plot(gg, filename=paste0(working.folder, "plots/RNA-BIN_", bin_width, "_cell_type_specific_DNAmarkers_RADICL_ratio_CDF.pdf"), base_height=6)

CAGE_expr = fread(file.path(working.folder, "mOPC_mESC_promoters_CAGE_TPM.GeneID.txt"),
                  select=c(1,2,6), col.names=c("gene_id", "mESC", "mOPC"))
CAGE_expr[, Ratio := log2(mESC)-log2(mOPC)]
CAGE_expr[, EDF := rank(Ratio)/length(Ratio)]

fwrite(CAGE_expr, file=paste0(working.folder, "CAGE_Ratio_mESC_over_mOPC.tsv"), sep="\t", row.names=FALSE)

mCAGE = CAGE_expr[gene_id%in%markers[, gene_id],][order(-Ratio), list(gene_id, Ratio, EDF)]
mCAGE[, Marker := markers[match(mCAGE[,gene_id], markers[, gene_id]), Marker]]
fwrite(mCAGE, file=paste0(working.folder, "Markers_CAGE_Ratio_mESC_over_mOPC.tsv"), sep="\t", row.names=FALSE)

gg1 = ggplot(CAGE_expr, aes(x=Ratio)) + ggtitle("mESC markers") + geom_rug(data=mCAGE[Marker=="mESC",], aes(x=Ratio), col="red") + geom_histogram(bins=100) + scale_x_continuous("CAGE mESC/mOPC (TPM normalised)", limits=c(-15, 15)) + theme_bw()
gg2 = ggplot(CAGE_expr, aes(x=Ratio)) + ggtitle("mOPC markers") + geom_rug(data=mCAGE[Marker=="mOPC",], aes(x=Ratio), col="blue") + geom_histogram(bins=100) + scale_x_continuous("CAGE mESC/mOPC (TPM normalised)", limits=c(-15, 15)) + theme_bw()
gg = ggarrange(gg1, gg2, nrow=2)
save_plot(gg, filename=paste0(working.folder, "plots/RNA-BIN_", bin_width, "_cell_type_specific_markers_CAGE_ratio.pdf"), base_height=6, base_aspect_ratio=2, ncol=1, nrow=2)

gg = ggplot(CAGE_expr, aes(x=Ratio, y=EDF)) + ggtitle("CAGE markers") + geom_line() + geom_point(data=mCAGE[Marker=="mESC",], aes(x=Ratio, y=EDF), col="red", size=0.75) + geom_point(data=mCAGE[Marker=="mOPC",], aes(x=Ratio, y=EDF), col="blue", size=0.75) + scale_x_continuous("RADICL mESC/mOPC (normalised interactions)", limits=c(-15, 15)) + theme_bw()
save_plot(gg, filename=paste0(working.folder, "plots/RNA-BIN_", bin_width, "_cell_type_specific_markers_CAGE_ratio_CDF.pdf"), base_height=6)
```

### Reproducibility

```{r createCounts, eval=FALSE}
interactions = lapply(unique(sampleTable[, Label]),
                      function(lbl){
                         
                         temp = fread(paste0("gunzip -c ", working.folder, "processed/RNA-BIN_", lbl, "_", gencode_version, "_level_1_2_3_Unique_", bin_width, "_GenicOnly.tsv.gz"))
                         
                         setkeyv(temp, c("gene_id.R", "gene_id.D"))
                         temp = temp[, .N, by=key(temp)]
                         setnames(temp, "N", "Count")
                         
                         temp[, log2Count := log2(Count)]
                         
                         temp  
                      })

names(interactions) = unique(sampleTable[, Label])
interactions = rbindlist(interactions, idcol="Label")

writeGzip(interactions, file=paste0(working.folder, "processed/RNA-BIN_", gencode_version, "_level_1_2_3_Unique_", bin_width, "_AllCounts.tsv"),
          col.names=TRUE, showProgress=TRUE, wait=TRUE, level=9)
```

```{r reproducibility, eval=FALSE}
if( !dir.exists(paste0(working.folder, "plots")) )
   dir.create(paste0(working.folder, "plots"), recursive=TRUE)

interactions = fread(cmd=paste0("gunzip -c ", working.folder, "processed/RNA-BIN_", gencode_version, "_level_1_2_3_Unique_", bin_width, "_AllCounts.tsv.gz"))

countTable = interactions[grepl("Control", Label),]

countTable = dcast.data.table(countTable, gene_id.R+gene_id.D~Label, value.var="Count", fill=0)

countTable = as.matrix(countTable[, 3:ncol(countTable), with=FALSE])

countTable = countTable[rowSums(countTable >= 1) > 1,]

pdf(paste0(working.folder, "plots/RNA-BIN_plotCorrelation_Controls.pdf"), width=16, height=16)
   pl = plotCorrelation2(countTable)
dev.off()

countTable = interactions[grepl("RADICL_mESC", Label) & grepl("FA", Label),]
   
countTable = dcast.data.table(countTable, gene_id.R+gene_id.D~Label, value.var="Count", fill=0)

countTable = as.matrix(countTable[, 3:ncol(countTable), with=FALSE])

countTable = countTable[rowSums(countTable >= 1) > 1,]

pdf(paste0(working.folder, "plots/RNA-BIN_plotCorrelation.pdf"), width=16, height=16)
   pl = plotCorrelation2(countTable)
dev.off()

countTable = interactions[grepl("1FA", Label) | grepl("GRID", Label),]
   
countTable = dcast.data.table(countTable, gene_id.R+gene_id.D~Label, value.var="Count", fill=0)

countTable = as.matrix(countTable[, 3:ncol(countTable), with=FALSE])

countTable = countTable[rowSums(countTable >= 1) > 1,]

pdf(paste0(working.folder, "plots/RNA-BIN_plotCorrelation_RADICL-GRID.pdf"), width=16, height=16)
   pl = plotCorrelation2(countTable)
dev.off()

countTable = interactions[grepl("RADICL_mESC", Label) & (grepl("1FA", Label) | grepl("4h_ActD", Label) | grepl("Control", Label)),]

countTable = dcast.data.table(countTable, gene_id.R+gene_id.D~Label, value.var="Count", fill=0)

countTable = as.matrix(countTable[, 3:ncol(countTable), with=FALSE])

countTable = countTable[rowSums(countTable >= 1) > 1,]

pdf(paste0(working.folder, "plots/RNA-BIN_plotCorrelation_Suppl.pdf"), width=16, height=16)
   pl = plotCorrelation2(countTable)
dev.off()

countTable = interactions[grepl("RADICL_mOPC", Label),]

countTable = dcast.data.table(countTable, gene_id.R+gene_id.D~Label, value.var="Count", fill=0)

countTable = as.matrix(countTable[, 3:ncol(countTable), with=FALSE])

countTable = countTable[rowSums(countTable >= 1) > 1,]

pdf(paste0(working.folder, "plots/RNA-BIN_plotCorrelation_mOPC.pdf"), width=16, height=16)
   pl = plotCorrelation2(countTable)
dev.off()

# countTable = interactions[grepl("RADICL_mESC_Control", Label),][, Replicate := tstrsplit(Label, "_")[[4]]]
# 
# c.max = ceiling(log2(max(countTable[, Count])))
# 
# countTable = dcast.data.table(countTable, gene_id.R+gene_id.D~Replicate, value.var="Count", fill=NA)
# 
# corVal = data.frame(value=cor(countTable[, list(`1`, `2`)], use="complete")[1,2])
# 
# gg1.12 = ggplot(countTable[!(is.na(`1`)|is.na(`2`)),], aes(x=log2(`1`), y=log2(`2`))) +
#    geom_hex(bins=100) +
#    scale_x_continuous("Control Replicate 1 log2(count)", limits=c(-0.1,c.max)) +
#    scale_y_continuous("Control Replicate 2 log2(count)", limits=c(-0.1,c.max)) +
#    geom_text(data=corVal, aes(label=paste("R =", round(value, 3))), x=0, y=c.max, hjust=0, vjust=1, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) +
#    theme_bw() + coord_fixed()
# # corVal = data.frame(value=cor(count[Condition%in%"Control2", list(`1`, `2`)], use="complete")[1,2])
# # gg2.12 = ggplot(count[Condition%in%"Control2" & !(is.na(`1`)|is.na(`2`))], aes(x=log2(`1`), y=log2(`2`))) +
# #    geom_hex(bins=100) +
# #    scale_x_continuous("Control2 Replicate 1 log2(count)", limits=c(-0.1,c.max)) +
# #    scale_y_continuous("Control2 Replicate 2 log2(count)", limits=c(-0.1,c.max)) +
# #    geom_text(data=corVal, aes(label=paste("R =", round(value, 3))), x=0, y=c.max, hjust=0, vjust=1, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) +
# #    theme_bw() + coord_fixed()
# png(paste0(working.folder, "plots/RNA-BIN_", bin_width, "_Control_replicates_reproducibility.png"), units="in", height=10, width=16, res=300)
#    # ggarrange(gg1.12, gg2.12, ncol=2, nrow=1)
#    gg1.12
# dev.off()
# 
# countTable = interactions[grepl("RADICL_mESC_1FA", Label),][, Replicate := tstrsplit(Label, "_")[[4]]]
# 
# c.max = ceiling(log2(max(countTable[, Count])))
# 
# countTable = dcast.data.table(countTable, gene_id.R+gene_id.D~Replicate, value.var="Count", fill=NA)
# 
# corVal = data.frame(value=cor(countTable[, list(`1`, `2`)], use="complete")[1,2])
# gg12 = ggplot(countTable[!(is.na(`1`)|is.na(`2`))], aes(x=log2(`1`), y=log2(`2`))) +
#    geom_hex(bins=100) +
#    scale_x_continuous("Replicate 1 log2(count)", limits=c(-0.1,c.max)) +
#    scale_y_continuous("Replicate 2 log2(count)", limits=c(-0.1,c.max)) +
#    geom_text(data=corVal, aes(label=paste("R =", round(value, 3))), x=0, y=c.max, hjust=0, vjust=1, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) +
#    theme_bw() + coord_fixed()
# corVal = data.frame(value=cor(countTable[, list(`1`, `3`)], use="complete")[1,2])
# gg13 = ggplot(countTable[!(is.na(`1`)|is.na(`3`))], aes(x=log2(`1`), y=log2(`3`))) +
#    geom_hex(bins=100) +
#    scale_x_continuous("Replicate 1 log2(count)", limits=c(-0.1,c.max)) +
#    scale_y_continuous("Replicate 3 log2(count)", limits=c(-0.1,c.max)) +
#    geom_text(data=corVal, aes(label=paste("R =", round(value, 3))), x=0, y=c.max, hjust=0, vjust=1, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) +
#    theme_bw() + coord_fixed()
# corVal = data.frame(value=cor(countTable[, list(`2`, `3`)], use="complete")[1,2])
# gg23 = ggplot(countTable[!(is.na(`2`)|is.na(`3`))], aes(x=log2(`2`), y=log2(`3`))) +
#    geom_hex(bins=100) +
#    scale_x_continuous("Replicate 2 log2(count)", limits=c(-0.1,c.max)) +
#    scale_y_continuous("Replicate 3 log2(count)", limits=c(-0.1,c.max)) +
#    geom_text(data=corVal, aes(label=paste("R =", round(value, 3))), x=0, y=c.max, hjust=0, vjust=1, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) +
#    theme_bw() + coord_fixed()
# 
# png(paste0(working.folder, "plots/RNA-BIN_", bin_width, "_1FA_replicates_reproducibility.png"), units="in", height=5, width=16, res=300)
#    ggarrange(gg12, gg13, gg23, ncol=3, nrow=1)
# dev.off()
# 
# # count = interactions[Condition%in%"2FA",]
# # c.max = ceiling(log2(max(count[, 4:ncol(count), with=FALSE], na.rm=TRUE)))
# # 
# # corVal = data.frame(value=cor(count[, list(`1`, `2`)], use="complete")[1,2])
# # gg12 = ggplot(count[!(is.na(`1`)|is.na(`2`))], aes(x=log2(`1`), y=log2(`2`))) +
# #    geom_hex(bins=100) +
# #    scale_x_continuous("Replicate 1 log2(count)", limits=c(-0.1,c.max)) +
# #    scale_y_continuous("Replicate 2 log2(count)", limits=c(-0.1,c.max)) +
# #    geom_text(data=corVal, aes(label=paste("R =", round(value, 3))), x=0, y=c.max, hjust=0, vjust=1, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) +
# #    theme_bw() + coord_fixed()
# # corVal = data.frame(value=cor(count[, list(`1`, `3`)], use="complete")[1,2])
# # gg13 = ggplot(count[!(is.na(`1`)|is.na(`3`))], aes(x=log2(`1`), y=log2(`3`))) +
# #    geom_hex(bins=100) +
# #    scale_x_continuous("Replicate 1 log2(count)", limits=c(-0.1,c.max)) +
# #    scale_y_continuous("Replicate 3 log2(count)", limits=c(-0.1,c.max)) +
# #    geom_text(data=corVal, aes(label=paste("R =", round(value, 3))), x=0, y=c.max, hjust=0, vjust=1, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) +
# #    theme_bw() + coord_fixed()
# # corVal = data.frame(value=cor(count[, list(`2`, `3`)], use="complete")[1,2])
# # gg23 = ggplot(count[!(is.na(`2`)|is.na(`3`))], aes(x=log2(`2`), y=log2(`3`))) +
# #    geom_hex(bins=100) +
# #    scale_x_continuous("Replicate 2 log2(count)", limits=c(-0.1,c.max)) +
# #    scale_y_continuous("Replicate 3 log2(count)", limits=c(-0.1,c.max)) +
# #    geom_text(data=corVal, aes(label=paste("R =", round(value, 3))), x=0, y=c.max, hjust=0, vjust=1, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) +
# #    theme_bw() + coord_fixed()
# # 
# # png(paste0("plots/RNA-BIN_", bin_width, "_2FA_replicates_reproducibility.png"), units="in", height=5, width=16, res=300)
# #    ggarrange(gg12, gg13, gg23, ncol=3, nrow=1)
# # dev.off()
# 
# countTable = interactions[grepl("RADICL_mESC_4h_ActD", Label),][, Replicate := tstrsplit(Label, "_")[[5]]]
# 
# c.max = ceiling(log2(max(countTable[, Count])))
# 
# countTable = dcast.data.table(countTable, gene_id.R+gene_id.D~Replicate, value.var="Count", fill=NA)
# 
# corVal = data.frame(value=cor(countTable[, list(`1`, `2`)], use="complete")[1,2])
# gg12 = ggplot(countTable[!(is.na(`1`)|is.na(`2`))], aes(x=log2(`1`), y=log2(`2`))) +
#    geom_hex(bins=100) +
#    scale_x_continuous("Replicate 1 log2(count)", limits=c(-0.1,c.max)) +
#    scale_y_continuous("Replicate 2 log2(count)", limits=c(-0.1,c.max)) +
#    geom_text(data=corVal, aes(label=paste("R =", round(value, 3))), x=0, y=c.max, hjust=0, vjust=1, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) +
#    theme_bw() + coord_fixed()
# 
# png(paste0(working.folder, "plots/RNA-BIN_", bin_width, "_4h_ActD_replicates_reproducibility.png"), units="in", height=5, width=6, res=300)
#    ggarrange(gg12, ncol=1, nrow=1)
# dev.off()
# 
# # count = interactions[Condition%in%"6h_ActD",]
# # count[, `3` := NULL]
# # c.max = ceiling(log2(max(count[, 4:ncol(count), with=FALSE], na.rm=TRUE)))
# # 
# # corVal = data.frame(value=cor(count[, list(`1`, `2`)], use="complete")[1,2])
# # gg12 = ggplot(count[!(is.na(`1`)|is.na(`2`))], aes(x=log2(`1`), y=log2(`2`))) +
# #    geom_hex(bins=100) +
# #    scale_x_continuous("Replicate 1 log2(count)", limits=c(-0.1,c.max)) +
# #    scale_y_continuous("Replicate 2 log2(count)", limits=c(-0.1,c.max)) +
# #    geom_text(data=corVal, aes(label=paste("R =", round(value, 3))), x=0, y=c.max, hjust=0, vjust=1, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) +
# #    theme_bw() + coord_fixed()
# # 
# # png(paste0("plots/RNA-BIN_", bin_width, "_6h_ActD_replicates_reproducibility.png"), units="in", height=5, width=6, res=300)
# #    ggarrange(gg12, ncol=1, nrow=1)
# # dev.off()
# 
# count = interactions[, list(Condition, gene_id.R, gene_id.D, Count=rowSums(.SD, na.rm=TRUE)), .SDcols=c("1", "2", "3")]
# count = dcast(count, gene_id.R+gene_id.D~Condition, value.var="Count", fill=NA)
# 
# s.max = ceiling(log2(max(count[, 3:ncol(count), with=FALSE], na.rm=TRUE)))
# 
# # corVal = data.frame(value=cor(count[, list(`Control1`, `Control2`)], use="complete")[1,2])
# # gg12.12 = ggplot(count[!(is.na(`Control1`)|is.na(`Control2`))], aes(x=log2(`Control1`), y=log2(`Control2`))) +
# #    geom_hex(bins=100) +
# #    scale_x_continuous("Control1 log2(count)", limits=c(-0.1,s.max)) +
# #    scale_y_continuous("Control2 log2(count)",     limits=c(-0.1,s.max)) +
# #    geom_text(data=corVal, aes(label=paste("R =", round(value, 3))), x=0, y=s.max, hjust=0, vjust=1, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) +
# #    theme_bw() + coord_fixed()
# corVal = data.frame(value=cor(count[, list(`Control`, `1FA`)], use="complete")[1,2])
# gg12.1 = ggplot(count[!(is.na(`Control`)|is.na(`1FA`))], aes(x=log2(`Control`), y=log2(`1FA`))) +
#    geom_hex(bins=100) +
#    scale_x_continuous("Control log2(count)", limits=c(-0.1,s.max)) +
#    scale_y_continuous("1FA log2(count)",     limits=c(-0.1,s.max)) +
#    geom_text(data=corVal, aes(label=paste("R =", round(value, 3))), x=0, y=s.max, hjust=0, vjust=1, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) +
#    theme_bw() + coord_fixed()
# # corVal = data.frame(value=cor(count[, list(`Control2`, `1FA`)], use="complete")[1,2])
# # gg12.2 = ggplot(count[!(is.na(`Control2`)|is.na(`1FA`))], aes(x=log2(`Control2`), y=log2(`1FA`))) +
# #    geom_hex(bins=100) +
# #    scale_x_continuous("Control2 log2(count)", limits=c(-0.1,s.max)) +
# #    scale_y_continuous("1FA log2(count)",     limits=c(-0.1,s.max)) +
# #    geom_text(data=corVal, aes(label=paste("R =", round(value, 3))), x=0, y=s.max, hjust=0, vjust=1, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) +
# #    theme_bw() + coord_fixed()
# corVal = data.frame(value=cor(count[, list(`Control`, `2FA`)], use="complete")[1,2])
# gg13.1 = ggplot(count[!(is.na(`Control`)|is.na(`2FA`))], aes(x=log2(`Control`), y=log2(`2FA`))) +
#    geom_hex(bins=100) +
#    scale_x_continuous("Control log2(count)", limits=c(-0.1,s.max)) +
#    scale_y_continuous("2FA log2(count)",     limits=c(-0.1,s.max)) +
#    geom_text(data=corVal, aes(label=paste("R =", round(value, 3))), x=0, y=s.max, hjust=0, vjust=1, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) +
#    theme_bw() + coord_fixed()
# # corVal = data.frame(value=cor(count[, list(`Control2`, `2FA`)], use="complete")[1,2])
# # gg13.2 = ggplot(count[!(is.na(`Control2`)|is.na(`2FA`))], aes(x=log2(`Control2`), y=log2(`2FA`))) +
# #    geom_hex(bins=100) +
# #    scale_x_continuous("Control2 log2(count)", limits=c(-0.1,s.max)) +
# #    scale_y_continuous("2FA log2(count)",     limits=c(-0.1,s.max)) +
# #    geom_text(data=corVal, aes(label=paste("R =", round(value, 3))), x=0, y=s.max, hjust=0, vjust=1, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) +
# #    theme_bw() + coord_fixed()
# corVal = data.frame(value=cor(count[, list(`1FA`, `2FA`)], use="complete")[1,2])
# gg23 = ggplot(count[!(is.na(`1FA`)|is.na(`2FA`))], aes(x=log2(`1FA`), y=log2(`2FA`))) +
#    geom_hex(bins=100) +
#    scale_x_continuous("1FA log2(count)", limits=c(-0.1,s.max)) +
#    scale_y_continuous("2FA log2(count)", limits=c(-0.1,s.max)) +
#    geom_text(data=corVal, aes(label=paste("R =", round(value, 3))), x=0, y=s.max, hjust=0, vjust=1, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) +
#    theme_bw() + coord_fixed()
# 
# png(paste0(working.folder, "plots/RNA-BIN_", bin_width, "_Reproducibility.png"), units="in", height=5, width=16, res=300)
#    # ggarrange(gg12.12, gg12.1, gg12.2, gg13.1, gg13.2, gg23, ncol=3, nrow=2)
#    ggarrange(gg12.1, gg13.1, gg23, ncol=3, nrow=)
# dev.off()
# 
# # Cross-reproducibility
# count = interactions[Condition%in%c("1FA", "2FA"),]
# count = dcast.data.table(count, gene_id.R+gene_id.D~Condition, value.var=c("1", "2", "3"))
# s.max = ceiling(log2(max(count[, 3:ncol(count), with=FALSE], na.rm=TRUE)))
# 
# corVal = data.frame(value=cor(count[, list(`1_1FA`, `1_2FA`)], use="complete")[1,2])
# gg12.11 = ggplot(count[!(is.na(`1_1FA`)|is.na(`1_2FA`))], aes(x=log2(`1_1FA`), y=log2(`1_2FA`))) +
#    geom_hex(bins=100) +
#    scale_x_continuous("1FA Replicate 1 log2(count)", limits=c(-0.1,s.max)) +
#    scale_y_continuous("2FA Replicate 1 log2(count)",     limits=c(-0.1,s.max)) +
#    geom_text(data=corVal, aes(label=paste("R =", round(value, 3))), x=0, y=s.max, hjust=0, vjust=1, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) +
#    theme_bw() + coord_fixed()
# corVal = data.frame(value=cor(count[, list(`1_1FA`, `2_2FA`)], use="complete")[1,2])
# gg12.12 = ggplot(count[!(is.na(`1_1FA`)|is.na(`2_2FA`))], aes(x=log2(`1_1FA`), y=log2(`2_2FA`))) +
#    geom_hex(bins=100) +
#    scale_x_continuous("1FA Replicate 1 log2(count)", limits=c(-0.1,s.max)) +
#    scale_y_continuous("2FA Replicate 2 log2(count)",     limits=c(-0.1,s.max)) +
#    geom_text(data=corVal, aes(label=paste("R =", round(value, 3))), x=0, y=s.max, hjust=0, vjust=1, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) +
#    theme_bw() + coord_fixed()
# corVal = data.frame(value=cor(count[, list(`1_1FA`, `3_2FA`)], use="complete")[1,2])
# gg12.13 = ggplot(count[!(is.na(`1_1FA`)|is.na(`3_2FA`))], aes(x=log2(`1_1FA`), y=log2(`3_2FA`))) +
#    geom_hex(bins=100) +
#    scale_x_continuous("1FA Replicate 1 log2(count)", limits=c(-0.1,s.max)) +
#    scale_y_continuous("2FA Replicate 3 log2(count)",     limits=c(-0.1,s.max)) +
#    geom_text(data=corVal, aes(label=paste("R =", round(value, 3))), x=0, y=s.max, hjust=0, vjust=1, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) +
#    theme_bw() + coord_fixed()
# corVal = data.frame(value=cor(count[, list(`2_1FA`, `1_2FA`)], use="complete")[1,2])
# gg12.21 = ggplot(count[!(is.na(`2_1FA`)|is.na(`1_2FA`))], aes(x=log2(`2_1FA`), y=log2(`1_2FA`))) +
#    geom_hex(bins=100) +
#    scale_x_continuous("1FA Replicate 2 log2(count)", limits=c(-0.1,s.max)) +
#    scale_y_continuous("2FA Replicate 1 log2(count)",     limits=c(-0.1,s.max)) +
#    geom_text(data=corVal, aes(label=paste("R =", round(value, 3))), x=0, y=s.max, hjust=0, vjust=1, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) +
#    theme_bw() + coord_fixed()
# corVal = data.frame(value=cor(count[, list(`2_1FA`, `2_2FA`)], use="complete")[1,2])
# gg12.22 = ggplot(count[!(is.na(`2_1FA`)|is.na(`2_2FA`))], aes(x=log2(`2_1FA`), y=log2(`2_2FA`))) +
#    geom_hex(bins=100) +
#    scale_x_continuous("1FA Replicate 2 log2(count)", limits=c(-0.1,s.max)) +
#    scale_y_continuous("2FA Replicate 2 log2(count)",     limits=c(-0.1,s.max)) +
#    geom_text(data=corVal, aes(label=paste("R =", round(value, 3))), x=0, y=s.max, hjust=0, vjust=1, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) +
#    theme_bw() + coord_fixed()
# corVal = data.frame(value=cor(count[, list(`3_1FA`, `2_2FA`)], use="complete")[1,2])
# gg12.23 = ggplot(count[!(is.na(`3_1FA`)|is.na(`2_2FA`))], aes(x=log2(`3_1FA`), y=log2(`2_2FA`))) +
#    geom_hex(bins=100) +
#    scale_x_continuous("1FA Replicate 3 log2(count)", limits=c(-0.1,s.max)) +
#    scale_y_continuous("2FA Replicate 2 log2(count)",     limits=c(-0.1,s.max)) +
#    geom_text(data=corVal, aes(label=paste("R =", round(value, 3))), x=0, y=s.max, hjust=0, vjust=1, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) +
#    theme_bw() + coord_fixed()
# corVal = data.frame(value=cor(count[, list(`3_1FA`, `1_2FA`)], use="complete")[1,2])
# gg12.31 = ggplot(count[!(is.na(`3_1FA`)|is.na(`1_2FA`))], aes(x=log2(`3_1FA`), y=log2(`1_2FA`))) +
#    geom_hex(bins=100) +
#    scale_x_continuous("1FA Replicate 3 log2(count)", limits=c(-0.1,s.max)) +
#    scale_y_continuous("2FA Replicate 1 log2(count)",     limits=c(-0.1,s.max)) +
#    geom_text(data=corVal, aes(label=paste("R =", round(value, 3))), x=0, y=s.max, hjust=0, vjust=1, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) +
#    theme_bw() + coord_fixed()
# corVal = data.frame(value=cor(count[, list(`3_1FA`, `2_2FA`)], use="complete")[1,2])
# gg12.32 = ggplot(count[!(is.na(`3_1FA`)|is.na(`2_2FA`))], aes(x=log2(`3_1FA`), y=log2(`2_2FA`))) +
#    geom_hex(bins=100) +
#    scale_x_continuous("1FA Replicate 3 log2(count)", limits=c(-0.1,s.max)) +
#    scale_y_continuous("2FA Replicate 2 log2(count)",     limits=c(-0.1,s.max)) +
#    geom_text(data=corVal, aes(label=paste("R =", round(value, 3))), x=0, y=s.max, hjust=0, vjust=1, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) +
#    theme_bw() + coord_fixed()
# corVal = data.frame(value=cor(count[, list(`3_1FA`, `3_2FA`)], use="complete")[1,2])
# gg12.33 = ggplot(count[!(is.na(`3_1FA`)|is.na(`3_2FA`))], aes(x=log2(`3_1FA`), y=log2(`3_2FA`))) +
#    geom_hex(bins=100) +
#    scale_x_continuous("1FA Replicate 3 log2(count)", limits=c(-0.1,s.max)) +
#    scale_y_continuous("2FA Replicate 3 log2(count)",     limits=c(-0.1,s.max)) +
#    geom_text(data=corVal, aes(label=paste("R =", round(value, 3))), x=0, y=s.max, hjust=0, vjust=1, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) +
#    theme_bw() + coord_fixed()
# 
# png(paste0(working.folder, "plots/RNA-BIN_", bin_width, "_Cross_reproducibility.png"), units="in", height=16, width=18, res=300)
#    ggarrange(gg12.11, gg12.12, gg12.13, gg12.21, gg12.22, gg12.23, gg12.31, gg12.32, gg12.33, ncol=3, nrow=3)
# dev.off()
```

## Filtering for Giovanni (RepeatMasker)

```{r repeatMasker, eval=FALSE}
for( grp in unique(paste(sampleTable[, Experiment], sampleTable[, Condition], sep="_")) ){
   
   # Skip the iteration if the final file already exists
   if( file.exists(paste0(working.folder, "REPEATS_OUTSIDE_GENCODE/RNA-BIN-repeatMasker_", grp, "_", gencode_version, "_level_1_2_3_Unique_", bin_width, "_significant", fdr.th, "_extended_tsc_noDistance.tsv.gz")) )
      next
   
   print(grp)

   files = list.files(paste0(working.folder, "processed"), pattern=paste0(grp, ".*.gz"), full.names=TRUE)
   files = files[grepl(bin_width, files) & !grepl("GenicOnly", files) & !grepl("significant", files)]
   interactions = rbindlist(lapply(seq_along(files),
                                   function(x){
                                      tmp = fread(paste("gunzip -c", files[[as.numeric(as.character(x))]]))
                                      tmp[, replicate := as.numeric(as.character(x))]
                                      tmp
                                   }))
   setkeyv(interactions, c("chr.R", "start.R", "end.R", "strand.R", "chr.D", "start.D", "end.D", "replicate"))
   
   files.repeats = list.files(paste0(working.folder, "REPEATS_OUTSIDE_GENCODE"), pattern="ID.gz$", full.names=TRUE, recursive=TRUE)
   files.repeats = files.repeats[grepl(tstrsplit(grp, "_")[[2]], files.repeats) & grepl(tstrsplit(grp, "_")[[3]], files.repeats)]
   cnames = c("chr.R", "start.R", "end.R", "strand.R", "chr.D", "start.D", "end.D", "gene_id.D", "gene_id.R")
   
   if( length(files.repeats)==0 )
      next
   
   repeats = rbindlist(lapply(seq_along(files.repeats),
                              function(i){
                                 tmp = fread(paste("gunzip -c", files.repeats[as.numeric(as.character(i))]),
                                             col.names=cnames, select=c(1:3,6,11:14,22))
                                 tmp[, replicate := as.numeric(as.character(i))]
                                 tmp
                              }))
   setkeyv(repeats, key(interactions))
   interactions[repeats, gene_id.R := i.gene_id.R]
   
   # Calculate the count per interaction pair
   setkeyv(interactions, c("gene_id.R", "gene_id.D"))
   subset = interactions[, .N, by=key(interactions)]
   setnames(subset, "N", "Count")
   
   rm(interactions); gc()
   
   frandom=1
   
   # Calculate the frequency per RNA
   setkeyv(subset, "gene_id.R")
   rj = subset[, list(coverage=sum(Count)), by=key(subset)]
   rj[, relative := coverage/sum(coverage)]
   setkeyv(rj, "gene_id.R")
   subset[rj, rj := relative]
   
   # Calculate the frequency per DNA (or BIN)
   setkeyv(subset, "gene_id.D")
   rh = subset[, list(coverage=sum(Count)), by=key(subset)]
   rh[, relative := coverage/sum(coverage)]
   setkeyv(rh, "gene_id.D")
   subset[rh, rh := relative]
   
   # Calculate the total number of interactions (needed for the binomial test)
   subset[, N := sum(Count)]
   
   # Remove the Multi- and Inter-genic
   subset = subset[!(gene_id.R%in%c("Multigenic", "Intergenic") | gene_id.D%in%c("Multigenic", "Intergenic"))]
   
   # Calculate the conditional (and weighted) probability
   subset[, `:=`(pjh = 1*(1/.N), N = sum(Count)), by="gene_id.R"]

   # Perform the binomial test
   setkeyv(subset, c("gene_id.R", "gene_id.D"))
   subset[, `:=`(pval=binom.test(Count, N, p=pjh, alternative="greater")$p.value), by=key(subset)]
   
   # # Perform the correction (Benjamini-Hochberg and Benjamini-Yekutieli) per transcript
   # # Calculate the total number of interacting bins (whether with genic or intergenic regions)
   # unique.bins = length(subset[, unique(gene_id.D)])
   
   setkeyv(subset, "gene_id.R")
   subset[, fdr.tsc := p.adjust(pval, method="BH"), by=key(subset)]
   # # Perform the correction (Benjamini-Hochberg and Benjamini-Yekutieli) per condition
   # # Calculate the total number of interacting bins (whether with genic or intergenic regions)
   # total.inter = as.numeric(length(subset[, unique(gene_id.R)]))*as.numeric(length(subset[, unique(gene_id.D)]))
   # subset[, fdr.cnd := p.adjust(pval, method="BH", n=total.inter)]
   subset[, fdr.cnd := p.adjust(pval, method="BH")]

   # Remove all but the repeats interactions
   subset = subset[!startsWith(gene_id.R, "ENSMUSG"),]

   # Calculate the observed vs expected ratio
   subset[, `:=`(Rjh = log2(Count/(pjh*N)))]
   
   # Remove some columns
   subset[, `:=`(pjh = NULL, N = NULL)]
    
   setkeyv(subset, c("gene_id.R", "gene_id.D"))
   setkeyv(repeats, key(subset))

   output = repeats[subset[, list(gene_id.R, gene_id.D, Pvalue=pval, FDR=fdr.tsc)], nomatch=0]
   
   writeGzip(output, file=paste0(working.folder, "REPEATS_OUTSIDE_GENCODE/RNA-BIN-repeatMasker_", grp, "_", gencode_version, "_level_1_2_3_Unique_", bin_width, "_significant", fdr.th, "_extended_tsc_noDistance.tsv"),
             col.names=TRUE, showProgress=TRUE, wait=FALSE, level=9)
   
   rm(output); gc()
   
   output = repeats[subset[, list(gene_id.R, gene_id.D, Pvalue=pval, FDR=fdr.cnd)], nomatch=0]

   writeGzip(output, file=paste0(working.folder, "REPEATS_OUTSIDE_GENCODE/RNA-BIN-repeatMasker_", grp, "_", gencode_version, "_level_1_2_3_Unique_", bin_width, "_significant", fdr.th, "_extended_cnd_noDistance.tsv"),
             col.names=TRUE, showProgress=TRUE, wait=FALSE, level=9)

   rm(repeats, subset, output); gc()
   
}
```

## Enhancers

### Check on directionality

```{r old, eval=FALSE}
### Convergent Enhancers ###
convergent = fread(paste0(working.folder, "mouse_enhancers.midpoint.mm10.bed"), col.names=c("chr", "start", "end", "name"))
convergent = with(convergent, GRanges(chr, IRanges(start, end), "*", name))
convergent.name = convergent$name
convergent = list(flank(convergent, width=1e3, start=TRUE, both=FALSE),
                  resize(convergent, width=1e3, fix="start"))
strand(convergent[[1]]) = "+"
strand(convergent[[2]]) = "-"
convergent[[1]]$name = paste("EnhancerF", seq_along(convergent[[1]]), sep="_")
convergent[[2]]$name = paste("EnhancerR", seq_along(convergent[[2]]), sep="_")
convergent[[1]]$old_name = convergent.name
convergent[[2]]$old_name = convergent.name
convergent = do.call(c, convergent)
# Remove enhancers overlapping with annotated genes (or NASTs)
convergent = convergent[!overlapsAny(convergent, tx.regions),]
# Remove overlapping enhancers
convergent = convergent[-as.data.table(findOverlaps(convergent))[, .N, queryHits][N>1, queryHits],]
### END ###

enhancer.list = list(Diverging=enhancer, Converging=convergent)

# Check only with "RADICL_mESC_1FA_1"
interactions = fread(paste0("gunzip -c ", working.folder, "processed/RNA-BIN_RADICL_mESC_1FA_1_", gencode_version, "_level_1_2_3_Unique_", bin_width, ".tsv.gz"))
interactions.gr = with(interactions, GRanges(chr.R, IRanges(start.R, end.R), strand.R, annotation.R))

enhancer.count = lapply(enhancer.list,
                        function(x){
                              data.table(Name=x$name, Count=countOverlaps(x, interactions.gr))
                           })
enhancer.count = rbindlist(enhancer.count, idcol="Orientation")[, Name := ifelse(Name%like%"EnhancerF", "Forward", "Reverse")]

gg1 = ggplot(enhancer.count, aes(x=log2(Count+1), fill=paste(Orientation, Name))) +
   geom_histogram(position="dodge", bins=20, alpha=0.4, col="black") + scale_fill_brewer("", palette="Paired") + theme_minimal()
gg2 = ggplot(enhancer.count, aes(x=log2(Count), fill=paste(Orientation, Name))) +
   geom_histogram(position="dodge", bins=20, alpha=0.4, col="black") + scale_fill_brewer("", palette="Paired") + theme_minimal()

ggl1 = ggarrange(gg1, gg2, ncol=2)

# Check only with "RADICL_mESC_1FA_1" - Nascent removed
# interactions.gr = with(interactions[!(chr.R==chr.D & abs(start.R-start.D)<2e3)], GRanges(chr.R, IRanges(start.R, end.R), strand.R, annotation.R))
interactions.gr = with(interactions[!(chr.R==chr.D)], GRanges(chr.R, IRanges(start.R, end.R), strand.R, annotation.R))

enhancer.count = lapply(enhancer.list,
                        function(x){
                              data.table(Name=x$name, Count=countOverlaps(x, interactions.gr))
                           })
enhancer.count = rbindlist(enhancer.count, idcol="Orientation")[, Name := ifelse(Name%like%"EnhancerF", "Forward", "Reverse")]

gg1 = ggplot(enhancer.count, aes(x=log2(Count+1), fill=paste(Orientation, Name))) +
   geom_histogram(position="dodge", bins=20, alpha=0.4, col="black") + scale_fill_brewer("", palette="Paired") + theme_minimal()
gg2 = ggplot(enhancer.count, aes(x=log2(Count), fill=paste(Orientation, Name))) +
   geom_histogram(position="dodge", bins=20, alpha=0.4, col="black") + scale_fill_brewer("", palette="Paired") + theme_minimal()

ggl2 = ggarrange(gg1, gg2, ncol=2)
```

```{r new, eval=FALSE}
### Convergent Enhancers ###
convergent = fread(paste0(working.folder, "F5.mm10.enhancers.bed"), select=c(1,4,7), col.names=c("chr", "name", "start"))
convergent = with(convergent, GRanges(chr, IRanges(start, width=1), "*", name))
convergent.name = convergent$name
convergent = list(flank(convergent, width=1e3, start=TRUE, both=FALSE),
                  resize(convergent, width=1e3, fix="start"))
strand(convergent[[1]]) = "+"
strand(convergent[[2]]) = "-"
convergent[[1]]$name = paste("EnhancerF", seq_along(convergent[[1]]), sep="_")
convergent[[2]]$name = paste("EnhancerR", seq_along(convergent[[2]]), sep="_")
convergent[[1]]$old_name = convergent.name
convergent[[2]]$old_name = convergent.name
convergent = do.call(c, convergent)
# Remove enhancers overlapping with annotated genes (or NASTs)
convergent = convergent[!overlapsAny(convergent, tx.regions),]
# Remove overlapping enhancers
convergent = convergent[-as.data.table(findOverlaps(convergent))[, .N, queryHits][N>1, queryHits],]
### END ###

enhancer.list = list(Diverging=enhancer, Converging=convergent)

# Check only with "RADICL_mESC_1FA_1"
interactions = fread(paste0("gunzip -c ", working.folder, "processed/RNA-BIN_RADICL_mESC_1FA_1_", gencode_version, "_level_1_2_3_Unique_", bin_width, ".tsv.gz"))
interactions.gr = with(interactions, GRanges(chr.R, IRanges(start.R, end.R), strand.R, annotation.R))

enhancer.count = lapply(enhancer.list,
                        function(x){
                              data.table(Name=x$name, Count=countOverlaps(x, interactions.gr))
                           })
enhancer.count = rbindlist(enhancer.count, idcol="Orientation")[, Name := ifelse(Name%like%"EnhancerF", "Forward", "Reverse")]

gg1 = ggplot(enhancer.count, aes(x=log2(Count+1), fill=paste(Orientation, Name))) +
   geom_histogram(position="dodge", bins=20, alpha=0.4, col="black") + scale_fill_brewer("", palette="Paired") + theme_minimal()
gg2 = ggplot(enhancer.count, aes(x=log2(Count), fill=paste(Orientation, Name))) +
   geom_histogram(position="dodge", bins=20, alpha=0.4, col="black") + scale_fill_brewer("", palette="Paired") + theme_minimal()

ggl1 = ggarrange(gg1, gg2, ncol=2)

# Check only with "RADICL_mESC_1FA_1" - Nascent removed
# interactions.gr = with(interactions[!(chr.R==chr.D & abs(start.R-start.D)<2e3)], GRanges(chr.R, IRanges(start.R, end.R), strand.R, annotation.R))
interactions.gr = with(interactions[!(chr.R==chr.D)], GRanges(chr.R, IRanges(start.R, end.R), strand.R, annotation.R))

enhancer.count = lapply(enhancer.list,
                        function(x){
                              data.table(Name=x$name, Count=countOverlaps(x, interactions.gr))
                           })
enhancer.count = rbindlist(enhancer.count, idcol="Orientation")[, Name := ifelse(Name%like%"EnhancerF", "Forward", "Reverse")]

gg1 = ggplot(enhancer.count, aes(x=log2(Count+1), fill=paste(Orientation, Name))) +
   geom_histogram(position="dodge", bins=20, alpha=0.4, col="black") + scale_fill_brewer("", palette="Paired") + theme_minimal()
gg2 = ggplot(enhancer.count, aes(x=log2(Count), fill=paste(Orientation, Name))) +
   geom_histogram(position="dodge", bins=20, alpha=0.4, col="black") + scale_fill_brewer("", palette="Paired") + theme_minimal()

ggl2 = ggarrange(gg1, gg2, ncol=2)
```

### Filtering for Erik (Enhancers)

```{r enhancer, eval=FALSE}
enhancer.folder = "Enhancers_F5"
if( !dir.exists(paste0(working.folder, enhancer.folder)) )
   dir.create(paste0(working.folder, enhancer.folder))

fwrite(as.data.table(enhancer), file=paste0(working.folder, enhancer.folder, "/Enhancers_divergent_reference.tsv"), sep="\t", row.names=FALSE)

for( lbl in unique(sampleTable[, Label]) ){

   if( file.exists(paste0(working.folder, enhancer.folder, "/", lbl, ".tsv.gz")) )
      next
   
   interactions = fread(paste0("gunzip -c ", working.folder, "processed/RNA-BIN_", lbl, "_", gencode_version, "_level_1_2_3_Unique_", bin_width, ".tsv.gz"))
   interactions = interactions[gene_id.R%in%"Intergenic",]
   temp = with(interactions, GRanges(chr.R, IRanges(start.R, end.R), strand.R))
   temp = as.data.table(findOverlaps(temp, enhancer))
   interactions[temp[, queryHits], `:=`(gene_id.R = enhancer[temp[, subjectHits],]$name, annotation.R = "enhancer", gene_type.R = "enhancer", region.R = "enhancer")]
   interactions = interactions[!gene_id.R%in%"Intergenic",]

      # Write all interactions
   writeGzip(interactions,
             file=paste0(working.folder, enhancer.folder, "/", lbl, ".tsv"),
             col.names=TRUE, showProgress=TRUE, wait=TRUE, level=9)
}
   
for( grp in unique(paste(sampleTable[, Experiment], sampleTable[, Condition], sep="_")) ){

   print(grp)
   
   files = list.files(paste0(working.folder, "processed"), pattern=paste0(grp, ".*.gz"), full.names=TRUE)
   files = files[grepl(bin_width, files) & !grepl("GenicOnly", files) & !grepl("significant", files)]
   
   print(files)
   
   files.enhancers = list.files(paste0(working.folder, enhancer.folder), pattern="gz$", full.names=TRUE)
   files.enhancers = files.enhancers[grepl(tstrsplit(grp, "_")[[2]], basename(files.enhancers)) & grepl(tstrsplit(grp, "_")[[3]], basename(files.enhancers)) & !grepl("significant", basename(files.enhancers))]
   cnames = c("chr.R", "start.R", "end.R", "strand.R", "gene_id.R", "annotation.R", "gene_type.R", "region.R", "chr.D", "start.D", "end.D", "gene_id.D")
   
   print(files.enhancers)
   
   if( length(files.enhancers)==0 )
      next
   
   # Skip the iteration if the final file already exists
   if( file.exists(paste0(working.folder, enhancer.folder, "/RNA-BIN-enhancers_", grp, "_", gencode_version, "_level_1_2_3_Unique_", bin_width, "_significant_extended_tsc_noDistance.tsv.gz")) )
      next
   
   interactions = rbindlist(lapply(seq_along(files),
                                   function(x){
                                      tmp = fread(paste("gunzip -c", files[[as.numeric(as.character(x))]]))
                                      tmp[, replicate := as.numeric(as.character(x))]
                                      tmp
                                   }))
   setkeyv(interactions, c("chr.R", "start.R", "end.R", "strand.R", "chr.D", "start.D", "end.D", "replicate"))
   
   enhancer = rbindlist(lapply(seq_along(files.enhancers),
                               function(i){
                                  tmp = fread(paste("gunzip -c", files.enhancers[as.numeric(as.character(i))]),
                                              col.names=cnames)
                                  tmp[, replicate := as.numeric(as.character(i))]
                                  tmp
                               }))
   setkeyv(enhancer, key(interactions))
   interactions[enhancer, gene_id.R := i.gene_id.R]
   
   # Calculate the count per interaction pair
   setkeyv(interactions, c("gene_id.R", "gene_id.D"))
   subset = interactions[, .N, by=key(interactions)]
   setnames(subset, "N", "Count")
   
   rm(interactions); gc()
   
   frandom=1
   
   # Calculate the frequency per RNA
   setkeyv(subset, "gene_id.R")
   rj = subset[, list(coverage=sum(Count)), by=key(subset)]
   rj[, relative := coverage/sum(coverage)]
   setkeyv(rj, "gene_id.R")
   subset[rj, rj := relative]
   
   # Calculate the frequency per DNA (or BIN)
   setkeyv(subset, "gene_id.D")
   rh = subset[, list(coverage=sum(Count)), by=key(subset)]
   rh[, relative := coverage/sum(coverage)]
   setkeyv(rh, "gene_id.D")
   subset[rh, rh := relative]
   
   # Calculate the total number of interactions (needed for the binomial test)
   subset[, N := sum(Count)]
   
   # Remove the Multi- and Inter-genic
   subset = subset[!(gene_id.R%in%c("Multigenic", "Intergenic") | gene_id.D%in%c("Multigenic", "Intergenic"))]
   
   # Calculate the conditional (and weighted) probability
   subset[, `:=`(pjh = 1*(1/.N), N = sum(Count)), by="gene_id.R"]

   # Perform the binomial test
   setkeyv(subset, c("gene_id.R", "gene_id.D"))
   subset[, `:=`(pval=binom.test(Count, N, p=pjh, alternative="greater")$p.value), by=key(subset)]
   
   # # Perform the correction (Benjamini-Hochberg and Benjamini-Yekutieli) per transcript
   # # Calculate the total number of interacting bins (whether with genic or intergenic regions)
   # unique.bins = length(subset[, unique(gene_id.D)])
   
   setkeyv(subset, "gene_id.R")
   subset[, fdr.tsc := p.adjust(pval, method="BH"), by=key(subset)]
   # # Perform the correction (Benjamini-Hochberg and Benjamini-Yekutieli) per condition
   # # Calculate the total number of interacting bins (whether with genic or intergenic regions)
   # total.inter = as.numeric(length(subset[, unique(gene_id.R)]))*as.numeric(length(subset[, unique(gene_id.D)]))
   # subset[, fdr.cnd := p.adjust(pval, method="BH", n=total.inter)]
   subset[, fdr.cnd := p.adjust(pval, method="BH")]

   # Remove all but the enhancer interactions
   subset = subset[startsWith(gene_id.R, "Enhancer"),]

   # Calculate the observed vs expected ratio
   subset[, `:=`(Rjh = log2(Count/(pjh*N)))]
   
   # Remove some columns
   subset[, `:=`(pjh = NULL, N = NULL)]
    
   setkeyv(subset, c("gene_id.R", "gene_id.D"))
   setkeyv(enhancer, key(subset))

   output = enhancer[subset[, list(gene_id.R, gene_id.D, Pvalue=pval, FDR=fdr.tsc)], nomatch=0]
   
   writeGzip(output, file=paste0(working.folder, enhancer.folder, "/RNA-BIN-enhancers_", grp, "_", gencode_version, "_level_1_2_3_Unique_", bin_width, "_significant_extended_tsc_noDistance.tsv"),
             col.names=TRUE, showProgress=TRUE, wait=FALSE, level=9)
   
   rm(output); gc()
   
   output = enhancer[subset[, list(gene_id.R, gene_id.D, Pvalue=pval, FDR=fdr.cnd)], nomatch=0]

   writeGzip(output, file=paste0(working.folder, enhancer.folder, "/RNA-BIN-enhancers_", grp, "_", gencode_version, "_level_1_2_3_Unique_", bin_width, "_significant_extended_cnd_noDistance.tsv"),
             col.names=TRUE, showProgress=TRUE, wait=FALSE, level=9)

   rm(enhancer, subset, output); gc()
   
}
```

```{r, statsDatasets, eval=FALSE}
for( grp in unique(gsub(" ", "_", do.call(paste, tstrsplit(sampleTable[, Label], "_")[1:3]))) ){
   
   print(grp)
   
   ##### Raw #####
   interactions = lapply(unique(sampleTable[grepl(grp, Label), Label]),
                         function(lbl){
                            temp = fread(cmd=paste0("gunzip -c ", working.folder, "processed/RNA-BIN_", lbl, "_", gencode_version, "_level_1_2_3_Unique_", bin_width, "_GenicOnly.tsv.gz"))
                            setkeyv(temp, c("chr.R", "gene_id.R", "chr.D", "gene_id.D", "annotation.R", "gene_type.R"))
                            temp = temp[, .N, by=key(temp)]
                            setnames(temp, "N", "Count")
                            temp[, c("Condition", "Replicate") := tstrsplit(lbl, "_")[3:4]]
                            temp
                         })
   
   interactions = rbindlist(interactions)
   
   setkeyv(interactions, c("Condition", "chr.R", "gene_id.R", "chr.D", "gene_id.D"))
   subset = interactions[, list(Count=sum(Count)), by=key(interactions)]
   
   stats1 = subset[, list(Unique_Interactions=sum(Count), Cis=nrow(.SD[chr.R==chr.D,])/sum(Count)*100, Trans=nrow(.SD[chr.R!=chr.D,])/sum(Count)*100), by="Condition"]
   
   fwrite(stats1, file=paste0(working.folder, "stats/RNA-BIN_", grp, "_CisTransUniqueInteractions_", gencode_version, "_level_1_2_3_Unique_", bin_width, ".tsv"), quote=FALSE, sep='\t', row.names=FALSE)
   
   setkeyv(interactions, c("Condition", "Replicate", "chr.R", "gene_id.R", "chr.D", "gene_id.D", "annotation.R", "gene_type.R"))
   interactions = interactions[, list(Count=sum(Count)), by=key(interactions)]
   
   setkeyv(interactions, c("chr.R", "gene_id.R", "chr.D", "gene_id.D", "annotation.R", "gene_type.R"))
   subset = interactions[, list(Count=sum(Count)), by=c("Condition", key(interactions))]
   
   ##### Significant #####
   files.sig = list.files(paste0(working.folder, "processed"), pattern="*_cnd_*", full.names=TRUE)
   files.sig = files.sig[grepl(grp, files.sig)]
   
   significant = rbindlist(lapply(files.sig,
                                  function(x)
                                     fread(cmd=paste("gunzip -c", x), showProgress=FALSE)))
   setnames(significant, "replicate", "Replicate")
   # significant = fread(paste0("gunzip -c RNA-BIN_counts_", gencode_version, "_level_1_2_3_AllOverlapsUnique_", bin_width, "_significant", fdr.th, "_extended_tsc_noDistance.tsv.gz"), showProgress=FALSE)
   
   setkeyv(subset, c("chr.R", "gene_id.R", "chr.D", "gene_id.D", "annotation.R", "gene_type.R"))
   setkeyv(significant, key(subset))
   subset[, Set := factor("Raw", levels=c("Raw", "Significant"))][significant, Set := "Significant"]
   
   stats2 = subset[, list(significant=length(unique(.SD[Set%in%"Significant", gene_id.R])), total=length(unique(gene_id.R))), by=c("Condition", "annotation.R")] 
   stats2 = stats2[, Percentage := round(significant/total*100, 2)][order(-Percentage)]
   
   fwrite(stats2, file=paste0(working.folder, "stats/RNA-BIN_", grp, "_TranscriptBiotypesSignificant", fdr.th, "_", gencode_version, "_level_1_2_3_Unique_", bin_width, ".tsv"), quote=FALSE, sep='\t', row.names=FALSE)
   
   subset[, Bin := findInterval(log2(Count), seq(min(log2(Count)), round(max(log2(Count))), 1), left.open=TRUE)]
   
   gg = ggplot(subset[, list(Count=sum(Count)), by=c("Set", "Bin")], aes(x=Bin, y=Count, fill=Set)) +
      ggtitle(paste("Interactions distribution for", grp), subtitle="Raw (blue) and significant (red) interactions") +
      geom_bar(stat="identity", position="stack", col="black", alpha=0.75) +
      scale_x_continuous("Interactions count per RNA-DNA pair (log2)") +
      scale_fill_brewer("Dataset", palette="Set1") +
      theme_bw() + facet_zoom(y = Set%in%"Significant") +
      theme(legend.position=c(0.9, 0.9), legend.box.background = element_rect(),
            legend.title=element_text(size=12)) 
   ggsave(gg, filename=paste0(working.folder, "plots/Figure3A_RNA-BIN_", grp, ".pdf"), width=12, height=6)
   
   tab = interactions[, list(Intra=sum(.SD[chr.R==chr.D, Count]), Total=sum(Count)), by=c("Condition", "Replicate")][, Perc := Intra/Total*100]
   fwrite(tab, file=paste0(working.folder, "stats/RNA-BIN_", grp, "_CisTransInteractions_", gencode_version, "_level_1_2_3_Unique_", bin_width, ".tsv"), quote=FALSE, sep='\t', row.names=FALSE)
   tab = interactions[, list(Intra=sum(.SD[chr.R==chr.D, Count]), Total=sum(Count)), by=c("Condition")][, Perc := Intra/Total*100]
   fwrite(tab, file=paste0(working.folder, "stats/RNA-BIN_", grp, "_CisTransInteractions_", gencode_version, "_level_1_2_3_Unique_Condition_", bin_width, ".tsv"), quote=FALSE, sep='\t', row.names=FALSE)
   
   setkeyv(significant, c("Condition", "Replicate", "chr.R", "gene_id.R", "chr.D", "gene_id.D", "annotation.R", "gene_type.R"))
   significant = significant[, .N, by=key(significant)]
   setnames(significant, "N", "Count")
   
   tab2 = significant[, list(Intra=sum(.SD[chr.R==chr.D, Count]), Total=sum(Count)), by=c("Condition", "Replicate")][, Perc := Intra/Total*100]
   fwrite(tab2, file=paste0(working.folder, "stats/RNA-BIN_", grp, "_CisTransInteractions_", gencode_version, "_level_1_2_3_Unique_", bin_width, "_significant.tsv"), quote=FALSE, sep='\t', row.names=FALSE)
   tab2 = significant[, list(Intra=sum(.SD[chr.R==chr.D, Count]), Total=sum(Count)), by=c("Condition")][, Perc := Intra/Total*100]
   fwrite(tab2, file=paste0(working.folder, "stats/RNA-BIN_", grp, "_CisTransInteractions_", gencode_version, "_level_1_2_3_Unique_Condition_", bin_width, "_significant.tsv"), quote=FALSE, sep='\t', row.names=FALSE)
   
   setkeyv(significant, c("Condition", "gene_id.R", "annotation.R", "gene_type.R"))
   
   tab3 = significant[, list(binNum=length(unique(gene_id.D)), binCount=sum(Count)), by=key(significant)][order(-binNum)]
   fwrite(tab3, file=paste0(working.folder, "stats/RNA-BIN_", grp, "_TranscriptInteractionsBinNumber_", gencode_version, "_level_1_2_3_Unique_", bin_width, "_significant.tsv"), quote=FALSE, sep='\t', row.names=FALSE)
   
   tab4 = significant[, list(binNum=length(unique(gene_id.D)), binCount=sum(Count)), by=key(significant)][order(-binCount)]
   fwrite(tab4, file=paste0(working.folder, "stats/RNA-BIN_", grp, "_TranscriptInteractionsBinCount_", gencode_version, "_level_1_2_3_Unique_", bin_width, "_significant.tsv"), quote=FALSE, sep='\t', row.names=FALSE)
   
   rm(interactions)
   rm(subset)
   rm(significant)
   gc()
   
}
```

### Intronic Rate

```{r calculateExonIntron, eval=FALSE}
exon_intron_width = tx.regions.dt[annotation%in%c("protein_coding", "long_ncRNA") & type%in%c("exon", "intron", "CDS", "five_prime_UTR", "three_prime_UTR"),]
exon_intron_width = exon_intron_width[, type := ifelse(type%in%"intron", "intron", "exon")][, list(width = sum(width)), by=.(gene_id, type)]
# Select only genes with at least one intron
exon_intron_width = exon_intron_width[, list(type, width, N=.N), by=.(gene_id)][N==2]
setkeyv(exon_intron_width, c("gene_id", "type"))


##### Significant #####
files.sig = list.files(paste0(working.folder, "processed"), pattern="*_cnd_*", full.names=TRUE)

# interactions = lapply(unique(sampleTable[, Label]),
#                       function(lbl){
#                          
#                          temp = fread(paste0("gunzip -c ", working.folder, "processed/RNA-BIN_", lbl, "_", gencode_version, "_level_1_2_3_Unique_", bin_width, "_GenicOnly.tsv.gz"))
#                          
#                          temp = temp[annotation.R%in%c("protein_coding", "long_ncRNA") & region.R%in%c("exon", "intron", "CDS", "five_prime_UTR", "three_prime_UTR"),]
#                          
#                          temp[, region.R := ifelse(region.R%in%"intron", "intron", "exon")]
#                          setkeyv(temp, c("gene_id.R", "region.R"))
#                          
#                          temp = temp[exon_intron_width, width := width][!is.na(width),]
#                          
#                          temp[, mindist := abs(start.R-start.D)]
#                          temp[, distance := as.character(cut(mindist,
#                                                              breaks=c(0, 1e3, 1e4, 1e5, 1e6, Inf),
#                                                              labels=c("<1Kb", "1-10Kb", "10-100Kb", "100Kb-1Mb", ">1Mb"),
#                                                              include.lowest=TRUE))]
#                          
#                          temp[gene_id.R==gene_id.D, distance := "Self-interaction"]
#                          temp[chr.R!=chr.D, distance := "Interchromosome"]
#                          temp[, distance := factor(distance,
#                                                    levels=c("Self-interaction", "<1Kb", "1-10Kb", "10-100Kb", "100Kb-1Mb", ">1Mb", "Interchromosome"))]
#                          temp[, c("Cell", "Condition", "Replicate") := tstrsplit(gsub("4h_ActD", "4hActD", lbl), "_")[c(2:4)]]
#                          temp[, Label := lbl]
#                          setkeyv(temp, c("Label", "Cell", "Condition", "Replicate", "annotation.R", "gene_id.R", "region.R", "distance"))
#                          temp[, .N, by=key(temp)]
#                       })
# interactions = rbindlist(interactions)
#
# saveRDS(interactions, paste0(working.folder, "RData/RNA-DNA_interactions_", gencode_version, "_level_1_2_3_Unique_ExonsIntrons.rds"))

significant = rbindlist(lapply(files.sig,
                               function(x)
                                  fread(paste("gunzip -c", x), showProgress=FALSE)))
setnames(significant, "replicate", "Replicate")

significant = significant[annotation.R%in%c("protein_coding", "long_ncRNA") & region.R%in%c("exon", "intron", "CDS", "five_prime_UTR", "three_prime_UTR"),]
                         
significant[, region.R := ifelse(region.R%in%"intron", "intron", "exon")]
setkeyv(significant, c("gene_id.R", "region.R"))

significant = significant[exon_intron_width, width := width][!is.na(width),]
                         
significant[, mindist := abs(start.R-start.D)]
significant[, distance := as.character(cut(mindist,
                                           breaks=c(0, 1e3, 1e4, 1e5, 1e6, Inf),
                                           labels=c("<1Kb", "1-10Kb", "10-100Kb", "100Kb-1Mb", ">1Mb"),
                                           include.lowest=TRUE))]

significant[gene_id.R==gene_id.D, distance := "Self-interaction"]
significant[chr.R!=chr.D, distance := "Interchromosome"]
significant[, distance := factor(distance,
                                 levels=c("Self-interaction", "<1Kb", "1-10Kb", "10-100Kb", "100Kb-1Mb", ">1Mb", "Interchromosome"))]

setnames(significant, "Condition", "Label")
significant[, c("Cell", "Condition") := tstrsplit(gsub("4h_ActD", "4hActD", Label), "_")[c(2:3)]]
setkeyv(significant, c("Label", "Cell", "Condition", "Replicate", "annotation.R", "gene_id.R", "region.R", "distance"))
significant = significant[, .N, by=key(significant)]

saveRDS(significant, paste0(working.folder, "RData/RNA-DNA_interactions_", gencode_version, "_level_1_2_3_Unique_ExonsIntrons.rds"))
```

```{r plotExonIntronPerc, eval=FALSE}
plotdata = readRDS(paste0(working.folder, "RData/RNA-DNA_interactions_", gencode_version, "_level_1_2_3_Unique_ExonsIntrons.rds"))

# Pseudo-count for missing exonic/intronic reads
plotdata = dcast.data.table(plotdata, Cell+Condition+gene_id.R+annotation.R+distance~region.R, value.var="N",
                            fun.aggregate="sum", fill=0)

plotdata = melt.data.table(plotdata, id.vars=c("Cell", "Condition", "gene_id.R", "annotation.R", "distance"),
                           measure.vars=c("exon", "intron"), variable.name="gene_feature", value.name="N")

plotdata[, Perc := N/sum(N), by=c("Cell", "Condition", "annotation.R", "gene_id.R", "gene_feature")]

plotdata = plotdata[!is.na(Perc), list(Perc = mean(Perc)), by=c("Cell", "Condition", "annotation.R", "distance", "gene_feature")]

gg = ggplot(plotdata, aes(x=distance, y=Perc, fill=gene_feature)) +
   geom_bar(stat="identity", position="dodge", colour="black") +
      ggtitle("RNA processing rate (exon vs inton ~ distance)",
           subtitle="Reads are sum and normalised for each genic feature and averaged per interaction and then per gene.") +
   scale_fill_manual("Genic region", values=c("red3", "dodgerblue3")) +
   scale_y_continuous("Average fraction of reads", labels=percent) +
   scale_x_discrete("") +
   theme_bw() +
   theme(axis.text.x=element_text(angle=60, hjust=1, vjust=1, size=14),
         axis.text.y=element_text(size=14), legend.text=element_text(size=14),
         axis.title.y=element_text(size=14), legend.title=element_text(size=14),
         strip.text.x=element_text(size=14), strip.text.y=element_text(size=14),
         legend.position="bottom") +
   guides(fill = guide_legend(ncol = 2)) + facet_wrap(paste(Cell, Condition)~annotation.R, ncol=2, drop=FALSE)
ggsave(gg, filename=paste0(working.folder, "plots/RNA-DNA_processing_rate_per_region.pdf"), units="in", height=21, width=8)
```

```{r plotExonIntron, eval=FALSE}
plotdata = readRDS(paste0(working.folder, "RData/RNA-DNA_interactions_", gencode_version, "_level_1_2_3_Unique_ExonsIntrons.rds"))

# Pseudo-count for missing exonic/intronic reads
plotdata = dcast.data.table(plotdata, Cell+Condition+Replicate+gene_id.R+annotation.R+distance~region.R, value.var="N",
                            fun.aggregate="sum", fill=1)

plotdata = melt.data.table(plotdata, id.vars=c("Cell", "Condition", "Replicate", "gene_id.R", "annotation.R", "distance"),
                           measure.vars=c("exon", "intron"), variable.name="gene_feature", value.name="N")

if(!exists("exon_intron_width")){
   exon_intron_width = tx.regions.dt[annotation%in%c("protein_coding", "long_ncRNA") & type%in%c("exon", "intron", "CDS", "five_prime_UTR", "three_prime_UTR"),]
   exon_intron_width = exon_intron_width[, type := ifelse(type%in%"intron", "intron", "exon")][, list(width = sum(width)), by=.(gene_id, type)]
   # Select only genes with at least one intron
   exon_intron_width = exon_intron_width[, list(type, width, N=.N), by=.(gene_id)][N==2]
   setkeyv(exon_intron_width, c("gene_id", "type"))
}

# Add the exonic and intronic total width per RNA gene
setkeyv(plotdata, c("gene_id.R", "gene_feature"))
plotdata = plotdata[exon_intron_width, width := as.numeric(width)]

plotdata[, K := N/width*1e3]

# Assign the facto levels
plotdata[, `:=`(gene_feature = factor(gene_feature, levels=c("exon", "intron")),
                gene_type = factor(annotation.R, levels=c("protein_coding", "long_ncRNA")))]

# Collapse the replicates by condition
plotdata = plotdata[, list(mK=mean(K)), by=c("Cell", "Condition", "gene_type", "gene_id.R", "gene_feature", "distance")]
 
plotdata2 = copy(plotdata)
plotdata2[distance%in%c("<1Kb"), distance := "1-10Kb"][distance%in%"1-10Kb", distance := "<10Kb"][distance%in%"Self-interaction", distance := "Gene body"]
plotdata2[, distance := factor(distance, levels=c("Gene body", "<10Kb", "10-100Kb", "100Kb-1Mb", ">1Mb", "Interchromosome"))]
# plotdata2[distance%in%c("<1Kb", "1-10Kb"), distance := "Local interactions"][distance%in%"Self-interaction", distance := "Local interactions"]
# plotdata2[, distance := factor(distance, levels=c("Local interactions", "10-100Kb", "100Kb-1Mb", ">1Mb", "Interchromosome"))]
plotdata2 = dcast.data.table(plotdata2, Cell+Condition+gene_type+distance+gene_id.R~gene_feature, value.var="mK", fun.aggregate=mean, fill=0)
plotdata2 = plotdata2[exon>0, `:=`(ratioIE = (intron)/(exon), ratioEI = (exon)/(intron))]
setkeyv(plotdata2, c("Cell", "Condition", "gene_id.R"))
sel = plotdata2[is.finite(ratioEI), length(unique(distance)), by=key(plotdata2)][V1==length(unique(plotdata2[, distance]))]
plotdata2 = plotdata2[sel, nomatch=0]

tab = plotdata2[Condition%in%c("1FA", "4hActD"), list(N=.N/length(unique(distance))), by=c("Cell", "Condition", "gene_type")]
gg = ggviolin(plotdata2[Condition%in%c("1FA", "4hActD")], x="distance", y="log2(ratioEI)", fill="distance", facet.by="gene_type~paste(Cell, Condition)") +
   ggtitle("RNA processing rate (exon vs inton ~ distance)",
           subtitle="Reads are sum and normalised for each genic feature and averaged per interaction and then per gene.\nOnly RNAs with exonic and intronic reads within all distance bins are reported.") +
   # stat_compare_means(ref.group="Gene body", hide.ns=TRUE, label = "p.signif") +
   scale_fill_manual("Distance to transcription site", values=rev(brewer.pal(7,"Blues"))) +
   scale_y_continuous("Exonic/Intronic reads (log2)") +
   scale_x_discrete("") +
   geom_text(data=tab, aes(label=paste("N =", N)), x=-Inf, y=-Inf, hjust=-0.2, vjust=-1, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) +
   theme_bw() +
   theme(axis.text.x=element_text(angle=60, hjust=1, vjust=1, size=14),
         axis.text.y=element_text(size=14), legend.text=element_text(size=14),
         axis.title.y=element_text(size=14), legend.title=element_text(size=14),
         strip.text.x=element_text(size=14), strip.text.y=element_text(size=14),
         legend.position="bottom") +
   guides(fill = guide_legend(ncol = 7)) + facet_wrap(paste(Cell, Condition)~gene_type, nrow=3, drop=FALSE) + geom_boxplot(outlier.colour=NA, width=0.2)
ggsave(gg, filename=paste0(working.folder, "plots/RNA-DNA_processing_rate.png"), units="in", height=21, width=10)
ggsave(gg, filename=paste0(working.folder, "plots/RNA-DNA_processing_rate.pdf"), height=21, width=10)

for( x in unique(plotdata2[, Cell]) ){
   plotdata3 = plotdata2[Cell%in%x]
   for( y in unique(plotdata3[, Condition]) ){
      if( nrow(plotdata3[Condition%in%y,])>0 ){
         tab = plotdata3[Condition%in%y, list(N=.N/length(unique(distance))), by=c("Condition", "gene_type")]
         gg = ggviolin(plotdata3[Condition%in%y], x="distance", y="log2(ratioEI)", fill="distance", facet.by="gene_type~Condition") +
            ggtitle("RNA processing rate (exon vs inton ~ distance)",
                    subtitle="Reads are sum and normalised for each genic feature and averaged per interaction and then per gene.\nOnly RNAs with exonic and intronic reads within all distance bins are reported.") +
            stat_compare_means(ref.group="Gene body", hide.ns=TRUE, label = "p.signif") +
            scale_fill_manual("Distance to transcription site", values=rev(brewer.pal(7,"Blues"))) +
            scale_y_continuous("Exonic/Intronic reads (log2)") +
            scale_x_discrete("") +
            geom_text(data=tab, aes(label=paste("N =", N)), x=-Inf, y=Inf, hjust=-0.2, vjust=2, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) +
            theme_bw() +
            theme(axis.text.x=element_text(angle=60, hjust=1, vjust=1, size=14),
                  axis.text.y=element_text(size=14), legend.text=element_text(size=14),
                  axis.title.y=element_text(size=14), legend.title=element_text(size=14),
                  strip.text.x=element_text(size=14), strip.text.y=element_text(size=14),
                  legend.position="bottom") +
            guides(fill = guide_legend(ncol = 7)) + facet_wrap(~gene_type, nrow=1) + geom_boxplot(outlier.colour=NA, width=0.2)
         ggsave(gg, filename=paste0(working.folder, "plots/RNA-DNA_processing_rate_", x, "_", y, ".png"), units="in", height=8, width=12)
         ggsave(gg, filename=paste0(working.folder, "plots/RNA-DNA_processing_rate_", x, "_", y, ".pdf"), height=8, width=12)
      }
   }
}
```

### Comparison with RAP data

```{r rap, eval=FALSE}
genes.pc = unique(tx.regions.dt[annotation%in%"protein_coding", gene_id])
genes.pc = genes[genes$gene_id%in%genes.pc,]

sample.names = paste("malat1", c("GRID_mESC_GRIDseq", "RADICL_mESC_1FA", "RADICL_mESC_2FA", "RAP_Treatment.picard"), sep="_")

peaks = lapply(setNames(sample.names, sample.names), 
       function(x)
          fread(file=file.path(working.folder, "MALAT1_Pyicoteo", paste0(x, ".1kb.pyicos.peaks"))))


# genes.pc.counts = countOverlaps(genes.pc, with(interactions, GRanges(chr.R, IRanges(start.R, end.R), strand.R)))
# genes.pc.tpm = countToTPM(genes.pc.counts, genes.pc)

sample.names = c("GRID_mESC_GRIDseq", "RADICL_mESC_1FA", "RADICL_mESC_2FA")

malat1 = rbindlist(lapply(setNames(sample.names, sample.names),
                         function(grp){
                            
                            print(grp)
                            
                            files = list.files(paste0(working.folder, "processed"), pattern=paste0(grp, ".*.gz"), full.names=TRUE)
                            files = files[grepl(bin_width, files) & !grepl("GenicOnly", files) & !grepl("significant", files)]
                            interactions = rbindlist(lapply(seq_along(files),
                                                            function(x){
                                                               tmp = fread(cmd=paste("gunzip -c", files[[as.numeric(as.character(x))]]), showProgress=FALSE)
                                                               tmp[, replicate := as.numeric(as.character(x))]
                                                               tmp = tmp[chr.D%in%paste0("chr", c(1:19, "X", "Y")),]
                                                               tmp.gr = with(tmp, GRanges(chr.D, IRanges(start.D, end.D)))
                                                               tmp = tmp[!overlapsAny(tmp.gr, blacklist.gr),]
                                                               tmp.gr = with(tmp, GRanges(chr.D, IRanges(start.D, end.D)))
                                                               tmp = tmp[overlapsAny(tmp.gr, reduce(tx.regions)),]
                                                               tmp = tmp[gene_id.R%in%"ENSMUSG00000092341.2",]
                                                               tmp
                                                            }))
                            interactions
                         }), idcol="Set")

rap.counts = data.table(as.data.frame(mm10), fread(file.path(working.folder, "MACS", "mm10.cov.txt"), select=7, col.names="counts"))
setkey(rap.counts, bin_id)

tmp = malat1[Set=="GRID_mESC_GRIDseq",]
tmp = tmp[, list(GRID=.N), by=c("gene_id.D")]
setkey(tmp, gene_id.D)
rap.counts = merge(x=rap.counts, y=tmp, all.x=TRUE, by.x="bin_id", by.y="gene_id.D")
rap.counts[is.na(GRID), GRID := 0]

tmp = malat1[Set=="RADICL_mESC_1FA",]
tmp = tmp[, list(FA1=.N), by=c("gene_id.D")]
setkey(tmp, gene_id.D)
rap.counts = merge(x=rap.counts, y=tmp, all.x=TRUE, by.x="bin_id", by.y="gene_id.D")
rap.counts[is.na(FA1), FA1 := 0]

tmp = malat1[Set=="RADICL_mESC_2FA",]
tmp = tmp[, list(FA2=.N), by=c("gene_id.D")]
setkey(tmp, gene_id.D)
rap.counts = merge(x=rap.counts, y=tmp, all.x=TRUE, by.x="bin_id", by.y="gene_id.D")
rap.counts[is.na(FA2), FA2 := 0]

tmp = malat1[Set%in%c("RADICL_mESC_1FA", "RADICL_mESC_2FA"),]
tmp = tmp[, list(FA1.2=.N), by=c("gene_id.D")]
setkey(tmp, gene_id.D)
rap.counts = merge(x=rap.counts, y=tmp, all.x=TRUE, by.x="bin_id", by.y="gene_id.D")
rap.counts[is.na(FA1.2), FA1.2 := 0]

mm10.10k = unlist(tile(reduce(mm10), width=1e4))
start(mm10.10k) = start(mm10.10k)+2
end(mm10.10k) = end(mm10.10k)+1
export.bed(mm10.10k, paste0(working.folder, "MACS/mm10_10k_bins.bed"))

rap.counts = data.table(as.data.frame(mm10.10k), fread(file.path(working.folder, "MACS", "mm10.10k.cov.txt"), select=7, col.names="counts"))
tmp1 = malat1[Set=="GRID_mESC_GRIDseq",]
tmp1 = sort.GenomicRanges(with(tmp1, GRanges(chr.D, IRanges(start.D, end.D), strand="+")), ignore.strand=TRUE)
tmp2 = malat1[Set%in%c("RADICL_mESC_1FA"),]
tmp2 = sort.GenomicRanges(with(tmp2, GRanges(chr.D, IRanges(start.D, end.D), strand="+")), ignore.strand=TRUE)
tmp3 = malat1[Set%in%c("RADICL_mESC_2FA"),]
tmp3 = sort.GenomicRanges(with(tmp3, GRanges(chr.D, IRanges(start.D, end.D), strand="+")), ignore.strand=TRUE)

rap.counts[, `:=`(GRID = countOverlaps(mm10.10k, tmp1), FA1 = countOverlaps(mm10.10k, tmp2), FA2 = countOverlaps(mm10.10k, tmp3), FA1.2 = countOverlaps(mm10.10k, c(tmp2, tmp3)))]

cor(rap.counts[rowSums(rap.counts[, 6:10, with=FALSE])!=0, 6:10])




tmp = malat1[Set=="GRID_mESC_GRIDseq",]
tmp = sort.GenomicRanges(with(tmp, GRanges(chr.D, IRanges(start.D, end.D), strand="+")), ignore.strand=TRUE)
seqlevels(tmp) = paste0("chr", c(1:19, "X", "Y"))
export.bed(resize(tmp, width=27, fix="center"), file.path(working.folder, paste0("MACS/malat1_GRID_mESC_GRIDseq.bed")))

tmp = malat1[Set%in%c("RADICL_mESC_1FA"),]
tmp = sort.GenomicRanges(with(tmp, GRanges(chr.D, IRanges(start.D, end.D), strand="+")), ignore.strand=TRUE)
seqlevels(tmp) = paste0("chr", c(1:19, "X", "Y"))
export.bed(resize(tmp, width=27, fix="center"), file.path(working.folder, paste0("MACS/malat1_RADICL_mESC_1FA.bed")))

tmp = malat1[Set%in%c("RADICL_mESC_2FA"),]
tmp = sort.GenomicRanges(with(tmp, GRanges(chr.D, IRanges(start.D, end.D), strand="+")), ignore.strand=TRUE)
seqlevels(tmp) = paste0("chr", c(1:19, "X", "Y"))
export.bed(resize(tmp, width=27, fix="center"), file.path(working.folder, paste0("MACS/malat1_RADICL_mESC_2FA.bed")))

# system(command="/Library/Frameworks/Python.framework/Versions/2.7/bin/macs2 callpeak -f BAMPE -t /Users/agostif/Desktop/Bonetti/MACS/RAP-DNA/SRR1193454.picard.bam -c /Users/agostif/Desktop/Bonetti/MACS/RAP-DNA/SRR1193455.picard.bam -g mm --keep-dup all --outdir /Users/agostif/Desktop/Bonetti/MACS/ --broad --extsize 100 -n malat1_RAP", intern=FALSE, wait=TRUE)
system(command="/usr/local/bin/macs2 callpeak -f BED -t /Users/agostif/Desktop/Bonetti/MACS/malat1_RADICL_mESC_1FA.bed -g mm --outdir /Users/agostif/Desktop/Bonetti/MACS --broad --nolambda --nomodel --keep-dup 1 --extsize 147 -n malat1_RADICL_mESC_1FA --broad-cutoff 0.2", intern=FALSE, wait=TRUE)
system(command="/usr/local/bin/macs2 callpeak -f BED -t /Users/agostif/Desktop/Bonetti/MACS/malat1_GRID_mESC_GRIDseq.bed -g mm --outdir /Users/agostif/Desktop/Bonetti/MACS --broad --nolambda --nomodel --keep-dup 1 --extsize 147 -n malat1_GRID_mESC_GRIDseq --broad-cutoff 0.2", intern=FALSE, wait=TRUE)


macs.cols = c("chr", "start", "end", "name", "score", ".", "fc", "pval", "qval")

rap.fdr = 0.2
rap.tpm = 10

rap = fread(file.path(working.folder, "MACS/malat1_RAP_peaks.broadPeak"), col.names=macs.cols)
rap = rap[qval>(-log10(rap.fdr)),][chr%in%paste0("chr", c(1:19, "X", "Y"))]

rap.gr = with(rap, GRanges(chr, IRanges(start, end), strand="*"))

rap.genes = genes.pc[overlapsAny(genes.pc, rap.gr),]

rad = fread(file.path(working.folder, "MACS/malat1_RADICL_mESC_1FA_peaks.broadPeak"), col.names=macs.cols)
rad = rad[qval>(-log10(rap.fdr)),][chr%in%paste0("chr", c(1:19, "X", "Y"))]

rad.gr = with(rad, GRanges(chr, IRanges(start, end), strand="*"))

rad.genes = genes.pc[overlapsAny(genes.pc, rad.gr),]

gri = fread(file.path(working.folder, "MACS/malat1_GRID_mESC_GRIDseq_peaks.broadPeak"), col.names=macs.cols)
gri = gri[qval>(-log10(rap.fdr)),][chr%in%paste0("chr", c(1:19, "X", "Y"))]

gri.gr = with(gri, GRanges(chr, IRanges(start, end), strand="*"))

gri.genes = genes.pc[overlapsAny(genes.pc, gri.gr),]

table(overlapsAny(rad.genes, rap.genes))
table(overlapsAny(rap.genes, rad.genes))
table(overlapsAny(gri.genes, rap.genes))
table(overlapsAny(rap.genes, gri.genes))
table(overlapsAny(gri.genes, rad.genes))
table(overlapsAny(rad.genes, gri.genes))

# vennlist = list(RAP=rap.genes$gene_id, RADICL=rad.genes$gene_id, GRID=gri.genes$gene_id)
vennlist = list(RAP=rap.genes$gene_id, RADICL=rad.genes$gene_id)
venn.plot.P = venn.diagram(vennlist, filename=NULL, fill=c("dodgerblue3", "firebrick3", "grey3")[1:length(vennlist)],
                               resolution=300, imagetype="png", units="in", height=10, width=10,
                               print.mode = c("raw", "percent"), fontfamily = "Helvetica", main.fontfamily = "Helvetica", cat.fontfamily = "Helvetica")
pdf(file=paste0(working.folder, "plots/RNA-DNA_RAP_Malat1_1FA_GRID_FDR", rap.fdr, ".pdf"), width=6, height=6)
   grid.arrange(gTree(children=venn.plot.P), ncol=1)
dev.off()

rap = fread("MACS/malat1_RAP_peaks.broadPeak")
rap = rap[V9>=(-log10(0.01)),][V1%in%paste0("chr", c(1:19, "X", "Y"))]

rap.gr = with(rap, GRanges(V1, IRanges(V2, V3), strand="*"))

rap.genes = genes.pc[overlapsAny(genes.pc, rap.gr),]

rad = fread("MACS/malat1_1FA_peaks.broadPeak")
rad = rad[V9>=(-log10(0.01)),][V1%in%paste0("chr", c(1:19, "X", "Y"))]

rad.gr = with(rad, GRanges(V1, IRanges(V2, V3), strand="*"))

rad.genes = genes.pc[overlapsAny(genes.pc, rad.gr),]

table(overlapsAny(rad.genes, rap.genes))
table(overlapsAny(rap.genes, rad.genes))

vennlist = list(RAP=rap.genes$gene_id, RADICL=rad.genes$gene_id)
venn.plot.P = venn.diagram(vennlist, filename=NULL, fill=c("dodgerblue3", "firebrick3")[1:length(vennlist)],
                               resolution=300, imagetype="png", units="in", height=10, width=10,
                               print.mode = c("raw", "percent"), fontfamily = "Helvetica", main.fontfamily = "Helvetica", cat.fontfamily = "Helvetica")
pdf(filename=paste0("plots/RNA-DNA_RAP_Malat1_1FA_overlaps_at_FDR_0.01.pdf"), width=6, height=6)
   grid.arrange(gTree(children=venn.plot.P), ncol=1)
dev.off()
```

<!-- ### TAD -->

<!-- ```{r TAD, eval=FALSE} -->
<!-- # tad = fread("gunzip -c mESC-J1.mm10.chr10.25000.simpleNorm.tsv.gz", col.names=c("chr.R", "start.R", "end.R", "chr.D", "start.D", "end.D", "freq")) -->
<!-- # tad[, `:=`(bin.R = paste(chr.R, start.R/25e3+1, sep="_"), bin.D = paste(chr.D, start.D/25e3+1, sep="_"))] -->
<!-- # tad = tad[freq>0,] -->
<!-- # tad.gr = with(tad, GRanges(chr.R, IRanges(start.R, end.R), strand="*", bin.R)) -->
<!-- #  -->
<!-- # interactions = fread(paste0("gunzip -c RNA-BIN_interactions_", gencode_version, "_level_1_2_3_AllOverlapsUnique_", bin_width, ".tsv.gz")) -->
<!-- #  -->
<!-- # setnames(interactions, "BiolReplicate", "Replicate") -->
<!-- # setkeyv(interactions, c("Condition", "chr.R", "gene_id.R", "chr.D", "gene_id.D")) -->
<!-- # interactions = interactions[, .N, by=key(interactions)] -->
<!-- # setnames(interactions, "N", "Count") -->
<!-- #  -->
<!-- # interactions.gr = with(interactions, GRanges(chr.R, IRanges(start.R, end.R), strand.R)) -->
<!-- #  -->
<!-- # save(tad, interactions, file="tmp.RData") -->
<!-- #  -->
<!-- # rm(tad, interactions) -->
<!-- # gc() -->

<!-- # overlaps = data.table(as.data.frame(findOverlaps(tad.gr, interactions.gr)), key="queryHits") -->

<!-- tad = fread("mESC_boundaries.bed", col.names=c("chr", "start", "end", "domain"), sep='\t', header=FALSE) -->
<!-- tad.gr = with(tad, GRanges(chr, IRanges(start, end), name=domain)) -->

<!-- tad.genes = genes[overlapsAny(genes, tad.gr)] -->
<!-- tad.genes = tad.genes[tad.genes$gene_id%in%c(coding.sel, unlist(noncoding.sel)),] -->

<!-- fwrite(as.data.frame(tad.genes), file="mESC_boundaries_overlapping_genes.tsv", sep='\t', quote=FALSE) -->
<!-- ``` -->

<!-- ### Distance Analysis -->

<!-- ```{r Distance, eval=FALSE} -->
<!-- interactions = lapply(unique(sampleTable[grepl("RADICL_mESC_1FA", Label), Label]), -->
<!--                       function(lbl){ -->
<!--                          fread(paste0("gunzip -c ", working.folder, "processed/RNA-BIN_", lbl, "_", gencode_version, "_level_1_2_3_Unique_", bin_width, ".tsv.gz")) -->
<!--                       }) -->
<!-- interactions = rbindlist(interactions) -->
<!-- interactions = interactions[!gene_id.R%in%c("Intergenic", "Multigenic"),] -->
<!-- interactions[, Condition := "RADICL_mESC_1FA"] -->

<!-- # interactions = fread(paste0("gunzip -c RNA-BIN_biotypes_", gencode_version, "_level_1_2_3_AllOverlapsUnique_", bin_width, ".tsv.gz"), showProgress=FALSE) -->
<!-- # interactions = interactions[!gene_id.R%in%c("Intergenic", "Multigenic"),] -->
<!-- setkeyv(interactions, c("Condition", "gene_id.R")) -->

<!-- tab = interactions[, .N, by=key(interactions)] -->
<!-- setkey(tab, gene_id.R) -->
<!-- setkey(genes.dt, gene_id) -->
<!-- tab[genes.dt, width := width] -->
<!-- tab[, Norm := N/width*1e3] -->
<!-- tab[, Quartile := cut(Norm, breaks=quantile(Norm), labels=c("I", "II", "III", "IV"), include.lowest=TRUE), by="Condition"] -->
<!-- setkeyv(tab, c("Condition", "gene_id.R")) -->

<!-- interactions[tab, Quartile := Quartile] -->

<!-- interactions = interactions[, distance := abs(start.R-end.D)] -->

<!-- binDistance = as.character(cut(interactions[, distance], breaks=c(-1, 1e3, 5e3, 1e4, 1e5, 1e6, 1e7, Inf), labels=c("<1Kb", "1-5Kb", "5-10Kb", "10-100Kb", "100Kb-1Mb", "1-10Mb", ">10Mb"))) -->
<!-- binDistance[interactions[, chr.R!=chr.D]] = "Interchromosomal" -->
<!-- interactions[, distance := as.character(distance)][, distance := NA] -->
<!-- interactions[, distance := factor(binDistance, levels=c("<1Kb", "1-5Kb", "5-10Kb", "10-100Kb", "100Kb-1Mb", "1-10Mb", ">10Mb", "Interchromosomal"))] -->

<!-- dt = interactions[, .N, by=c("Condition", "annotation.R", "Quartile", "gene_id.R", "distance")][, list(distance, N, T=sum(N), P=N/sum(N)), by=c("Condition", "annotation.R", "Quartile", "gene_id.R")] -->
<!-- # dt = significant[, .N, by=c("Condition", "gene_type.R", "gene_id.R", "distance")][!distance%in%"<1Kb", list(distance, N, T=sum(N), P=N/sum(N)), by=c("Condition", "gene_type.R", "gene_id.R")] -->
<!-- dt[, annotation.R := factor(annotation.R, levels=c("protein_coding", "long_ncRNA", "ncRNA", "other", "NAST"))] -->

<!-- setkeyv(dt, c("Condition", "annotation.R", "Quartile")) -->
<!-- tab = dt[, list(N=length(unique(gene_id.R))), by=key(dt)] -->
<!-- tab2 = as.data.table(with(tab, expand.grid(Condition=unique(Condition), annotation.R=unique(annotation.R), Quartile=unique(Quartile), N=0))) -->
<!-- setkeyv(tab2, c("Condition", "annotation.R", "Quartile")) -->
<!-- tab = tab[tab2,][, N := as.integer(ifelse(is.na(N), i.N, N))] -->
<!-- tab[, `:=`(X=8, Y=rep(c(0.95, 0.85, 0.75, 0.65), 5))] -->

<!-- gg = ggboxplot(dt, x="distance", y="P", fill="annotation.R", add="box", facet.by="Quartile~Condition", palette="npg", -->
<!--           title="Distribution of RNA-BIN interactions (raw dataset)", -->
<!--           subtitle="Transcript biotypes are expression matched and divided into quartiles of expression.") + -->
<!--    scale_y_continuous("Dataset (biotype) percentage", labels=scales::percent) + -->
<!--    scale_x_discrete("Distance to transcriptional site", limits=c("<1Kb", "1-5Kb", "5-10Kb", "10-100Kb", "100Kb-1Mb", "1-10Mb", ">10Mb", "")) + -->
<!--    geom_label(data=tab, aes(x=X, y=Y, fill=annotation.R, label=N), show.legend=FALSE) + -->
<!--    theme_bw() + -->
<!--    theme(axis.text.x=element_text(angle=30, hjust=1, vjust=1, size=14), -->
<!--          axis.text.y=element_text(size=14), legend.text=element_text(size=14), -->
<!--          axis.title.y=element_text(size=14), legend.title=element_text(size=14), -->
<!--          strip.text.x=element_text(size=14), strip.text.y=element_text(size=14), -->
<!--          legend.position="right") + -->
<!--    guides(fill = guide_legend(title = "RNA gene biotype")) -->
<!-- ggsave(gg, filename="RNA-BIN_Interactions_distance_distribution_biotype_RAW.pdf", width=20, height=16) -->

<!-- rm(interactions); gc() -->

<!--    files.sig = list.files(paste0(working.folder, "processed"), pattern="*_cnd_*", full.names=TRUE) -->

<!--    significant = rbindlist(lapply(files.sig, -->
<!--                                   function(x) -->
<!--                                      fread(paste("gunzip -c", x), showProgress=FALSE))) -->
<!--    setnames(significant, "replicate", "Replicate") -->


<!-- # significant = fread(paste0("gunzip -c RNA-BIN_counts_", gencode_version, "_level_1_2_3_AllOverlapsUnique_", bin_width, "_significant", fdr.th, "_extended_tsc_noDistance.tsv.gz"), showProgress=FALSE) -->
<!-- setkeyv(significant, c("Condition", "gene_id.R")) -->

<!-- tab = significant[, .N, by=key(significant)] -->
<!-- setkey(tab, gene_id.R) -->
<!-- setkey(genes.dt, gene_id) -->
<!-- tab[genes.dt, width := width] -->
<!-- tab[, Norm := N/width*1e3] -->
<!-- tab[, Quartile := cut(Norm, breaks=quantile(Norm), labels=c("I", "II", "III", "IV"), include.lowest=TRUE), by="Condition"] -->
<!-- setkeyv(tab, c("Condition", "gene_id.R")) -->

<!-- significant[tab, Quartile := Quartile] -->

<!-- significant = significant[chr.R==chr.D,][, distance := abs(start.R-end.D)] -->

<!-- binDistance = as.character(cut(significant[, distance], breaks=c(-1, 1e3, 5e3, 1e4, 1e5, 1e6, 1e7, Inf), labels=c("<1Kb", "1-5Kb", "5-10Kb", "10-100Kb", "100Kb-1Mb", "1-10Mb", ">10Mb"))) -->
<!-- significant[, distance := factor(binDistance, levels=c("<1Kb", "1-5Kb", "5-10Kb", "10-100Kb", "100Kb-1Mb", "1-10Mb", ">10Mb"))] -->

<!-- dt = significant[, .N, by=c("Condition", "annotation.R", "Quartile", "gene_id.R", "distance")][, list(distance, N, T=sum(N), P=N/sum(N)), by=c("Condition", "annotation.R", "Quartile", "gene_id.R")] -->
<!-- # dt = significant[, .N, by=c("Condition", "gene_type.R", "gene_id.R", "distance")][!distance%in%"<1Kb", list(distance, N, T=sum(N), P=N/sum(N)), by=c("Condition", "gene_type.R", "gene_id.R")] -->
<!-- dt[, annotation.R := factor(annotation.R, levels=c("protein_coding", "long_ncRNA", "ncRNA", "other"))] -->

<!-- setkeyv(dt, c("Condition", "annotation.R", "Quartile")) -->
<!-- tab = dt[, list(N=length(unique(gene_id.R))), by=key(dt)] -->
<!-- tab2 = as.data.table(with(tab, expand.grid(Condition=unique(Condition), annotation.R=unique(annotation.R), Quartile=unique(Quartile), N=0))) -->
<!-- setkeyv(tab2, c("Condition", "annotation.R", "Quartile")) -->
<!-- tab = tab[tab2,][, N := as.integer(ifelse(is.na(N), i.N, N))] -->
<!-- tab[, `:=`(X=8, Y=rep(rep(c(0.95, 0.85, 0.75, 0.65), each=4), 4))] -->

<!-- gg = ggboxplot(dt, x="distance", y="P", fill="annotation.R", add="box", facet.by="Quartile~Condition", palette="npg", -->
<!--           title="Distribution of RNA-BIN interactions (significant dataset)", -->
<!--           subtitle="Transcript biotypes are expression matched and divided into quartiles of expression.") + -->
<!--    scale_y_continuous("Dataset (biotype) percentage", labels=scales::percent) + -->
<!--    scale_x_discrete("Distance to transcriptional site", limits=c("<1Kb", "1-5Kb", "5-10Kb", "10-100Kb", "100Kb-1Mb", "1-10Mb", ">10Mb", "")) + -->
<!--    geom_label(data=tab, aes(x=X, y=Y, fill=annotation.R, label=N), show.legend=FALSE) + -->
<!--    theme_bw() + -->
<!--    theme(axis.text.x=element_text(angle=30, hjust=1, vjust=1, size=14), -->
<!--          axis.text.y=element_text(size=14), legend.text=element_text(size=14), -->
<!--          axis.title.y=element_text(size=14), legend.title=element_text(size=14), -->
<!--          strip.text.x=element_text(size=14), strip.text.y=element_text(size=14), -->
<!--          legend.position="right") + -->
<!--    guides(fill = guide_legend(title = "RNA gene biotype")) -->
<!-- ggsave(gg, filename="RNA-BIN_Interactions_distance_distribution_biotype_significant.pdf", width=20, height=16) -->
<!-- ``` -->

### Motifs

```{r motifs, eval=FALSE}
# significant = fread(paste0("gunzip -c RNA-BIN_counts_", gencode_version, "_level_1_2_3_AllOverlapsUnique_", bin_width, "_significant", fdr.th, "_extended_tsc_noDistance.tsv.gz"), showProgress=FALSE)

files.sig = list.files(paste0(working.folder, "processed"), pattern="*_cnd_*", full.names=TRUE)

files.sig = files.sig[grepl("RADICL_mESC_1FA", files.sig) | grepl("RADICL_mESC_Control", files.sig)]

significant = rbindlist(lapply(files.sig,
                               function(x)
                                  fread(paste("gunzip -c", x), showProgress=FALSE)))
setnames(significant, "replicate", "Replicate")

meg3 = genes.dt[gene_name%in%"Meg3", gene_id]
meg3.gr = with(genes.dt[gene_name%in%"Meg3",], GRanges(seqnames, IRanges(start, end), strand))
subset = significant[gene_id.R%in%meg3]

subset.gr = with(subset[Condition%in%"RADICL_mESC_1FA",], GRanges(chr.D, IRanges(start.D, end.D), strand="*"))
subset.gr = subset.gr[!overlapsAny(subset.gr, resize(meg3.gr, width=width(meg3.gr)+2e4, fix="center")),]
sequences = getSeq(BSgenome.Mmusculus.UCSC.mm10, resize(subset.gr, width=100, fix="center"))
names(sequences) = paste("Seq", 1:length(sequences), sep="_")
writeXStringSet(sequences, filepath=paste0(working.folder, "MEME/Meg3_Targets_1FA.fasta"), format="fasta")

# system("meme MEME/Meg3_Targets_1FA.fasta -oc MEME/Meg3_1FA -dna -nmotifs 3 -minw 5 -maxw 10 -revcomp -maxsize 500000")

subset.gr = with(subset[Condition%in%"RADICL_mESC_Control",], GRanges(chr.D, IRanges(start.D, end.D), strand="*"))
subset.gr = subset.gr[!overlapsAny(subset.gr, resize(meg3.gr, width=width(meg3.gr)+2e4, fix="center")),]
sequences = getSeq(BSgenome.Mmusculus.UCSC.mm10,  resize(subset.gr, width=100, fix="center"))
names(sequences) = paste("Seq", 1:length(sequences), sep="_")
writeXStringSet(sequences, filepath=paste0(working.folder, "MEME/Meg3_Targets_Control.fasta"), format="fasta")

# system("meme MEME/Meg3_Targets_Control.fasta -oc MEME/Meg3_Control -dna -nmotifs 3 -minw 5 -maxw 10 -revcomp -maxsize 500000")

subset.rna.gr = with(subset[Condition%in%"RADICL_mESC_1FA",], GRanges(chr.R, IRanges(start.R, end.R), strand=strand.R))
subset.dna.gr = with(subset[Condition%in%"RADICL_mESC_1FA",], GRanges(chr.D, IRanges(start.D, end.D), strand="*"))
subset.rna.gr = subset.rna.gr[!overlapsAny(subset.dna.gr, resize(meg3.gr, width=width(meg3.gr)+2e4, fix="center")),]
sequences = getSeq(BSgenome.Mmusculus.UCSC.mm10, resize(subset.rna.gr, width=100, fix="center"))
names(sequences) = paste("Seq", 1:length(sequences), sep="_")
writeXStringSet(sequences, filepath=paste0(working.folder, "MEME/Meg3_RNA_1FA.fasta"), format="fasta")

# system("meme MEME/Meg3_RNA_1FA.fasta -oc MEME/Meg3_1FA_RNA -rna -nmotifs 3 -minw 5 -maxw 10 -maxsize 500000")

# wwox = genes.dt[gene_name%in%"Wwox", gene_id]
# wwox.gr = with(genes.dt[gene_name%in%"Wwox",], GRanges(seqnames, IRanges(start, end), strand))
# subset = significant[gene_id.R%in%wwox]
# 
# subset.gr = with(subset[Condition%in%"1FA"], GRanges(chr.D, IRanges(start.D, end.D), strand="*"))
# subset.gr = subset.gr[!overlapsAny(subset.gr, resize(wwox.gr, width=width(wwox.gr)+2e4, fix="center")),]
# sequences = getSeq(BSgenome.Mmusculus.UCSC.mm10, subset.gr)
# names(sequences) = paste("Seq", 1:length(sequences), sep="_")
# writeXStringSet(sequences, filepath="MEME/Wwox_Targets_1FA.fasta", format="fasta")
# 
# system("meme MEME/Wwox_Targets_1FA.fasta -oc MEME/Wwox_1FA -dna -nmotifs 3 -minw 5 -maxw 10 -revcomp -maxsize 2000000")
# 
# subset.gr = with(subset[Condition%in%"Control1"], GRanges(chr.D, IRanges(start.D, end.D), strand="*"))
# subset.gr = subset.gr[!overlapsAny(subset.gr, resize(wwox.gr, width=width(wwox.gr)+2e4, fix="center")),]
# sequences = getSeq(BSgenome.Mmusculus.UCSC.mm10, subset.gr)
# names(sequences) = paste("Seq", 1:length(sequences), sep="_")
# writeXStringSet(sequences, filepath="MEME/Wwox_Targets_Control1.fasta", format="fasta")
# 
# system("meme MEME/Wwox_Targets_Control1.fasta -oc MEME/Wwox_Control1 -dna -nmotifs 3 -minw 5 -maxw 10 -revcomp -maxsize 2000000")
# 
# subset.rna.gr = with(subset[Condition%in%"1FA"], GRanges(chr.R, IRanges(start.R, end.R), strand=strand.R))
# subset.dna.gr = with(subset[Condition%in%"1FA"], GRanges(chr.D, IRanges(start.D, end.D), strand="*"))
# subset.rna.gr = subset.rna.gr[!overlapsAny(subset.dna.gr, resize(wwox.gr, width=width(wwox.gr)+2e4, fix="center")),]
# sequences = getSeq(BSgenome.Mmusculus.UCSC.mm10, subset.rna.gr)
# names(sequences) = paste("Seq", 1:length(sequences), sep="_")
# writeXStringSet(sequences, filepath="MEME/Wwox_RNA_1FA.fasta", format="fasta")
# 
# system("meme MEME/Wwox_RNA_1FA.fasta -oc MEME/Wwox_1FA_RNA -rna -nmotifs 3 -minw 5 -maxw 10 -maxsize 2000000")
```

<!-- ```{r examples, eval=FALSE} -->
<!-- interactions = rbindlist(list(readRDS(paste0("RNA-BIN_overlaps_", gencode_version, "_level_1_2_3_AllOverlapsUniqueNoActD_", bin_width, "_Control.rds")), -->
<!--                               readRDS(paste0("RNA-BIN_overlaps_", gencode_version, "_level_1_2_3_AllOverlapsUniqueNoActD_", bin_width, "_1FA.rds")), -->
<!--                               readRDS(paste0("RNA-BIN_overlaps_", gencode_version, "_level_1_2_3_AllOverlapsUniqueNoActD_", bin_width, "_2FA.rds")))) -->
<!-- interactions = interactions[Condition%in%c("1FA", "2FA"), list(Condition, Replicate=BiolReplicate, gene_id.R, gene_id.D)] -->
<!-- setkeyv(interactions, c("Condition", "Replicate", "gene_id.R", "gene_id.D")) -->
<!-- interactions = interactions[, .N, by=key(interactions)] -->
<!-- setnames(interactions, "N", "Count") -->

<!-- setkeyv(interactions, c("Condition", "gene_id.R", "gene_id.D")) -->

<!-- filtered = rbindlist(list(readRDS(paste0("RNA-BIN_counts_", gencode_version, "_level_1_2_3_AllOverlapsUniqueNoActD_", bin_width, "_Control_filtered.rds")), -->
<!--                           readRDS(paste0("RNA-BIN_counts_", gencode_version, "_level_1_2_3_AllOverlapsUniqueNoActD_", bin_width, "_1FA_filtered.rds")), -->
<!--                           readRDS(paste0("RNA-BIN_counts_", gencode_version, "_level_1_2_3_AllOverlapsUniqueNoActD_", bin_width, "_2FA_filtered.rds")))) -->
<!-- filtered = unique(filtered[Condition%in%c("1FA", "2FA"), list(Condition, gene_id.R, gene_id.D)]) -->
<!-- filtered = filtered[gene_id.R%in%examples[, id],][, Type := "Filtered"] -->
<!-- setkeyv(filtered, c("gene_id.R", "gene_id.D", "Condition")) -->

<!-- ##### Load filtered before running this one: -->
<!-- setkeyv(filtered, c("Condition", "gene_id.R", "gene_id.D")) -->
<!-- interactions = merge(interactions, filtered, all.x=TRUE) -->
<!-- interactions[is.na(Type), Type := "Raw"] -->

<!-- setkey(interactions, gene_id.R) -->
<!-- interactions = interactions[examples,] -->

<!-- y.max = max(interactions[, log2(Count)]) -->
<!-- data = dcast.data.table(interactions, name+Condition+gene_id.D~Replicate, value.var="Count") -->

<!-- tab = lapply(unique(data[, name]), function(x) -->
<!--    cor(data[Condition%in%"1FA" & name%in%as.character(x), list(`1`, `2`, `3`)], use="complete")) -->
<!-- names(tab) = unique(data[, name]) -->

<!-- # Get lower triangle of the correlation matrix -->
<!--   get_lower_tri<-function(cormat){ -->
<!--     cormat[upper.tri(cormat)] <- NA -->
<!--     return(cormat) -->
<!--   } -->
<!--   # Get upper triangle of the correlation matrix -->
<!--   get_upper_tri <- function(cormat){ -->
<!--     cormat[lower.tri(cormat)]<- NA -->
<!--     return(cormat) -->
<!--   } -->

<!-- reorder_cormat <- function(cormat){ -->
<!-- # Use correlation between variables as distance -->
<!-- dd <- as.dist((1-cormat)/2) -->
<!-- hc <- hclust(dd) -->
<!-- cormat <-cormat[hc$order, hc$order] -->
<!-- } -->

<!-- ggl = lapply(tab, function(x){ -->

<!--    # cormat <- reorder_cormat(tab[[1]]) -->
<!--    upper_tri <- get_upper_tri(x) -->
<!--    # Melt the correlation matrix -->
<!--    melted_cormat <- melt(upper_tri, na.rm = TRUE) -->

<!--    gg0 = ggplot(melted_cormat, aes(Var2, Var1, fill = value))+ -->
<!--       geom_tile(color = "white")+ -->
<!--       scale_fill_gradient2(low = "blue", high = "red", mid = "white",  -->
<!--                            midpoint = 0, limit = c(-1,1), space = "Lab",  -->
<!--                            name="Pearson\nCorrelation") + -->
<!--       theme_minimal()+ # minimal theme -->
<!--       theme(axis.text.x = element_text(angle = 45, vjust = 1,  -->
<!--                                        size = 12, hjust = 1))+ -->
<!--       coord_fixed() -->
<!--    gg0 +  -->
<!--       geom_text(aes(Var2, Var1, label = round(value, 2)), color = "black", size = 4) + -->
<!--       theme( -->
<!--          axis.title.x = element_blank(), -->
<!--          axis.title.y = element_blank(), -->
<!--          panel.grid.major = element_blank(), -->
<!--          panel.border = element_blank(), -->
<!--          panel.background = element_blank(), -->
<!--          axis.ticks = element_blank(), -->
<!--          legend.justification = c(1, 0), -->
<!--          legend.position = c(0.6, 0.7), -->
<!--          legend.direction = "horizontal")+ -->
<!--       guides(fill = guide_colorbar(barwidth = 7, barheight = 1, -->
<!--                                    title.position = "top", title.hjust = 0.5)) -->
<!-- }) -->

<!-- gg1 = ggplot(data, aes(x=log2(`1FA`), y=log2(`2FA`))) + -->
<!--    ggtitle("Raw") + -->
<!--    geom_point(alpha=0.5, size=1) + -->
<!--    scale_colour_brewer("", palette="Set1") + -->
<!--    scale_x_continuous("1FA - Average count (log2)", limits=c(0, y.max+0.5)) + -->
<!--    scale_y_continuous("2FA - Average count (log2)", limits=c(0, y.max+0.5)) + -->
<!--    # geom_text(data=tab, aes(label=paste("R =", round(V1, 2))), x=y.max, y=0, hjust=1, vjust=0, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) + -->
<!--    facet_wrap(Replicate~name, ncol=3) + coord_fixed() + theme_bw() -->

<!-- data = dcast.data.table(interactions[Type%in%"Filtered"], name+Condition+gene_id.D~Replicate, value.var="Count") -->
<!-- tab = lapply(unique(data[, name]), function(x) -->
<!--    cor(data[Condition%in%"1FA" & name%in%as.character(x), list(`1`, `2`, `3`)], use="complete")) -->
<!-- names(tab) = unique(data[, name]) -->

<!-- ggl2 = lapply(tab, function(x){ -->

<!--    # cormat <- reorder_cormat(tab[[1]]) -->
<!--    upper_tri <- get_upper_tri(x) -->
<!--    # Melt the correlation matrix -->
<!--    melted_cormat <- melt(upper_tri, na.rm = TRUE) -->

<!--    gg0 = ggplot(melted_cormat, aes(Var2, Var1, fill = value))+ -->
<!--       geom_tile(color = "white")+ -->
<!--       scale_fill_gradient2(low = "blue", high = "red", mid = "white",  -->
<!--                            midpoint = 0, limit = c(-1,1), space = "Lab",  -->
<!--                            name="Pearson\nCorrelation") + -->
<!--       theme_minimal()+ # minimal theme -->
<!--       theme(axis.text.x = element_text(angle = 45, vjust = 1,  -->
<!--                                        size = 12, hjust = 1))+ -->
<!--       coord_fixed() -->
<!--    gg0 +  -->
<!--       geom_text(aes(Var2, Var1, label = round(value, 2)), color = "black", size = 4) + -->
<!--       theme( -->
<!--          axis.title.x = element_blank(), -->
<!--          axis.title.y = element_blank(), -->
<!--          panel.grid.major = element_blank(), -->
<!--          panel.border = element_blank(), -->
<!--          panel.background = element_blank(), -->
<!--          axis.ticks = element_blank(), -->
<!--          legend.justification = c(1, 0), -->
<!--          legend.position = c(0.6, 0.7), -->
<!--          legend.direction = "horizontal")+ -->
<!--       guides(fill = guide_colorbar(barwidth = 7, barheight = 1, -->
<!--                                    title.position = "top", title.hjust = 0.5)) -->
<!-- }) -->


<!-- gg2 = ggplot(data, aes(x=log2(`1FA`), y=log2(`2FA`))) + -->
<!--    ggtitle("Filtered") + -->
<!--    geom_point(alpha=0.5, size=1) + -->
<!--    scale_colour_brewer("", palette="Set1") + -->
<!--    scale_x_continuous("1FA - Average count (log2)", limits=c(0, y.max+0.5)) + -->
<!--    scale_y_continuous("2FA - Average count (log2)", limits=c(0, y.max+0.5)) + -->
<!--    # geom_text(data=tab, aes(label=paste("R =", round(V1, 2))), x=y.max, y=0, hjust=1, vjust=0, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) + -->
<!--    facet_wrap(Replicate~name, ncol=4) + coord_fixed() + theme_bw() -->

<!-- ggarrange(ggarrange(plotlist=ggl, ncol=4, nrow=1, labels=unique(data[, name])), ggarrange(plotlist=ggl2, ncol=4, nrow=1, labels=unique(data[, name])), ncol=1, nrow=2) -->


<!-- if(!exists("fdr.th")) fdr.th = 0.00001 -->

<!-- examples = data.table(name=c("Rn7sk", "Kcnq1ot1", "Firre", "Tfcp2l1")) -->
<!-- examples[, id := genes.dt[match(name, gene_name), gene_id]] -->
<!-- setkey(examples, id) -->

<!-- filtered = rbindlist(list(readRDS(paste0("RNA-BIN_counts_", gencode_version, "_level_1_2_3_AllOverlapsUniqueNoActD_", bin_width, "_Control_filtered.rds")), -->
<!--                           readRDS(paste0("RNA-BIN_counts_", gencode_version, "_level_1_2_3_AllOverlapsUniqueNoActD_", bin_width, "_1FA_filtered.rds")), -->
<!--                           readRDS(paste0("RNA-BIN_counts_", gencode_version, "_level_1_2_3_AllOverlapsUniqueNoActD_", bin_width, "_2FA_filtered.rds")))) -->
<!-- filtered = unique(filtered[Condition%in%c("1FA", "2FA"), list(Condition, gene_id.R, gene_id.D)]) -->
<!-- filtered = filtered[gene_id.R%in%examples[, id],][, Type := "Filtered"] -->
<!-- setkeyv(filtered, c("gene_id.R", "gene_id.D", "Condition")) -->

<!-- subset = readRDS(paste0("RData/RNA-BIN_noMultiOverlapsUniqueNoActD_counts_", gencode_version, "_level_1_2_3_", bin_width, "_pvalue_FDR.rds")) -->
<!-- subset = subset[Condition%in%c("1FA", "2FA"), list(Condition, gene_id.R, gene_id.D, Count, pval, fdr.by, Rjh)] -->
<!-- subset = subset[gene_id.R%in%examples[, id],] -->
<!-- setkeyv(subset, c("gene_id.R", "gene_id.D", "Condition")) -->
<!-- subset = merge(subset, filtered, all.x=TRUE) -->
<!-- subset[is.na(Type), Type := "Raw"] -->

<!-- setkey(subset, gene_id.R) -->
<!-- subset = subset[examples,] -->

<!-- y.max = max(subset[, log2(Count)]) -->
<!-- data = dcast.data.table(subset, name+gene_id.D~Condition, value.var="Count") -->
<!-- tab = data[, list(cor(log2(`1FA`), log2(`2FA`), use="pairwise.complete.obs")), by="name"] -->
<!-- gg1 = ggplot(data, aes(x=log2(`1FA`), y=log2(`2FA`))) + -->
<!--    ggtitle("Raw") + -->
<!--    geom_point(alpha=0.5, size=1) + -->
<!--    scale_colour_brewer("", palette="Set1") + -->
<!--    scale_x_continuous("1FA - Average count (log2)", limits=c(0, y.max+0.5)) + -->
<!--    scale_y_continuous("2FA - Average count (log2)", limits=c(0, y.max+0.5)) + -->
<!--    geom_text(data=tab, aes(label=paste("R =", round(V1, 2))), x=y.max, y=0, hjust=1, vjust=0, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) + -->
<!--    facet_wrap(~name, ncol=4) + coord_fixed() + theme_bw() -->

<!-- data = dcast.data.table(subset[Type%in%"Filtered"], name+gene_id.D~Condition, value.var="Count") -->
<!-- tab = data[, list(cor(log2(`1FA`), log2(`2FA`), use="pairwise.complete.obs")), by="name"] -->
<!-- gg2 = ggplot(data, aes(x=log2(`1FA`), y=log2(`2FA`))) + -->
<!--    ggtitle("Filtered") + -->
<!--    geom_point(alpha=0.5, size=1) + -->
<!--    scale_colour_brewer("", palette="Set1") + -->
<!--    scale_x_continuous("1FA - Average count (log2)", limits=c(0, y.max+0.5)) + -->
<!--    scale_y_continuous("2FA - Average count (log2)", limits=c(0, y.max+0.5)) + -->
<!--    geom_text(data=tab, aes(label=paste("R =", round(V1, 2))), x=y.max, y=0, hjust=1, vjust=0, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) + -->
<!--    facet_wrap(~name, ncol=4) + coord_fixed() + theme_bw() -->

<!-- data = dcast.data.table(subset[fdr.by<fdr.th], name+gene_id.D~Condition, value.var="Count") -->
<!-- tab = data[, list(cor(log2(`1FA`), log2(`2FA`), use="pairwise.complete.obs")), by="name"] -->
<!-- gg3 = ggplot(data, aes(x=log2(`1FA`), y=log2(`2FA`))) + -->
<!--    ggtitle(paste0("Significant (FDR<", fdr.th,")")) + -->
<!--    geom_point(alpha=0.5, size=1) + -->
<!--    scale_colour_brewer("", palette="Set1") + -->
<!--    scale_x_continuous("1FA - Average count (log2)", limits=c(0, y.max+0.5)) + -->
<!--    scale_y_continuous("2FA - Average count (log2)", limits=c(0, y.max+0.5)) + -->
<!--    geom_text(data=tab, aes(label=paste("R =", round(V1, 2))), x=y.max, y=0, hjust=1, vjust=0, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) + -->
<!--    facet_wrap(~name, ncol=4) + coord_fixed() + theme_bw() -->

<!-- ggarrange(gg1, gg2, gg3, ncol=1, nrow=3) -->
<!-- ``` -->

<!-- ```{r plotObsExp, eval=FALSE} -->
<!-- subset = readRDS(paste0("RData/RNA-BIN_noMultiOverlapsUniqueNoActD_counts_", gencode_version, "_level_1_2_3_", bin_width, "_pvalue_FDR.rds")) -->

<!-- tab = data.table(label=floor(min(subset[, Rjh])):ceiling(max(subset[, Rjh]))) -->
<!-- subset[, bin := findInterval(Rjh, floor(min(subset[, Rjh])):ceiling(max(subset[, Rjh])))] -->

<!-- subset[, bin := tab[bin, label+0.5]] -->

<!-- non = subset[!fdr.by<fdr.th, .N, by=c("Condition", "bin")] -->
<!-- sig = subset[ fdr.by<fdr.th, .N, by=c("Condition", "bin")] -->

<!-- gg = ggplot(non, aes(x=bin, y=N)) + -->
<!--    ggtitle("Distribution of observed/expected log ratio", subtitle="Significant (red) and non-significant (blue) interactions") + -->
<!--    geom_bar(stat="identity", position="identity", fill="dodgerblue3", col="black") + -->
<!--    geom_bar(data=sig, aes(x=bin, y=N), stat="identity", position="identity", col="black", fill="firebrick3") + -->
<!--    scale_y_continuous("Count") + -->
<!--    scale_x_continuous("log2(observed/expected)") + -->
<!--    facet_wrap(~Condition, ncol=3, labeller=label_both) + -->
<!--    theme_bw() -->
<!-- gg -->
<!-- ggsave(gg, filename=paste0("plots//RNA-BIN_", bin_width, "_distribution_observed_vs_expected_FDR_", fdr.th,".png"), units="in", height=5, width=12, dpi=300) -->

<!-- counts = subset -->

<!-- ggl = lapply(c("Pou5f1", "Rn7sk", "Kcnq1ot1", "Neat1", "Firre", "Nanog", "Utf1", "Klf4", "Tfcp2l1", "Miat", "Tunar", "Dubr", "Fgf4", "Pvt1", "Rian", "Rnu11", "Rnu12", "Gm22710", "Gm26249", "Actb"), -->
<!--              function(gene.name){ -->

<!--                 # gene.name = "Pou5f1" # Oct4 -->
<!--                 gene.id = genes.dt[gene_name%in%gene.name, gene_id] -->
<!--                 gene.chr = as.character(genes.dt[gene_name%in%gene.name, seqnames]) -->

<!--                 setkeyv(counts, c("Condition", "gene_id.D")) -->
<!--                 subset = counts[gene_id.R%in%gene.id, list(mRjh = mean(Rjh), sig = any(fdr.bh<fdr.th)), by=key(counts)] -->
<!--                 subset = subset[Condition%in%c("1FA", "2FA"),] -->
<!--                 print(paste(gene.name, nrow(subset))) -->
<!--                 if(any(subset[, .N, by="gene_id.D"][, N]>1)){ -->
<!--                    subset.chr = as.character(genes.dt[match(subset[, gene_id.D], gene_id), seqnames]) -->
<!--                    subset.name = as.character(genes.dt[match(subset[, gene_id.D], gene_id), gene_name]) -->
<!--                    subset[, `:=`(colour = ifelse(subset.chr%in%gene.chr, "firebrick3", "dodgerblue3"), -->
<!--                                  name = subset.name)] -->
<!--                    subset = dcast.data.table(subset, gene_id.D+name+colour+sig~Condition, value.var="mRjh") -->
<!--                    # subset[is.na(`1FA`), `1FA` := 0][is.na(`2FA`), `2FA` := 0] -->
<!--                    subset = subset[!(is.na(`1FA`) | is.na(`2FA`)),] -->
<!--                    corS = cor(subset[, list(`1FA`, `2FA`)], method="spearman") -->

<!--                    limitsXY = subset[, list(minXY=min(`1FA`, `2FA`), maxXY=max(`1FA`, `2FA`))] -->

<!--                    gg = ggplot(subset, aes(x=`1FA`, y=`2FA`, col=colour)) + -->
<!--                       ggtitle(paste(gene.name, "intercting genes")) + -->
<!--                       geom_point(alpha=0.5) + -->
<!--                       scale_colour_manual("Interaction type", values=setNames(subset[,colour], subset[,colour]), labels=c("Interchromosome", "Intrachromosome")) + -->
<!--                       scale_x_continuous("1FA log2(obs/exp)") + -->
<!--                       scale_y_continuous("2FA log2(obs/exp)") + -->
<!--                    coord_fixed(ratio=1, xlim=c(limitsXY[, minXY], limitsXY[, maxXY]), ylim=c(limitsXY[, minXY], limitsXY[, maxXY])) + -->
<!--                       geom_text(aes(label=paste("r =", round(corS[1,2],2))),  -->
<!--                                 x=limitsXY[, maxXY], y=limitsXY[, minXY], hjust=1, vjust=1, colour="black", inherit.aes=FALSE, parse=FALSE, size=4) + -->
<!--    theme_minimal() + theme(legend.position = c(0.5,0.96), legend.box.background = element_rect(), legend.justification = c("right", "top"), legend.box.just = "right", legend.margin = margin(3,3,3,3)) -->
<!--                    gg -->
<!--                 } -->
<!--              }) -->
<!-- png(paste0("plots/RNA-BIN_", bin_width, "_SelectedTranscripts_interactions_observed_vs_expected_1FA_2FA_noText_noSig_FDR_", fdr.th,".png"), units="in", res=300, width=20, height=17) -->
<!--    ggarrange(plotlist=ggl, ncol=5, nrow=4) -->
<!-- dev.off() -->
<!-- ``` -->

<!-- ```{r rapMalat1, eval=FALSE} -->
<!-- rap = fread("gunzip -c GSM1348346_Malat1-RAP-DNA-130928.bed.gz", col.names=c("seqnames", "start", "end", "score")) -->

<!-- rap.gr = with(rap, GRanges(seqnames, IRanges(start, end), strand="*", score)) -->

<!-- overlaps = as.data.table(findOverlaps(rap.gr, mm10, type="within")) -->
<!-- overlaps[,`:=`(bin_id=mm10$bin_id[subjectHits], score=rap.gr$score[queryHits])] -->

<!-- overlaps = overlaps[, list(score=mean(score)), by="bin_id"] -->
<!-- setkey(overlaps, bin_id) -->

<!-- malat1 = fread("gunzip -c RNA-BIN_counts_m14_level_1_2_3_AllOverlapsUniqueNoActD_1KB_filtered_extended.tsv.gz") -->
<!-- setkeyv(malat1, c("Condition", "gene_id.R", "gene_id.D")) -->
<!-- malat1 = malat1[gene_id.R%in%"ENSMUSG00000092341.2", .N, by=key(malat1)] -->
<!-- setkey(malat1, gene_id.D) -->

<!-- ggplot(malat1[overlaps, nomatch=0][Condition%in%"1FA",], aes(x=log2(N), y=log2(score))) + scale_x_continuous("RADICL 1FA log2(count)") + scale_x_continuous("RAP log2(count)") + geom_point() + theme_bw() + coord_fixed(ratio=1, xlim=c(-2,12), ylim=c(-2,12) ) -->
<!-- ``` -->

```{r denistyFunction}
# Get density of points in 2 dimensions.
# @param x A numeric vector.
# @param y A numeric vector.
# @param n Create a square n by n grid to compute density.
# @return The density within each square.
get_density <- function(x, y, n = 100) {
  dens <- MASS::kde2d(x = x, y = y, n = n)
  ix <- findInterval(x, dens$x)
  iy <- findInterval(y, dens$y)
  ii <- cbind(ix, iy)
  return(dens$z[ii])
}

set.seed(1)
dat <- data.frame(
  x = c(
    rnorm(1e4, mean = 0, sd = 0.1),
    rnorm(1e3, mean = 0, sd = 0.1)
  ),
  y = c(
    rnorm(1e4, mean = 0, sd = 0.1),
    rnorm(1e3, mean = 0.1, sd = 0.2)
  )
)
```

```{r ratioIntraInter, eval=FALSE}
counts = lapply(unique(gsub(" ", "_", do.call(paste, tstrsplit(sampleTable[, Label], "_")[1:3]))),
                function(grp){
                   
                   print(grp)
                   
                   ##### Raw #####
                   interactions = lapply(unique(sampleTable[grepl(grp, Label), Label]),
                                         function(lbl){
                                            temp = fread(paste0("gunzip -c ", working.folder, "processed/RNA-BIN_", lbl, "_", gencode_version, "_level_1_2_3_Unique_", bin_width, "_GenicOnly.tsv.gz"))
                                            setkeyv(temp, c("chr.R", "gene_id.R", "chr.D", "gene_id.D", "annotation.R", "gene_type.R"))
                                            temp = temp[, .N, by=key(temp)]
                                            setnames(temp, "N", "Count")
                                            temp[, c("Condition", "Replicate") := tstrsplit(lbl, "_")[3:4]]
                                            temp
                                         })
                   
                   interactions = rbindlist(interactions)
                   
                   
                   # interactions = fread(paste0("gunzip -c RNA-BIN_biotypes_", gencode_version, "_level_1_2_3_AllOverlapsUnique_", bin_width, ".tsv.gz"))
                   
                   interactions[, interactionType := ifelse(chr.R==chr.D, "Intrachromosomal", "Interchromosomal")]
                   
                   setkeyv(interactions, c("gene_id.R", "interactionType", "annotation.R", "Condition", "Replicate"))
                   subset = interactions[gene_id.R!=gene_id.D, .N, by=key(interactions)]
                   
                   subset = dcast.data.table(subset, Condition+Replicate+annotation.R+gene_id.R~interactionType, value.var="N", fill=1)
                   subset[, `:=`(Sum = log2(Interchromosomal+Intrachromosomal), Ratio = log2(Interchromosomal/Intrachromosomal))]
                   
                   # gg1 = ggplot(subset[Interchromosomal+Intrachromosomal>=10], aes(x=Biotype, y=Ratio, fill=as.factor(Replicate))) +
                   #    ggtitle("Interchromosomal vs intrachromosomal reads ratio", subtitle="A pseudo-count of 1 has been added to those interactions with 0 count in either intrachromosomal or interchromosomal fraction") +
                   #    geom_boxplot(outlier.size=0.5) + 
                   #    facet_grid(~Condition) +
                   #    scale_x_discrete("") +
                   #    scale_y_continuous("log2(Inter/Intra)") +
                   #    scale_fill_brewer("Replicate", palette="Set1") +
                   #    theme_bw() +
                   #    theme(legend.position="right", axis.text.x=element_text(angle = 45, hjust = 1))
                   # ggsave(gg1, filename="plots/RNA-BIN_Inter_vs_Intrachromosomal_interactions.png", units="in", width=12, height=4, points=10)
                   # 
                   # gg2 = ggplot(subset[Interchromosomal+Intrachromosomal>=10], aes(x=Biotype2, y=Ratio, fill=as.factor(Replicate))) +
                   #       ggtitle("Interchromosomal vs intrachromosomal reads ratio", subtitle="A pseudo-count of 1 has been added to those interactions with 0 count in either intrachromosomal or interchromosomal fraction\nlincRNA, processed_transcript, antisense, processed_pseudogene, unprocessed_pseudogene and misc_RNA were grouped under 'long ncRNA'") +
                   #    geom_boxplot(outlier.size=0.5) + 
                   #    facet_grid(~Condition) +
                   #    scale_x_discrete("") +
                   #    scale_y_continuous("log2(Inter/Intra)") +
                   #    scale_fill_brewer("Replicate", palette="Set1") +
                   #    theme_bw() +
                   #    theme(legend.position="right", axis.text.x=element_text(angle = 45, hjust = 1))
                   # ggsave(gg2, filename="plots/RNA-BIN_Inter_vs_Intrachromosomal_interactions_groupedBiotype.png", units="in", width=12, height=4, points=10)
                   # 
                   # png(paste0("plots/RNA-BIN_", bin_width, "_Inter_vs_Intrachromosomal_interactions_Biotypes.png"), units="in", height=10, width=16, res=300)
                   #    ggarrange(gg1, gg2, ncol=1, nrow=2)
                   # dev.off()
                   
                   counts = subset[is.finite(Ratio), list(Interchromosomal=sum(Interchromosomal), Intrachromosomal=sum(Intrachromosomal),
                                                          Sum=mean(Sum), Ratio=mean(Ratio)),
                                   by=c("Condition", "gene_id.R", "annotation.R")]
                   counts = counts[Interchromosomal+Intrachromosomal>=10,]
                   counts[, annotation.R := factor(annotation.R, levels=c("protein_coding", "long_ncRNA", "ncRNA", "NAST", "other"))]
                   
                   return(counts)
                   
                })
names(counts) = unique(gsub(" ", "_", do.call(paste, tstrsplit(sampleTable[, Label], "_")[1:3])))
counts = rbindlist(counts, idcol="Group")

gg1 = ggviolin(counts, x="annotation.R", y="Ratio", facet.by="Group") +
   ggtitle("Interchromosomal vs intrachromosomal reads ratio (Transcript collapsed)",
           subtitle="A pseudo-count of 1 has been added to those interactions with 0 count in either intrachromosomal or interchromosomal fraction") +
   stat_compare_means(ref.group="protein_coding", method="wilcox.test", hide.ns=TRUE, label = "p.signif") +
   scale_x_discrete("") + scale_y_continuous("log2(Inter/Intra)") +
   theme_bw() +
   theme(axis.text.x=element_text(angle=60, hjust=1, vjust=1, size=14),
         axis.text.y=element_text(size=14), legend.text=element_text(size=14),
         axis.title.y=element_text(size=14), legend.title=element_text(size=14),
         strip.text.x=element_text(size=14), strip.text.y=element_text(size=14),
         legend.position="bottom") +
   facet_wrap(~Group, nrow=1) + geom_boxplot(outlier.colour=NA, width=0.1)

counts[, Density := get_density(Sum, Ratio), by=c("Condition", "annotation.R")]
gg3 = ggplot(counts[Condition%in%"1FA"], aes(x=Sum, y=Ratio, col=Density)) +
   ggtitle("Sum and ration of Interchromosomal and intrachromosomal raw reads (Transcript collapsed)",
           subtitle="A pseudo-count of 1 has been added to those interactions with 0 count in either intrachromosomal or interchromosomal fraction\nlincRNA, processed_transcript, antisense, processed_pseudogene and unprocessed_pseudogene were grouped under 'lncRNA'") +
   geom_point(size=0.5, alpha=0.5) + scale_colour_viridis(option="plasma") + 
   geom_smooth(method="lm", se=FALSE, col="red", lwd=1) +
   scale_y_continuous("Ratio\nlog2(interchromosomal/intrachromosomal)") +
   scale_x_continuous("Sum\nlog2((interchromosomal+intrachromosomal)/width*1000)") +
   facet_wrap(~annotation.R, ncol=5, drop=FALSE) + theme_bw() + theme(legend.position="bottom")

# counts = subset[is.finite(Ratio), list(Interchromosomal=sum(Interchromosomal), Intrachromosomal=sum(Intrachromosomal), Sum=mean(Sum), Ratio=mean(Ratio)), by=c("Condition", "gene_id.R", "Biotype2")]
# counts = counts[Interchromosomal+Intrachromosomal>=10]
# 
# gg2 = ggviolin(counts, x="Biotype2", y="Ratio", facet.by="Condition") +
#    ggtitle("Interchromosomal vs intrachromosomal reads ratio (Transcript collapsed)",
#            subtitle="A pseudo-count of 1 has been added to those interactions with 0 count in either intrachromosomal or interchromosomal fraction\nlincRNA, processed_transcript, antisense, processed_pseudogene and unprocessed_pseudogene were grouped under 'lncRNA'") +
#    stat_compare_means(ref.group="protein_coding", method="wilcox.test", hide.ns=TRUE, label = "p.signif") +
#    scale_x_discrete("") + scale_y_continuous("log2(Inter/Intra)") +
#    theme_bw() +
#    theme(axis.text.x=element_text(angle=60, hjust=1, vjust=1, size=14),
#          axis.text.y=element_text(size=14), legend.text=element_text(size=14),
#          axis.title.y=element_text(size=14), legend.title=element_text(size=14),
#          strip.text.x=element_text(size=14), strip.text.y=element_text(size=14),
#          legend.position="bottom") +
#    facet_wrap(~Condition, nrow=2) + geom_boxplot(outlier.colour=NA, width=0.1)

pdf(paste0(working.folder, "plots/RNA-BIN_", bin_width, "_Inter_vs_Intrachromosomal_interactions_Biotypes_perTranscript.pdf"), height=12, width=18)
   ggarrange(gg1, gg3, ncol=1, nrow=2)
dev.off()

gg2.1 = ggviolin(counts[Condition%like%"1FA" & Sum>5], x="annotation.R", y="Ratio", facet.by="Condition") +
   ggtitle("Interchromosomal vs intrachromosomal reads ratio (Transcript collapsed)",
           subtitle="A pseudo-count of 1 has been added to those interactions with 0 count in either intrachromosomal or interchromosomal fraction\nlincRNA, processed_transcript, antisense, processed_pseudogene and unprocessed_pseudogene were grouped under 'lncRNA'") +
   stat_compare_means(ref.group="protein_coding", method="wilcox.test", hide.ns=TRUE, label = "p.signif") +
   scale_x_discrete("") + scale_y_continuous("log2(Inter/Intra)") +
   theme_bw() +
   theme(axis.text.x=element_text(angle=60, hjust=1, vjust=1, size=14),
         axis.text.y=element_text(size=14), legend.text=element_text(size=14),
         axis.title.y=element_text(size=14), legend.title=element_text(size=14),
         strip.text.x=element_text(size=14), strip.text.y=element_text(size=14),
         legend.position="bottom") +
   facet_wrap(~Condition) + geom_boxplot(outlier.colour=NA, width=0.1)
ggsave(gg2.1, filename=paste0(working.folder, "plots/RNA-BIN_", bin_width, "_Inter_vs_Intrachromosomal_interactions_Biotypes_perTranscript_1FA.png"), height=8, width=10)
ggsave(gg2.1, filename=paste0(working.folder, "plots/RNA-BIN_", bin_width, "_Inter_vs_Intrachromosomal_interactions_Biotypes_perTranscript_1FA.pdf"), height=8, width=10)


# Significant
# interactions = fread(paste0("gunzip -c RNA-BIN_counts_", gencode_version, "_level_1_2_3_AllOverlapsUnique_", bin_width, "_filtered_extended.tsv.gz"))
files.sig = list.files(paste0(working.folder, "processed"), pattern="*_cnd_*", full.names=TRUE)

significant = rbindlist(lapply(files.sig,
                               function(x)
                                  fread(paste("gunzip -c", x), showProgress=FALSE)))
setnames(significant, "replicate", "Replicate")

significant[, interactionType := ifelse(chr.R==chr.D, "Intrachromosomal", "Interchromosomal")]

significant[, chrBins := length(unique(gene_id.D)), by="chr.D"][chr.R!=chr.D, chrBins := NA]

setkeyv(significant, c("gene_id.R", "chr.R", "chr.D", "Condition", "Replicate"))
chr = significant[, list(IntraBinFrac=length(unique(gene_id.D))/unique(na.omit(chrBins))), by=key(significant)]
chr = chr[chr.R==chr.D,][!chr.R%in%c("chrY", "chrM"),]
chr[, `:=`(chr.D=NULL)]
setnames(chr, "chr.R", "chr")
setkeyv(chr, c("Condition", "Replicate", "gene_id.R"))

setkeyv(significant, c("gene_id.R", "interactionType", "Condition", "Replicate"))
subset = significant[, .N, by=key(significant)]

rm(significant)
gc()

subset = dcast.data.table(subset, Condition+Replicate+gene_id.R~interactionType, value.var="N", fill=1)
setkey(subset, gene_id.R)
setkey(genes.dt, gene_id)
subset[genes.dt, width := width]
subset[, `:=`(Sum = log2((Interchromosomal+Intrachromosomal)/width*1e3), Ratio = log2(Interchromosomal/Intrachromosomal))]

setkey(subset, gene_id.R)
setkey(genes.dt, gene_id)
subset[genes.dt, Biotype := gene_type]
subset[, Biotype2 := Biotype]

counts = copy(subset)
setkeyv(counts, key(chr))
counts = counts[chr, nomatch=0][, Biotype2 := NULL]
fwrite(counts[order(Ratio, IntraBinFrac, decreasing=TRUE)], file=paste0(working.folder, "RNA-BIN_", bin_width, "_Inter_vs_Intrachromosomal_interactions_Biotypes_Significant.tsv"), quote=FALSE, sep="\t")

subset[Biotype%in%c("lincRNA", "processed_transcript", "antisense", "processed_pseudogene", "unprocessed_pseudogene"), Biotype2 := "lncRNA"]
subset[, Biotype  := factor(Biotype,  levels=c("protein_coding", "lincRNA", "NAST", "processed_transcript", "antisense", "processed_pseudogene", "unprocessed_pseudogene", "misc_RNA", "miRNA", "snRNA", "snoRNA"))]
subset[, Biotype2 := factor(Biotype2, levels=c("protein_coding", "lncRNA", "NAST", "miRNA", "misc_RNA", "snRNA", "snoRNA"))]

# gg1 = ggplot(subset[Interchromosomal+Intrachromosomal>=10], aes(x=Biotype, y=Ratio, fill=as.factor(Replicate))) +
#    ggtitle("Interchromosomal vs intrachromosomal reads ratio", subtitle="A pseudo-count of 1 has been added to those interactions with 0 count in either intrachromosomal or interchromosomal fraction") +
#    geom_boxplot(outlier.size=0.5) + 
#    facet_grid(~Condition) +
#    scale_x_discrete("") +
#    scale_y_continuous("log2(Inter/Intra)") +
#    scale_fill_brewer("Replicate", palette="Set1") +
#    theme_bw() +
#    theme(legend.position="right", axis.text.x=element_text(angle = 45, hjust = 1))
# 
# gg2 = ggplot(subset[Interchromosomal+Intrachromosomal>=10], aes(x=Biotype2, y=Ratio, fill=as.factor(Replicate))) +
#       ggtitle("Interchromosomal vs intrachromosomal reads ratio", subtitle="A pseudo-count of 1 has been added to those interactions with 0 count in either intrachromosomal or interchromosomal fraction\nlincRNA, processed_transcript, antisense, processed_pseudogene, unprocessed_pseudogene and misc_RNA were grouped under 'long ncRNA'") +
#    geom_boxplot(outlier.size=0.5) + 
#    facet_grid(~Condition) +
#    scale_x_discrete("") +
#    scale_y_continuous("log2(Inter/Intra)") +
#    scale_fill_brewer("Replicate", palette="Set1") +
#    theme_bw() +
#    theme(legend.position="right", axis.text.x=element_text(angle = 45, hjust = 1))
# 
# png(paste0("plots/RNA-BIN_", bin_width, "_Inter_vs_Intrachromosomal_interactions_Biotypes_filtered.png"), units="in", height=10, width=16, res=300)
#    ggarrange(gg1, gg2, ncol=1, nrow=2)
# dev.off()

counts = subset[is.finite(Ratio), list(Interchromosomal=sum(Interchromosomal), Intrachromosomal=sum(Intrachromosomal), Sum=mean(Sum), Ratio=mean(Ratio)), by=c("Condition", "gene_id.R", "Biotype")]
counts = counts[Interchromosomal+Intrachromosomal>=10]

gg1 = ggviolin(counts, x="Biotype", y="Ratio", facet.by="Condition") +
   ggtitle("Interchromosomal vs intrachromosomal reads ratio (Transcript collapsed)", subtitle="A pseudo-count of 1 has been added to those interactions with 0 count in either intrachromosomal or interchromosomal fraction") +
   stat_compare_means(ref.group="protein_coding", method="wilcox.test", hide.ns=TRUE, label = "p.signif") +
   scale_x_discrete("") + scale_y_continuous("log2(Inter/Intra)") +
   theme_bw() +
   theme(axis.text.x=element_text(angle=60, hjust=1, vjust=1, size=14),
         axis.text.y=element_text(size=14), legend.text=element_text(size=14),
         axis.title.y=element_text(size=14), legend.title=element_text(size=14),
         strip.text.x=element_text(size=14), strip.text.y=element_text(size=14),
         legend.position="bottom") +
   facet_wrap(~Condition, nrow=1) + geom_boxplot(outlier.colour=NA, width=0.1)

# counts[grepl("1FA", Condition), Density := get_density(Sum, Ratio), by=c("Condition", "Biotype")]
# gg4 = ggplot(counts[Condition%in%"1FA"], aes(x=Sum, y=Ratio, col=Density)) +
#    ggtitle("Sum and ration of Interchromosomal and intrachromosomal filtered reads (Transcript collapsed)",
#            subtitle="A pseudo-count of 1 has been added to those interactions with 0 count in either intrachromosomal or interchromosomal fraction\nlincRNA, processed_transcript, antisense, processed_pseudogene and unprocessed_pseudogene were grouped under 'lncRNA'") +
#    geom_point(size=0.5, alpha=0.5) + scale_colour_viridis(option="plasma") + 
#    geom_smooth(method="lm", se=FALSE, col="red", lwd=1) +
#    scale_y_continuous("Ratio\nlog2(interchromosomal/intrachromosomal)") +
#    scale_x_continuous("Sum\nlog2((interchromosomal+intrachromosomal)/width*1000)") +
#    facet_wrap(~Biotype, ncol=7, drop=FALSE) + theme_bw() + theme(legend.position="bottom")

counts = subset[is.finite(Ratio), list(Interchromosomal=sum(Interchromosomal), Intrachromosomal=sum(Intrachromosomal), Sum=mean(Sum), Ratio=mean(Ratio)), by=c("Condition", "gene_id.R", "Biotype2")]
counts = counts[Interchromosomal+Intrachromosomal>=10]

gg2 = ggviolin(counts, x="Biotype2", y="Ratio", facet.by="Condition") + 
      ggtitle("Interchromosomal vs intrachromosomal reads ratio (Transcript collapsed)",
              subtitle="A pseudo-count of 1 has been added to those interactions with 0 count in either intrachromosomal or interchromosomal fraction\nlincRNA, processed_transcript, antisense, processed_pseudogene and unprocessed_pseudogene were grouped under 'lncRNA'") +
   stat_compare_means(ref.group="protein_coding", method="wilcox.test", hide.ns=TRUE, label = "p.signif") +
   scale_x_discrete("") + scale_y_continuous("log2(Inter/Intra)") +
   theme_bw() +
   theme(axis.text.x=element_text(angle=60, hjust=1, vjust=1, size=14),
         axis.text.y=element_text(size=14), legend.text=element_text(size=14),
         axis.title.y=element_text(size=14), legend.title=element_text(size=14),
         strip.text.x=element_text(size=14), strip.text.y=element_text(size=14),
         legend.position="bottom") +
   facet_wrap(~Condition, nrow=1) + geom_boxplot(outlier.colour=NA, width=0.1)

png(paste0(working.folder, "plots/RNA-BIN_", bin_width, "_Inter_vs_Intrachromosomal_interactions_Biotypes_perTranscript_significant.png"), units="in", height=16, width=20, res=300)
   ggarrange(gg1, gg2, ncol=1, nrow=2)
dev.off()

gg2.1 = ggviolin(counts[Condition%like%"1FA"], x="Biotype2", y="Ratio", facet.by="Condition") +
   ggtitle("Interchromosomal vs intrachromosomal reads ratio (Transcript collapsed)",
           subtitle="A pseudo-count of 1 has been added to those interactions with 0 count in either intrachromosomal or interchromosomal fraction\nlincRNA, processed_transcript, antisense, processed_pseudogene and unprocessed_pseudogene were grouped under 'lncRNA'") +
   stat_compare_means(ref.group="protein_coding", method="wilcox.test", hide.ns=TRUE, label = "p.signif") +
   scale_x_discrete("") + scale_y_continuous("log2(Inter/Intra)") +
   theme_bw() +
   theme(axis.text.x=element_text(angle=60, hjust=1, vjust=1, size=14),
         axis.text.y=element_text(size=14), legend.text=element_text(size=14),
         axis.title.y=element_text(size=14), legend.title=element_text(size=14),
         strip.text.x=element_text(size=14), strip.text.y=element_text(size=14),
         legend.position="bottom") +
   facet_wrap(~Condition, nrow=1) + geom_boxplot(outlier.colour=NA, width=0.1)
ggsave(gg2.1, filename=paste0(working.folder, "plots/RNA-BIN_", bin_width, "_Inter_vs_Intrachromosomal_interactions_Biotypes_perTranscript_significant_1FA.png"), height=6, width=16)

# png(paste0(working.folder, "plots/RNA-BIN_", bin_width, "_Inter_vs_Intrachromosomal_interactions_Biotypes_Raw_and_Filtered_1FA.png"), units="in", height=16, width=16, res=300)
#    ggarrange(gg3, gg4, ncol=1, nrow=2)
# dev.off()
```

```{r complexHeatmap_1MB, eval=FALSE}
files.raw = list.files(file.path(working.folder, "processed"), pattern="*_GenicOnly.tsv.gz", full.names=TRUE)

files.raw = files.raw[grepl("1FA", files.raw)]

for( ce in c("mESC", "mOPC") ){
   
   interactions = lapply(files.raw[grepl(ce, files.raw)],
                         function(x){
                            temp = fread(paste("gunzip -c", x))
                            temp[!(gene_id.R%in%genes.dt[seqnames%in%"chrM", gene_id] | chr.D%in%"chrM"),]
                         })
   
   interactions = rbindlist(interactions)
   
   interactions.gr = with(interactions[, list(chr.D, start.D, end.D)], GRanges(chr.D, IRanges(start.D, end.D)))
   
   mm1MB.dt = fread(paste0(working.folder, "GRCm38.genome.1MB.bed"), col.names=c("seqnames", "start", "end", "genomic_bin"))
   mm1MB.dt[, bin_id := paste(seqnames, genomic_bin, sep="_")]
   mm1MB.dt = mm1MB.dt[, index := seq_along(genomic_bin)]
   mm1MB = with(mm1MB.dt, GRanges(seqnames, IRanges(start, end)))
   
   overlaps = findOverlaps(interactions.gr, mm1MB, select="first")
   rm(interactions.gr, mm1MB); gc()
   
   interactions[, gene_id.D := mm1MB.dt[overlaps, bin_id]]
   rm(overlaps); gc()
   
   setkeyv(interactions, c("gene_id.R", "gene_id.D"))
   interactions = interactions[, list(Count = .N), by=key(interactions)]
   
   setkey(interactions, gene_id.R)
   interactions[, `:=`(rowSum = sum(Count), rowMax = max(Count)), by=key(interactions)]
   
   rna.counts = interactions[, list(sum(Count)), by=key(interactions)]
   setkey(genes.dt, gene_id)
   setkey(rna.counts, gene_id.R)
   rna.counts[genes.dt, width := width][, rpk := V1/width*1e3]
   rna.rna.sel = rna.counts[rpk>=100, gene_id.R] # greater equal than 100 reads per kb of the gene body
   rna.dna.sel = interactions[Count/1e6*1e3>=10, gene_id.R] # greater equal than 10 reads per kb of the 1Mb bin
   
   interactions = interactions[gene_id.R%in%c(rna.rna.sel, rna.dna.sel),]
   
   genes.subset = genes.dt[gene_id%in%c(rna.rna.sel, rna.dna.sel),][order(index),]
   genes.subset[, index := seq_along(gene_id)]
   setkey(genes.subset, gene_id)
   
   setkey(interactions, gene_id.R)
   interactions[genes.subset, Rindex := index]
   
   setkey(interactions, gene_id.D)
   setkey(mm1MB.dt, bin_id)
   interactions = interactions[mm1MB.dt, Cindex := index]
   
   interactions[, `:=`(Norm = Count/rowMax, Dens = Count/rowSum, 
                       logCount = log2(Count), logNorm = log10(Count)-log10(rowMax), logDens = log10(Count)-log10(rowSum))]
   
   interactions[, `:=`(Rindex = factor(Rindex, levels=1:max(Rindex)), Cindex = factor(Cindex, levels=1:max(Cindex)))]
   interactions = interactions[order(Rindex, Cindex)]
   
   # big.matrix = dcast.data.table(interactions, Rindex~Cindex, value.var="logNorm", fill=0, drop=FALSE)
   big.matrix = dcast.data.table(interactions, Rindex~Cindex, value.var="logCount", fill=0, drop=FALSE)
   
   rm(interactions)
   gc()
   
   big.matrix = data.frame(big.matrix, row.names=1)
   
   big.matrix = as.matrix(big.matrix)
   
   rotate <- function(x) t(apply(x, 2, rev))
   
   big.matrix = rotate(big.matrix)
   
   # big.matrix = rotate(rotate(big.matrix))
   
   rownames(big.matrix) = NULL
   tsc.all = genes.subset[, list(gene_name, index)]
   tsc.all[, columnIndex := match(index, colnames(big.matrix))]
   tsc.all = tsc.all[order(-columnIndex)][!is.na(columnIndex)]
   tsc.sel = genes.subset[gene_name%in%c("Malat1", "Wwox", "Msi2", "Snord118", "Neat1"), list(gene_name, index)]
   tsc.sel[, columnIndex := match(index, colnames(big.matrix))]
   colnames(big.matrix) = NULL
   # colnames(big.matrix)[tsc.sel[, columnIndex]] = tsc.sel[, gene_name]
   
   big.matrix[!is.finite(big.matrix) | is.na(big.matrix)] = 0
   
   split_fact_row = mm1MB.dt[order(index), list(chr=seqnames, index)][1:nrow(big.matrix)]
   split_fact_row = split_fact_row[, list(chr = factor(chr, levels=unique(chr)))]
   
   ha = HeatmapAnnotation(link = column_anno_link(at = tsc.sel[, columnIndex], labels = tsc.sel[, gene_name]), width = unit(1, "in") + max_text_width(tsc.sel[, gene_name]))
   
   require("fields")
   
   # png(paste0("plots/Heatmap_1FA_genomic_1MB.png"), units="in", type="cairo-png", res=300, width=20, height=18)
   pdf(paste0(working.folder, "plots/Heatmap_", ce, "_1FA_genomic_1MB_count.pdf"), width=20, height=19)
   # Heatmap(big.matrix, name="Normalised binding", row_title="RNA (genes)", column_title="DNA (1MB bins)", row_title_side="right", column_title_side="bottom",
   print(Heatmap(big.matrix, name="Interaction count (log2)", column_title="", row_title="", row_title_side="right", column_title_side="top",
                 show_column_names=FALSE, show_row_names=FALSE, top_annotation = ha,
                 split=split_fact_row, gap = unit(1, "mm"), row_names_gp = gpar(fontsize = 10),
                 # col=colorRamp2(c(0, 1, 10, 100, 200), c('#253494', '#ffffb2', '#feb24c', '#fc4e2a', '#b10026')),
                 # col=colorRamp2(c(0, quantile(big.matrix[big.matrix!=0], seq(0, 1, by = 0.25))), inferno(6)),
                 # col=rev(colorRampPalette(brewer.pal(10, "RdYlBu"))(256)),
                 col=colorRampPalette(c("#303f9f", "#fbc02d","#d32f2f"))(256),
                 # heatmap_legend_param=list(color_bar="discrete", at=seq(0, 1.0, 0.25)),
                 cluster_rows=FALSE, 
                 cluster_columns=FALSE,
                 use_raster=TRUE, raster_quality=9, raster_device="CairoPNG"))
   dev.off()
   
   
   for( tsc in tsc.sel[, gene_name] ){
      flank.tsc = 5
      colIndex = tsc.sel[gene_name%in%tsc, columnIndex]
      small.matrix = big.matrix[, (colIndex-flank.tsc):(colIndex+flank.tsc)]
      ha = HeatmapAnnotation(link = column_anno_link(at = (2*flank.tsc+1):1,
                                                     labels = tsc.all[columnIndex%in%(colIndex-flank.tsc):(colIndex+flank.tsc), gene_name]),
                             height = unit(0.1, "in") + max_text_width(tsc.all[columnIndex%in%(colIndex-flank.tsc):(colIndex+flank.tsc), gene_name]))
      # png(paste0("plots/Heatmap_1FA_genomic_1MB.png"), units="in", type="cairo-png", res=300, width=20, height=18)
      pdf(paste0(working.folder, "plots/Heatmap_", ce, "_1FA_genomic_1MB_", tsc, "_count.pdf"), width=6, height=20)
      # Heatmap(big.matrix, name="Normalised binding", row_title="RNA (genes)", column_title="DNA (1MB bins)", row_title_side="right", column_title_side="bottom",
      print(Heatmap(small.matrix, name="Interaction count (log2)", column_title="", row_title="", row_title_side="right", column_title_side="top",
                    show_column_names=FALSE, show_row_names=FALSE, top_annotation = ha,
                    split=split_fact_row, gap = unit(1, "mm"), row_names_gp = gpar(fontsize = 10),
                    # col=colorRamp2(c(0, 1, 10, 100, 200), c('#253494', '#ffffb2', '#feb24c', '#fc4e2a', '#b10026')),
                    # col=colorRamp2(c(0, quantile(big.matrix[big.matrix!=0], seq(0, 1, by = 0.25))), inferno(6)),
                    # col=rev(colorRampPalette(brewer.pal(10, "RdYlBu"))(256)),
                    col=colorRampPalette(c("#303f9f", "#fbc02d","#d32f2f"))(256),
                    # heatmap_legend_param=list(color_bar="discrete", at=seq(0, 1.0, 0.25)),
                    cluster_rows=FALSE, 
                    cluster_columns=FALSE,
                    use_raster=TRUE, raster_quality=9, raster_device="CairoPNG"))
      dev.off()
   }
}
```

```{r complexHeatmap_1MB_significant, eval=FALSE}
rna.th = 100
dna.th = 10

for( ce in c("mESC", "mOPC") ){
   
   significant = fread(paste0("gunzip -c ", working.folder, "processed/RNA-BIN_RADICL_", ce, "_1FA_", gencode_version, "_level_1_2_3_Unique_", bin_width, "_significant", fdr.th, "_extended_cnd_noDistance.tsv.gz"),
                       showProgress=FALSE)
   setnames(significant, "replicate", "Replicate")
   
   significant = significant[!(gene_id.R%in%genes.dt[seqnames%in%"chrM", gene_id] | chr.D%in%"chrM")]
   
   significant.gr = with(significant[, list(chr.D, start.D, end.D)], GRanges(chr.D, IRanges(start.D, end.D)))
   
   mm1MB.dt = fread(paste0(working.folder, "GRCm38.genome.1MB.bed"), col.names=c("seqnames", "start", "end", "genomic_bin"))
   mm1MB.dt[, bin_id := paste(seqnames, genomic_bin, sep="_")]
   mm1MB.dt = mm1MB.dt[, index := seq_along(genomic_bin)]
   mm1MB = with(mm1MB.dt, GRanges(seqnames, IRanges(start, end)))
   
   overlaps = findOverlaps(significant.gr, mm1MB, select="first")
   rm(significant.gr, mm1MB); gc()
   
   significant[, gene_id.D := mm1MB.dt[overlaps, bin_id]]
   rm(overlaps); gc()
   
   setkeyv(significant, c("gene_id.R", "gene_id.D"))
   significant = significant[, list(Count = .N), by=key(significant)]
   
   setkey(significant, gene_id.R)
   significant[, `:=`(rowSum = sum(Count), rowMax = max(Count)), by=key(significant)]
   
   rna.counts = significant[, list(sum(Count)), by=key(significant)]
   setkey(genes.dt, gene_id)
   setkey(rna.counts, gene_id.R)
   rna.counts[genes.dt, width := width][, rpk := V1/width*1e3]
   rna.rna.sel = rna.counts[rpk>=rna.th, gene_id.R] # greater equal than 100 reads per kb of the gene body
   rna.dna.sel = significant[Count/1e6*1e3>=dna.th, gene_id.R] # greater equal than 10 reads per kb of the 1Mb bin
   
   significant = significant[gene_id.R%in%c(rna.rna.sel, rna.dna.sel),]
   
   genes.subset = genes.dt[gene_id%in%c(rna.rna.sel, rna.dna.sel),]
   genes.subset = genes.subset[order(index),]
   genes.subset[, index := seq_along(gene_id)]
   setkey(genes.subset, gene_id)
   
   setkey(significant, gene_id.R)
   significant[genes.subset, Rindex := index]
   
   setkey(significant, gene_id.D)
   # mm1MB.dt = mm1MB.dt[bin_id%in%significant[, gene_id.D],]
   # mm1MB.dt[, index := seq_along(index)]
   setkey(mm1MB.dt, bin_id)
   significant = significant[mm1MB.dt, Cindex := index]
   
   # significant[, `:=`(Norm = Count/rowMax, Dens = Count/rowSum, 
   #                     logCount = log2(Count), logNorm = log10(Count)-log10(rowMax), logDens = log10(Count)-log10(rowSum))]
   significant[, logCount := log2(Count)]
   
   significant[, `:=`(Rindex = factor(Rindex, levels=1:max(Rindex)), Cindex = factor(Cindex, levels=1:max(Cindex)))]
   significant = significant[order(Rindex, Cindex)]
   
   # big.matrix = dcast.data.table(interactions, Rindex~Cindex, value.var="logNorm", fill=0, drop=FALSE)
   big.matrix = dcast.data.table(significant, Rindex~Cindex, value.var="logCount", fill=0, drop=FALSE)
   
   rm(significant)
   gc()
   
   big.matrix = data.frame(big.matrix, row.names=1)
   
   big.matrix = as.matrix(big.matrix)
   
   rotate <- function(x) t(apply(x, 2, rev))
   
   big.matrix = rotate(big.matrix)
   
   # big.matrix = rotate(rotate(big.matrix))
   
   rownames(big.matrix) = NULL
   tsc.all = genes.subset[, list(gene_name, index)]
   tsc.all[, columnIndex := match(index, colnames(big.matrix))]
   tsc.all = tsc.all[order(-columnIndex)][!is.na(columnIndex)]
   tsc.sel = genes.subset[gene_name%in%c("Malat1", "Wwox", "Msi2", "Snord118", "Neat1"), list(gene_name, index)]
   tsc.sel[, columnIndex := match(index, colnames(big.matrix))]
   colnames(big.matrix) = NULL
   # colnames(big.matrix)[tsc.sel[, columnIndex]] = tsc.sel[, gene_name]
   
   big.matrix[!is.finite(big.matrix) | is.na(big.matrix)] = 0
   
   split_fact_row = mm1MB.dt[order(index), list(chr=seqnames, index)][1:nrow(big.matrix)]
   split_fact_row = split_fact_row[, list(chr = factor(chr, levels=unique(chr)))]
   
   ha = HeatmapAnnotation(link = column_anno_link(at = tsc.sel[, columnIndex], labels = tsc.sel[, gene_name]), width = unit(1, "in") + max_text_width(tsc.sel[, gene_name]))
   
   # png(paste0("plots/Heatmap_1FA_genomic_1MB.png"), units="in", type="cairo-png", res=300, width=20, height=18)
   pdf(paste0(working.folder, "plots/Heatmap_", ce, "_1FA_genomic_1MB_count_RNA", rna.th, "_DNA", dna.th, "_significant.pdf"), width=20, height=19)
   # Heatmap(big.matrix, name="Normalised binding", row_title="RNA (genes)", column_title="DNA (1MB bins)", row_title_side="right", column_title_side="bottom",
   print(Heatmap(big.matrix, name="Interaction count (log2)", column_title="", row_title="", row_title_side="right", column_title_side="top",
                 show_column_names=FALSE, show_row_names=FALSE, top_annotation = ha,
                 split=split_fact_row, gap = unit(1, "mm"), row_names_gp = gpar(fontsize = 10),
                 # col=colorRamp2(c(0, 1, 10, 100, 200), c('#253494', '#ffffb2', '#feb24c', '#fc4e2a', '#b10026')),
                 # col=colorRamp2(c(0, quantile(big.matrix[big.matrix!=0], seq(0, 1, by = 0.25))), inferno(6)),
                 # col=rev(colorRampPalette(brewer.pal(10, "RdYlBu"))(256)),
                 col=colorRampPalette(c("#303f9f", "#fbc02d","#d32f2f"))(256),
                 # heatmap_legend_param=list(color_bar="discrete", at=seq(0, 1.0, 0.25)),
                 cluster_rows=FALSE, 
                 cluster_columns=FALSE,
                 use_raster=TRUE, raster_quality=9, raster_device="CairoPNG"))
   dev.off()
   
   for( tsc in tsc.sel[, gene_name] ){
      flank.tsc = 5
      colIndex = tsc.sel[gene_name%in%tsc, columnIndex]
      small.matrix = big.matrix[, (colIndex-flank.tsc):(colIndex+flank.tsc)]
      ha = HeatmapAnnotation(link = column_anno_link(at = (2*flank.tsc+1):1,
                                                     labels = tsc.all[columnIndex%in%(colIndex-flank.tsc):(colIndex+flank.tsc), gene_name]),
                             height = unit(0.1, "in") + max_text_width(tsc.all[columnIndex%in%(colIndex-flank.tsc):(colIndex+flank.tsc), gene_name]))
      # png(paste0("plots/Heatmap_1FA_genomic_1MB.png"), units="in", type="cairo-png", res=300, width=20, height=18)
      pdf(paste0(working.folder, "plots/Heatmap_", ce, "1FA_genomic_1MB_", tsc, "_count_RNA", rna.th, "_DNA", dna.th, "_significant.pdf"), width=6, height=20)
      # Heatmap(big.matrix, name="Normalised binding", row_title="RNA (genes)", column_title="DNA (1MB bins)", row_title_side="right", column_title_side="bottom",
      print(Heatmap(small.matrix, name="Interaction count (log2)", column_title="", row_title="", row_title_side="right", column_title_side="top",
                    show_column_names=FALSE, show_row_names=FALSE, top_annotation = ha,
                    split=split_fact_row, gap = unit(1, "mm"), row_names_gp = gpar(fontsize = 10),
                    # col=colorRamp2(c(0, 1, 10, 100, 200), c('#253494', '#ffffb2', '#feb24c', '#fc4e2a', '#b10026')),
                    # col=colorRamp2(c(0, quantile(big.matrix[big.matrix!=0], seq(0, 1, by = 0.25))), inferno(6)),
                    # col=rev(colorRampPalette(brewer.pal(10, "RdYlBu"))(256)),
                    col=colorRampPalette(c("#303f9f", "#fbc02d","#d32f2f"))(256),
                    # heatmap_legend_param=list(color_bar="discrete", at=seq(0, 1.0, 0.25)),
                    cluster_rows=FALSE, 
                    cluster_columns=FALSE,
                    use_raster=TRUE, raster_quality=9, raster_device="CairoPNG"))
      dev.off()
   }
}
```

<!-- ```{r, eval=FALSE} -->
<!-- ha = HeatmapAnnotation(link = column_anno_link(at = 20, labels = "Ciao"), width = unit(1, "cm") + max_text_width(tsc.sel[, gene_name])) -->

<!-- ha = HeatmapAnnotation(link = column_anno_link(at = tsc.sel[, columnIndex], labels = tsc.sel[, gene_name]), width = unit(1, "in") + max_text_width(tsc.sel[, gene_name])) -->

<!-- pdf(paste0("plots/Heatmap_1FA_genomic_1MB_small.pdf"), width=20, height=18) -->
<!-- # Heatmap(big.matrix, name="Normalised binding", row_title="RNA (genes)", column_title="DNA (1MB bins)", row_title_side="right", column_title_side="bottom", -->
<!-- Heatmap(big.matrix[1:100, 1:51787], name="Normalised binding", column_title="", row_title="", row_title_side="left", column_title_side="bottom", show_column_names=FALSE, show_row_names=FALSE, top_annotation = ha, -->
<!--         split=split_fact_row[1:100], gap = unit(1, "mm"), row_names_gp = gpar(fontsize = 10), -->
<!--         # col=colorRamp2(c(0, 1, 10, 100, 200), c('#253494', '#ffffb2', '#feb24c', '#fc4e2a', '#b10026')), -->
<!--         col=colorRamp2(c(0, quantile(big.matrix[big.matrix!=0], seq(0, 1, by = 0.25))), inferno(6)), -->
<!--         # heatmap_legend_param=list(color_bar="discrete", at=seq(0, 1.0, 0.25)), -->
<!--         cluster_rows=FALSE,  -->
<!--         cluster_columns=FALSE, -->
<!--         use_raster=FALSE) -->
<!-- # , raster_device="CairoPNG") -->
<!-- dev.off() -->

<!-- ``` -->

<!-- ### IGV -->

<!-- ```{r IGV, eval=FALSE} -->
<!-- regions = list(Tfcp2l1 = GRanges("chr1", IRanges(118236095, 119565783), name="ENSMUSG00000026380.10"), -->
<!--                Nanog = GRanges("chr6", IRanges(122619443, 122785306), name="ENSMUSG00000012396.12"), -->
<!--                Kcnq1ot1 = GRanges("chr7", IRanges(142759759, 143749117), name="ENSMUSG00000101609.1")) -->

<!-- interactions = fread(paste0("gunzip -c RNA-BIN_interactions_", gencode_version, "_level_1_2_3_AllOverlapsUnique_", bin_width, ".tsv.gz")) -->
<!-- interactions = interactions[!(chr.R%in%"chrM" | grepl("chrM", gene_id.D)),] -->

<!-- subset = lapply(regions, -->
<!--                 function(x) -->
<!--                    with(interactions[gene_id.R%in%x$name,], -->
<!--                         GRanges(chr.D, IRanges(start.D, end.D), Condition=Condition), x)) -->

<!-- covList = lapply(seq_along(subset), function(x){ -->
<!--    lapply(unique(subset[[x]]$Condition), function(i){ -->
<!--       coverage(keepSeqlevels(subset[[x]][subset[[x]]$Condition%in%i,], -->
<!--                              paste0("chr", c(1:19, "X", "Y")), pruning.mode="coarse")) -->
<!--    }) -->
<!-- }) -->
<!-- for( i in seq_along(covList) ){ -->
<!--    names(covList[[i]]) = unique(subset[[1]]$Condition) -->
<!-- } -->
<!-- names(covList) = names(subset) -->

<!-- dt = as.data.table(covList[[2]][[4]][regions[[2]]]) -->
<!-- dt[, pos := 1:nrow(dt)] -->


<!-- require("wiggleplotr") -->

<!-- igv.genes = lapply(regions, -->
<!--                    function(x) -->
<!--                       subsetByOverlaps(genes, x)) -->
<!-- igv.genes = lapply(igv.genes, -->
<!--                    function(x) -->
<!--                       keepSeqlevels(with(gff[gene_id%in%x$gene_id & !(gene_feature%in%"intron")], -->
<!--                                          GRanges(seqnames, IRanges(start, end), strand, gene_id, gene_name, gene_feature)), -->
<!--                                     paste0("chr", c(1:19, "X", "Y")), pruning.mode="coarse")) -->
<!-- igv.genes = lapply(igv.genes, -->
<!--                    function(x) -->
<!--                       split(x, x$gene_name)) -->

<!-- sample_data = dplyr::data_frame( -->
<!--    sample_id = c("1FA", "4hActD", "Control1"),  -->
<!--    condition = factor(c("1FA", "4hActD", "Control1"), levels = c("1FA", "4hActD", "Control1")),  -->
<!--    scaling_factor = 1, -->
<!--    bigWig = paste0(getwd(), "/bigwig/", "Tfcp2l1", c("_1FA", "_4hActD", "_Control1"), ".bw")) -->

<!-- track_data = dplyr::mutate(sample_data, track_id = condition, colour_group = condition) -->

<!-- sample_data = dplyr::data_frame( -->
<!--    sample_id = c("1FA"),  -->
<!--    condition = factor(c("1FA"), levels = c("1FA")),  -->
<!--    scaling_factor = 1, -->
<!--    bigWig = "~/Desktop/wig/siMatr3.3.fwd.bw") -->

<!-- track_data[, `:=`(track_id = condition, colour_group = condition)] -->

<!-- plotCoverage(igv.genes[[1]], track_data=track_data, mean_only = FALSE, alpha = 0.5) -->

<!-- plotTranscripts(igv.genes[[1]], rescale_introns=FALSE) -->
<!-- track_data = dplyr::mutate(sample_data, track_id = "RNA-seq", colour_group = condition) -->
<!-- ``` -->

### Circos

```{r, eval=FALSE}
files.sig = list.files(paste0(working.folder, "processed"), pattern="*_cnd_*", full.names=TRUE)

significant = rbindlist(lapply(files.sig,
                               function(x)
                                  fread(cmd=paste("gunzip -c", x), showProgress=FALSE)))
setnames(significant, "replicate", "Replicate")

sel.R = !overlapsAny(with(significant, GRanges(chr.R, IRanges(start.R, width=1))), blacklist.gr)
sel.D = !overlapsAny(with(significant, GRanges(chr.D, IRanges(start.D, width=1))), blacklist.gr)
significant = significant[sel.R & sel.D,]

subset = significant[Condition%in%"RADICL_mESC_1FA", list(gene_id.R, chr.R, start.R, start.R, gene_id.D, chr.D, start.D, end.D)]

selection = subset[, list(Intra=length(.SD[chr.R==chr.D, chr.D]), Inter=length(.SD[chr.R!=chr.D, chr.D]), Chr=length(unique(chr.D))), by=c("gene_id.R")]
setkey(selection, gene_id.R)
setkey(genes.dt, gene_id)
selection[genes.dt, `:=`(gene_name=gene_name, gene_type=gene_type)]
selection = selection[order(-Inter, -(Inter/Intra), -Chr)][Intra!=0 & Inter!=0]
fwrite(selection, file=file.path(working.folder, "RNA-BIN_significant_intra_inter_1FA.tsv"), quote=FALSE, sep='\t', row.names=FALSE)

selection = subset[chr.R==chr.D, list(Int=length(chr.D), Chr=length(unique(chr.D))), by=c("gene_id.R")]
setkey(selection, gene_id.R)
setkey(genes.dt, gene_id)
selection[genes.dt, `:=`(gene_name=gene_name, gene_type=gene_type)]
selection = selection[order(-Int, -Chr)]
fwrite(selection, file=file.path(working.folder, "RNA-BIN_significant_intrachromosomal_1FA.tsv"), quote=FALSE, sep='\t', row.names=FALSE)

selection = subset[chr.R!=chr.D, list(Int=length(chr.D), Chr=length(unique(chr.D))), by=c("gene_id.R")]
setkey(selection, gene_id.R)
setkey(genes.dt, gene_id)
selection[genes.dt, `:=`(gene_name=gene_name, gene_type=gene_type)]
selection = selection[order(-Int, -Chr)]
fwrite(selection, file=file.path(working.folder, "RNA-BIN_significant_interchromosomal_1FA.tsv"), quote=FALSE, sep='\t', row.names=FALSE)

subset = significant[!grepl("chrM", gene_id.D), list(Count=.N), by=c("Condition", "gene_id.R", "gene_id.D")]

rna_ids = copy(genes)
rna_ids = keepSeqlevels(rna_ids, paste0("chr", c(1:19, "X", "Y")), pruning.mode="coarse")
rna_ids = data.table(as.data.frame(rna_ids), key="gene_id")

dna_ids = copy(mm10)
dna_ids = keepSeqlevels(dna_ids, paste0("chr", c(1:19, "X", "Y")), pruning.mode="coarse")
dna_ids = data.table(as.data.frame(dna_ids), key="bin_id")

gene_ids = c("ENSMUSG00000030849.18",
             "ENSMUSG00000064899.1",
             "ENSMUSG00000065232.1",
             "ENSMUSG00000088088.1",
             "ENSMUSG00000092341.2",
             "ENSMUSG00000004637.15",
             "ENSMUSG00000069769.13",
             "ENSMUSG00000038518.15",
             "ENSMUSG00000021745.13",
             "ENSMUSG00000097039.8",
             "ENSMUSG00000085396.7",
             "ENSMUSG00000087259.7",
             "ENSMUSG00000057215.6",
             "ENSMUSG00000058589.14",
             "ENSMUSG00000032087.10",
             "ENSMUSG00000051910.13",
             "ENSMUSG00000106379.1",
             "ENSMUSG00000021010.8",
             "ENSMUSG00000106943.1",
             "ENSMUSG00000022607.14",
             "ENSMUSG00000024073.14",
             "ENSMUSG00000060579.12",
             "ENSMUSG00000022961.17",
             "ENSMUSG00000053414.7",
             "ENSMUSG00000037110.19",
             "ENSMUSG00000035392.17",
             "ENSMUSG00000092274.2")

for( gene in gene_ids ){
   gene_name = genes.dt[gene_id%in%gene, gene_name]
   gene_chr = as.character(genes.dt[gene_id%in%gene, seqnames])
   
   message(gene_name)
   
   breaks = seq(0, ceiling(log2(max(subset[, Count]))), 1)
   col_fun = colorRamp2(breaks, viridis(length(breaks)))
   lgd_links = Legend(at = seq(0, max(round(range(breaks))), 2),
                      col_fun = col_fun, type="grid", title_position = "topcenter", title = "log2(Count)", direction = "horizontal")
   lgd_list_vertical = packLegend(lgd_links)
   
   myset = subset[gene_id.R%in%gene,]
   
   if(nrow(myset)>1){
      # breaks = seq(log2(min(myset[, Count])), log2(max(myset[, Count])), length.out=9)
      # col_fun = colorRamp2(breaks, cividis(length(breaks)))
      # lgd_links = Legend(at = seq(min(round(range(breaks))), max(round(range(breaks))), length.out=5),
      #                    col_fun = col_fun, type="grid", title_position = "topcenter", title = "log2(Count)", direction = "horizontal")
      # lgd_list_vertical = packLegend(lgd_links)
      
      myset = split(myset, myset$Condition)
      
      bed1 = lapply(myset,
                    function(x)
                       cbind(rna_ids[match(x[, gene_id.R], rna_ids[, gene_id]),], rou1=0.8))
      for( i in seq_along(bed1) )
         setkey(bed1[[i]], gene_type)
      
      bed2 = lapply(myset,
                    function(x)
                       cbind(dna_ids[match(x[, gene_id.D], dna_ids[, bin_id]),], rou2=0.9))
      
      bed2 = lapply(seq_along(bed2),
                    function(x)
                       bed2[[as.numeric(as.character(x))]][, `:=`(value = log2(myset[[as.numeric(as.character(x))]][, Count]),
                                                                  colour = col_fun(log2(myset[[as.numeric(as.character(x))]][, Count])))])
      
      cytoband = read.cytoband(species="mm10")
      cytoband_df = cytoband$df
      chromosome = cytoband$chromosome
      
      xrange = c(cytoband$chr.len, cytoband$chr.len[gene_chr])
      normal_chr_index = 1:length(chromosome)
      zoomed_chr_index = length(chromosome)+1
      
      # normalize in normal chromsomes and zoomed chromosomes separately
      sector.width = c(xrange[normal_chr_index] / sum(xrange[normal_chr_index]),
                       xrange[zoomed_chr_index] / sum(xrange[zoomed_chr_index]))
      
      extend_chromosomes = function(bed, chromosome, prefix = "zoom_") {
         if( chromosome %in% as.data.frame(bed)[, 1] ){
            zoom_bed = bed[bed[[1]] %in% chromosome, , drop = FALSE]
            zoom_bed[[1]] = paste0(prefix, zoom_bed[[1]])
            return(rbind(bed, zoom_bed))
         }else{
            return(bed)
         }
      }   
      
      # pdf(paste0("plots/circos_BIN_", gene_name, "_significant_Links.pdf"), width=18, height=10)
      # layout(matrix(1:length(bed1), 2, length(bed1)/2))
      # for( i in seq_along(bed1) ){
      #    circos.clear()
      #    circos.par(start.degree = 90, gap.degree=c(rep(1, length(chromosome)-1), 5))
      #    circos.initializeWithIdeogram(plotType = c("axis", "labels"), species = "mm10")
      #    circos.genomicLabels(bed1[[i]][1,], side = "outside", labels=gene_name)
      #    circos.genomicLink(bed1[[i]], bed2[[i]], col=bed2[[i]]$colour, rou1=bed1[[i]]$rou1, rou2=bed2[[i]]$rou2)
      #    text(0, 1.2, names(bed1)[i], cex=2, font=2)
      #    title(names(bed1)[i])
      #    circos.clear()
      # }
      # pushViewport(viewport(x = unit(2, "mm"), y = unit(4, "mm"), 
      #                       width = grobWidth(lgd_list_vertical)*2, 
      #                       height = grobHeight(lgd_list_vertical), 
      #                       just = c("left", "bottom")))
      # grid.draw(lgd_list_vertical)
      # dev.off()
      
      pdf(file.path(working.folder, paste0("plots/circos_BIN_", gene_name, "_significant_Lines.pdf")), width=18, height=10)
      vec = if( length(bed1)%%2==0 ){ c(1:length(bed1)) }else{ c(1:length(bed1), 0) }
      layout(matrix(vec, 2, length(vec)/2))
      for( i in seq_along(bed1) ){
         circos.clear()
         circos.par(start.degree = 90)
         circos.initializeWithIdeogram(extend_chromosomes(cytoband_df, gene_chr), sector.width = sector.width)
         if( diff(range(bed2[[i]][, value]))!=0 ){
            circos.genomicTrack(extend_chromosomes(bed2[[i]][, list(seqnames, start, end, value)], gene_chr),
                                panel.fun = function(region, value, ...) {
                                   circos.genomicLines(region, value, type = "h")
                                })         
            circos.link(gene_chr, get.cell.meta.data("cell.xlim", sector.index = gene_chr),
                        paste0("zoom_", gene_chr), get.cell.meta.data("cell.xlim", sector.index = paste0("zoom_", gene_chr)),
                        col = "#00000020", border = NA)
            bed2[[i]][seqnames%in%gene_chr, seqnames := paste0("zoom_", seqnames)]
            circos.genomicLink(bed1[[i]], bed2[[i]], col=bed2[[i]]$colour)
            # circos.genomicLabels(extend_chromosomes(bed1[[i]][1,], gene_chr), side = "inside", labels=gene_name, connection_height = convert_height(3, "mm"))
         }
         text(0, 1.2, names(bed1)[i], cex=2, font=2)
         title(names(bed1)[i])
         circos.clear()
      }
      pushViewport(viewport(x = unit(2, "mm"), y = unit(6, "mm"),
                            width = grobWidth(lgd_list_vertical)*2,
                            height = grobHeight(lgd_list_vertical),
                            just = c("left", "bottom")))
      grid.draw(lgd_list_vertical)
      dev.off()
      
   }
}
```

### TFBS enrichment analysis (plot)

```{r TFBS, eval=FALSE}
FA1 = fread("TFBS_enrichment_analysis_binomial_model_1FA.tsv", header=TRUE)
FA2 = fread("TFBS_enrichment_analysis_binomial_model_2FA.tsv", header=TRUE)

# dt = rbindlist(list(`1FA`=FA1, `2FA`=FA2), idcol="Condition")
# 
# dt = dcast.data.table(dt, `SwissRegulon motif`~Condition, value.var=c("p-value", "FDR (p-value)"), fill=1)
# dt = dt[`FDR (p-value)_1FA`<0.05 | `FDR (p-value)_2FA`<0.05]
# 
# dt = dt[order((`p-value_1FA`+`p-value_2FA`)/2), index := seq_along(`SwissRegulon motif`)]
# 
# pl = melt.data.table(dt, id.vars=c("index", "SwissRegulon motif"), measure.vars=c("p-value_1FA", "p-value_2FA"))
# pl[, variable := gsub("p-value_", "", variable)]
# pl = pl[order(index)]

pl.1FA = FA1[`FDR (p-value)`<0.05]
pl.1FA[, Label := factor(`SwissRegulon motif`, levels=`SwissRegulon motif`)]
pl.1FA[, `:=`(N = -log10(`FDR (p-value)`)/2, OD = `odds ratio`)][, fill := "1"]
pl.1FA = melt.data.table(pl.1FA, id.vars="Label", measure.vars=c("N", "OD"))

gg = ggplot(pl.1FA, aes(x=Label, y=value, fill=variable)) +
   geom_hline(yintercept=1, lty=2, col="grey30") +
   geom_bar(stat="identity", position="dodge", col="black", alpha=0.8) +
   scale_x_discrete("") + scale_y_continuous("Odds Ratio", labels=scales::comma, sec.axis = sec_axis(~.*2, name = "-log10(P-value)"), position = "right") +
   scale_fill_npg(labels=c("Significance", "Odds Ratio")) + theme_bw() + theme(legend.position=c(0.9, 0.9), legend.title=element_blank(), axis.text.x=element_text(angle = 45, hjust = 1))
ggsave(gg, file="plots/TFBS_enrichment_analysis_1FA.pdf", width=10, height=6)

# gg = ggplot(pl, aes(x=as.factor(index), y=-log10(value), fill=variable)) +
#    geom_hline(yintercept=-log10(0.1), lty=2, col="grey30") +
#    geom_bar(position="dodge", stat="identity", col="black", width=0.8, alpha=0.8) +
#    scale_x_discrete("SwissRegulon motif", labels=unique(pl[order(index), `SwissRegulon motif`])) +
#    scale_y_continuous("-log10(P-value)") +
#    scale_fill_tableau() +
#    theme_few() + theme(legend.position=c(1, 1), legend.justification=c(1.4, 1.4),
#                        axis.text.x = element_text(angle = 45, hjust = 1)) +
#    labs(fill = "Condition")
# ggsave(gg, file="plots/TFBS_enrichment_analysis.pdf", width=10, height=5)
# ggsave(gg, file="plots/TFBS_enrichment_analysis.eps", width=10, height=5)
```

### Kcnq1ot1 gene body density

```{r rnaKcnq1ot1, eval=FALSE}
# Kcnq1ot1.gene = with(gff[gene_name%in%"Kcnq1ot1" & !(gene_feature%in%"intron"),],
#                      GRanges(seqnames, IRanges(start, end), strand))
Kcnq1ot1.gene = with(genes.dt[gene_name%in%"Kcnq1ot1",], GRanges(seqnames, IRanges(start, end), strand))
Kcnq1ot1.gene = resize(Kcnq1ot1.gene, width=width(Kcnq1ot1.gene), fix="center")

require("fedevis")

species = "Mus musculus"
version = "M14"

TxDb = getTxDb(species, version)
data = createAnnoTrack(TxDb, region=Kcnq1ot1.gene)
ar = as.data.table(resize(unlist(tile(with(data[["Exons"]], GRanges(seqnames, IRanges(start, end), strand)), n=10)), 2))
data[["Arrows2"]] = data.table(gene=data[["Exons"]][, gene], ar[-nrow(ar), ], Y=data[["Exons"]][, Y])

theme_blank <- function(...) {
  ret <- theme_bw(...)
  ret$line <- element_blank()
  ret$rect <- element_blank()
  ret$strip.text <- element_blank()
  ret$axis.text <- element_blank()
  ret$plot.title <- element_blank()
  ret$axis.title <- element_blank()
  # ret$plot.margin <- structure(c(0, 0, -1, -1), unit = "lines", valid.unit = 3L, class = "unit")
  ret
}

pl = plotAnno(data, reg.range=range(as.data.table(Kcnq1ot1.gene)[, list(start, end)]), clean=TRUE) +
   geom_segment(data=data[["Arrows2"]][strand=="-"], mapping=aes(x=end, xend=start, y=Y, yend=Y), colour="white", arrow = arrow(length = unit(0.2, "cm"), type="closed")) +
   scale_y_continuous(limits=c(-0.5, 0.5))

if( !file.exists(paste0("RNA-BIN_Kcnq1ot1_interactions_", bin_width, ".tsv.gz")) ){
   interactions = fread(paste0("gunzip -c RNA-BIN_biotypes_", gencode_version, "_level_1_2_3_AllOverlapsUnique_", bin_width, ".tsv.gz"))
   interactions = interactions[!(chr.R%in%"chrM" | chr.D%in%"chrM"),]
   writeGzip(interactions[gene_id.R%in%genes.dt[gene_name%in%"Kcnq1ot1", gene_id],],
             file=paste0("RNA-BIN_Kcnq1ot1_interactions_", bin_width, ".tsv"), col.names = TRUE)
}
Kcnq1ot1.interactions = fread(paste0("gunzip -c RNA-BIN_Kcnq1ot1_interactions_", bin_width, ".tsv.gz"), header=TRUE)
# Remove self-interactions
Kcnq1ot1.interactions = Kcnq1ot1.interactions[!(chr.D%in%"chr7" & start.D>=start(Kcnq1ot1.gene) & end.D<=end((Kcnq1ot1.gene)))]

Kcnq1ot1.coverage.all = coverage(c(with(Kcnq1ot1.interactions, GRanges(chr.R, IRanges(start.R, end.R), strand.R)),
                               Kcnq1ot1.gene))
Kcnq1ot1.coverage.1fa = coverage(c(with(Kcnq1ot1.interactions[Condition%in%"1FA"],
                                        GRanges(chr.R, IRanges(start.R, end.R), strand.R)),
                               Kcnq1ot1.gene))

dt = as.data.table(Kcnq1ot1.coverage.all[Kcnq1ot1.gene])
dt[, `:=`(value = value-1, pos = start(Kcnq1ot1.gene):end(Kcnq1ot1.gene))]

dt.1fa = as.data.table(Kcnq1ot1.coverage.1fa[Kcnq1ot1.gene])
dt.1fa[, `:=`(value = value-1, pos = start(Kcnq1ot1.gene):end(Kcnq1ot1.gene))]

gg = ggplot(dt, aes(x=pos, weight=value/sum(value), fill=as.factor(group))) +
   geom_density(adjust=0.1) +
   # scale_x_reverse("Genomic position", breaks=seq(start(Kcnq1ot1.gene), end(Kcnq1ot1.gene), length.out=8)) +
   scale_x_continuous("Genomic position", breaks=seq(start(Kcnq1ot1.gene), end(Kcnq1ot1.gene), length.out=8)) +
   scale_y_continuous("RNA tags density") +
   scale_fill_tableau() +
   theme_pubr() + guides(fill=FALSE)
gg = plot_grid(plotlist=list(pl, gg), nrow=2, axis="lr", align="v", rel_heights=c(0.4, 1))
ggsave(gg, filename=paste0("plots/RNA-BIN_Kcnq1ot1_RNA_tags_density_", bin_width, ".pdf"), width=12, height=6)

gg = ggplot(dt.1fa, aes(x=pos, weight=value/sum(value), fill=as.factor(group))) +
   geom_density(adjust=0.1) +
   # scale_x_reverse("Genomic position", breaks=seq(start(Kcnq1ot1.gene), end(Kcnq1ot1.gene), length.out=8)) +
   scale_x_continuous("Genomic position", breaks=seq(start(Kcnq1ot1.gene), end(Kcnq1ot1.gene), length.out=8)) +
   scale_y_continuous("RNA tags density") +
   scale_fill_tableau() +
   theme_pubr() + guides(fill=FALSE)
gg = plot_grid(plotlist=list(pl, gg), nrow=2, axis="lr", align="v", rel_heights=c(0.4, 1))
ggsave(gg, filename=paste0("plots/RNA-BIN_Kcnq1ot1_RNA_tags_density_", bin_width, "_1FA.pdf"), width=12, height=6)

gg = ggplot(dt, aes(x=pos, weight=value, fill=as.factor(group))) +
   geom_histogram(bins=200, col="black", lwd=0.5) +
   # scale_x_reverse("Genomic position", breaks=seq(start(Kcnq1ot1.gene), end(Kcnq1ot1.gene), length.out=8)) +
   scale_x_continuous("Genomic position", breaks=seq(start(Kcnq1ot1.gene), end(Kcnq1ot1.gene), length.out=8)) +
   scale_y_continuous("RNA tags count") +
   scale_fill_tableau() +
   theme_pubr() + guides(fill=FALSE)
gg = plot_grid(plotlist=list(pl, gg), nrow=2, axis="lr", align="v", rel_heights=c(0.4, 1))
ggsave(gg, filename=paste0("plots/RNA-BIN_Kcnq1ot1_RNA_tags_counts_", bin_width, ".pdf"), width=12, height=6)

gg = ggplot(dt.1fa, aes(x=pos, weight=value, fill=as.factor(group))) +
   geom_histogram(bins=200, col="black", lwd=0.5) +
   # scale_x_reverse("Genomic position", breaks=seq(start(Kcnq1ot1.gene), end(Kcnq1ot1.gene), length.out=8)) +
   scale_x_continuous("Genomic position", breaks=seq(start(Kcnq1ot1.gene), end(Kcnq1ot1.gene), length.out=8)) +
   scale_y_continuous("RNA tags count") +
   scale_fill_tableau() +
   theme_pubr() + guides(fill=FALSE)
gg = plot_grid(plotlist=list(pl, gg), nrow=2, axis="lr", align="v", rel_heights=c(0.4, 1))
ggsave(gg, filename=paste0("plots/RNA-BIN_Kcnq1ot1_RNA_tags_counts_", bin_width, "_1FA.pdf"), width=12, height=6)
```

### Figures

```{r Figures, eval=FALSE}
require("fedevis")

species = "Mus musculus"
genome = "mm10"
version = "M14"

TxDb = getTxDb(species, genome, version)

regions = list(Tfcp2l1 = GRanges("chr1", IRanges(118236095,119565783), gene_id="ENSMUSG00000026380.10"),
               Nanog = GRanges("chr6", IRanges(122619443,122785306), gene_id="ENSMUSG00000012396.12"),
               Kcnq1ot1 = GRanges("chr7", IRanges(142759759,143749117), gene_id="ENSMUSG00000101609.1"))
regions.dt = lapply(regions, as.data.table)

anno.pl = lapply(regions,
                 function(reg)
                    createAnnoTrack(TxDb, reg, min.unit=width(reg)/100))

anno.pl = lapply(seq_along(anno.pl),
                 function(i)
                    plotAnno(anno.pl[[i]], range(as.data.table(regions[[i]])[, list(start, end)])))

if( !file.exists(paste0(working.folder, "RData/Genomic_regions_to_plot.RData"))){
   # interactions = fread(paste0("gunzip -c RNA-BIN_counts_", gencode_version, "_level_1_2_3_AllOverlapsUnique_", bin_width, "_significant", fdr.th, "_extended_tsc_noDistance.tsv.gz"), showProgress=FALSE)
   
   files.sig = list.files(paste0(working.folder, "processed"), pattern="*_cnd_*", full.names=TRUE)
   
   significant = rbindlist(lapply(files.sig,
                                  function(x)
                                     fread(paste("gunzip -c", x), showProgress=FALSE)))
   setnames(significant, "replicate", "Replicate")

   setkeyv(significant, c("chr.D", "gene_id.R"))
   
   reads = lapply(regions.dt,
                  function(reg){
                     setkeyv(reg, c("seqnames"))
                     significant[reg, nomatch=0]
                  })
   
   reads = lapply(reads, function(x) split(x, x$Condition))
   reads = lapply(reads,
                  function(y)
                     lapply(y,
                            function(x)
                               with(x, GRanges(chr.D, IRanges(start.D, end.D)))))
   save(regions, reads, file=paste0(working.folder, "RData/Genomic_regions_to_plot.RData"))
}else{
   load(paste0(working.folder, "RData/Genomic_regions_to_plot.RData"))
}

tracks = lapply(seq_along(regions),
                function(i)
                   createTracks(reads[[i]][names(reads[[i]])%in%c("RADICL_mESC_1FA", "RADICL_mESC_4h_ActD", "RADICL_mESC_Control")], track.names=c("Protein-mediated", "ActD treated", "Non protein-mediated"), region=regions[[i]]))

anno.pl[[2]] = anno.pl[[2]] + scale_y_continuous(limits=c(-0.3, 0.3))

pl = lapply(seq_along(anno.pl),
            function(i)
               plot_grid(plotlist=c(tracks[[i]], list(anno.pl[[i]])), nrow=length(c(tracks[[i]], list(anno.pl[[i]]))), axis="lr", align="v", rel_heights=c(rep(1,length(tracks[[1]])), 0.8)))

for( i in seq_along(pl) ){
   pdf(file=paste0(working.folder, "plots/Figure3_", i, ".pdf"), width=16, height=10)
      print(pl[[i]])
   dev.off()
}
   


# 
# genes = genes(TxDb)
# genes = genes[!grepl("PAR", names(genes)),]
# 
# exons = exonsBy(TxDb, by="gene")
# exons = exons[!grepl("PAR", names(exons)),]
# 
# Kcnq1ot1_id = genes.dt[gene_name%in%"Kcnq1ot1", gene_id]
# Kncq1ot1_rd = with(interactions[Condition%in%"1FA" & gene_id.R%in%Kcnq1ot1_id], GRanges(chr.R, IRanges(start.R, end.R))
# 
# genes = genes[names(genes)%in%Kcnq1ot1_id,]
# exons = reduce(exons[names(exons)%in%names(genes)])
# 
# genes$Y = 0
# exons = lapply(exons, function(x){ x$Y = 0; x})
# 
#    myList = list()
#    myList[["Genes"]] = data.table(as.data.frame(genes), key="gene_id")
#    myList[["Exons"]] = rbindlist(lapply(exons, as.data.table), idcol="gene")
#    
#    arrows.pl = split(genes, names(genes))
#    arrows.pl = lapply(arrows.pl, function(x) data.table(as.data.frame(tile(x, width=25))))
#    arrows.pl = rbindlist(arrows.pl, idcol="gene_id")
#    setkey(arrows.pl, gene_id)
#    myList[["Arrows"]] = arrows.pl[myList[["Genes"]], Y := i.Y]
#    
# colorVar="grey30"
# reg.range = range(as.data.table(genes)[, list(start, end)])
# gene.height = 1
#    ggplot() +
#       geom_segment(data=myList[["Arrows"]][strand=="+"], mapping=aes(x=start, xend=end, y=Y, yend=Y), colour=colorVar, arrow = arrow(length = unit(0.1, "cm"), type="closed")) +
#       geom_segment(data=myList[["Arrows"]][strand=="-"], mapping=aes(x=end, xend=start, y=Y, yend=Y), colour=colorVar, arrow = arrow(length = unit(0.1, "cm"), type="closed")) +
#       geom_segment(data=myList[["Genes"]], mapping=aes(x=start, xend=start, y=Y-(0.05*gene.height)/2, yend=Y+(0.05*gene.height)/2), colour=colorVar, lineend="square") +
#       geom_segment(data=myList[["Genes"]], mapping=aes(x=end,   xend=end,   y=Y-(0.05*gene.height)/2, yend=Y+(0.05*gene.height)/2), colour=colorVar, lineend="square") + 
#       geom_rect(data=myList[["Exons"]], mapping=aes(xmin=start, xmax=end, ymin=Y-(0.05*gene.height), ymax=Y+(0.05*gene.height), fill=as.factor(1))) +
#       coord_cartesian(xlim=reg.range) +
#       scale_fill_tableau() + scale_colour_tableau() +
#       theme_few() + theme(axis.title.x=element_blank(), axis.text.y=element_blank(), axis.title.y=element_blank(), axis.ticks.y=element_blank()) + guides(fill=FALSE, colour=FALSE) 

```
